# 前端环境变量。**两个都可以留空**,留空时走 Vite 代理(同源),这是推荐配置。
#
# VITE_API_BASE_URL —— 只在前端与后端**不同源且不经代理**时才需要。
#   它是绝对地址、由浏览器解析,所以填了 localhost 就只有 docker 宿主机本机能用
#   (评审 P0-2)。除非你确实要跨源调用,否则不要填。
#
#   **跨源地址与浏览器登录(session 模式)不兼容 —— 会话部署只支持同源反代。**
#   后端的 Session Cookie 是 `SameSite=Lax`(backend/app/main.py 的 same_site="lax"),
#   跨站 XHR 根本不带它;前端的 `withCredentials: true` 改变不了这一点,
#   它管的是"同源/同站请求要不要带凭据",不是"跨站要不要带"。
#   真填了跨源地址的表现是:`/auth/login` 返回 200,随后**全站 401** ——
#   看起来像"登录成功了,然后立刻又要登录"。
#   要让跨源真正可用,后端得把 Cookie 改成 `SameSite=None; Secure` 并配好
#   带 credentials 的 CORS,那是另一个安全权衡(见 config.py 里
#   `CORS_ORIGINS=*` 那一段:今天正是 Lax 在兜着它)。在那之前,
#   这个变量只服务 token / dev 模式的部署。
# VITE_API_BASE_URL=

# VITE_PROXY_TARGET —— 开发服务器把 /api 与 /files 转发到哪里。
#   本机直接 npm run dev 时留空即可(默认 http://localhost:8000);
#   在 docker compose 里必须写服务名,容器里的 localhost 是前端容器自己。
VITE_PROXY_TARGET=http://localhost:8000
