# 行尾归一化。**全仓兜底,不是白名单。**
#
# ## 这一版之前是一张 5 条模式的白名单,盖住了 814 个文件里的 4 个
#
# 原来只写了 `*.sh` / `*.ps1` / `Makefile` / workflows / `*.bat` / `*.cmd`,
# 实测 `git check-attr` 的结果是:**4 个文件有规则,810 个 unspecified**。
# 那 810 个跟着打包那台机器的 `core.autocrlf` 走,而 Git for Windows 的
# 默认值是 `true`。
#
# 后果不是"diff 难看"这种审美问题,是**同一个版本在两台机器上打出来的包
# 内容不同**:Windows 跑 `tools\pack.ps1` 得到 810 个 CRLF 文件 + 4 个 LF 文件,
# WSL/Linux 跑 `tools/pack.sh` 得到全 LF。两个打包脚本本身都是逐字节复制的
# (`CreateEntryFromFile` / `zip`),它们没做错任何事 —— 它们忠实地把工作树的
# 分歧带进了交付物。找错方向去改打包脚本,改不出结果。
#
# ## 三处会真的坏掉,不是观感问题
#
#     .env.example -> .env   compose 的 `env_file` **保留行尾的 \r**。
#                            `POSTGRES_PASSWORD=imagegen\r` 让认证失败,而日志
#                            只说密码错、密码看起来又完全正确;
#                            `API_PREFIX=/api\r` 让健康检查打到 `/api%0D/health`,
#                            而 worker / beat 都 `condition: service_healthy`
#     Dockerfile             `RUN ... \` 的续行后面跟一个 \r 就断了(两个
#                            Dockerfile 各有 4 处续行)
#     .dockerignore          带 \r 的条目匹配不上任何路径
#
# `docs/DEPLOYMENT.md` §10.1 ① 早就把这三条里的第一条诊断出来了,而且
# **给出的建议规则就是下面这一行** —— 落地时只实现了白名单那一半。
#
# ## 为什么是 `eol=lf` 而不是 `text=auto` 了事
#
# 只写 `text=auto` 的话,存库归一到 LF,但 Windows 检出仍按 `core.autocrlf`
# 决定 —— 也就是把这件事重新交回给每个人的全局配置,而那正是要消掉的变量。
# `eol=lf` 让 **Windows 工作树也是 LF**,于是"打包机器是谁"不再影响产物。
#
# 今天的 VS Code、PowerShell 7、Python、Node、GNU make 全都读得动 LF;
# 真需要 CRLF 的只有下面那两类 Windows 原生启动器。
#
# 规则**从上往下越靠后越优先**,所以兜底必须写在最前面。
* text=auto eol=lf

# ---- 打包入口:兜底已经盖住了,这三行留着是因为有守卫按字面量盯着 ----
#
# `tests/pure/test_a45_batch11_fixes.py::test_packaging_entrypoints_are_pinned_to_lf`
# 逐条断言这三行存在。删掉它们会让那条守卫红在一个行为完全正确的仓库上,
# 而重复一次的代价是零 —— 它们和兜底同向,不是覆盖。
*.sh text eol=lf
*.ps1 text eol=lf
Makefile text eol=lf

# CI 在 Linux 上跑这些文件,即使包是在 Windows 上打的。
.github/workflows/*.yml text eol=lf
.github/workflows/*.yaml text eol=lf

# ---- 唯一必须 CRLF 的:Windows 原生启动器 ----
# cmd.exe 对 LF 结尾的 .bat 行为不可靠(多行脚本尤其)。
*.bat text eol=crlf
*.cmd text eol=crlf

# ---- 二进制:git 一个字节都不许动 ----
#
# `text=auto` 的启发式本来就认得出它们,写出来是为了让这件事变成一条
# **读得到的规则**而不是一个推断 —— `docs/ARCHITECTURE.md` 里"为什么用
# Mermaid 而不是 PNG"那段的论据正是「`.gitattributes` 把 `*.png` 标成 binary」,
# 而在这一版之前那句话是假的:文件里根本没有这一行。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.bmp binary
*.ico binary
*.tif binary
*.tiff binary
*.psd binary
*.pdf binary
*.zip binary
*.xlsx binary
