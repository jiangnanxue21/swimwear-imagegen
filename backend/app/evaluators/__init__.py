"""候选图评分器(需求第十、十一、十二章)。

分层刻意做得很薄:

    base.py     接口与传输结构
    scoring.py  权重与总分 —— 总分由后端算,不采信大模型自报的那个数
    rules.py    A/B/C/D 分档,纯函数
    repair.py   问题代码 -> 确定性修复参数
    ranking.py  轮内预排序(需求第十三章),可关闭
    mock.py     离线评分器
    vision.py   真实视觉大模型骨架
    registry.py 按名字取评分器

分档与修复都是纯函数,不碰数据库、不发网络请求。
这样"为什么这张图被判成 C"永远可以在一次单元测试里复现。
"""
