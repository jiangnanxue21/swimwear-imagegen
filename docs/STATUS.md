# 当前状态

<!-- DELIVERY_STAGE: 4 -->
<!-- CODE_STAGE: 6 -->
<!--
  ## 两个标记,两件事(2026-08-09 评审 F-04 修正)

  这里原来只有 `DELIVERY_STAGE`,而它的说明写的是"本仓当前落码到 PRD §13 的
  第几阶段"。**那句话已经不成立**:阶段 5 与阶段 6 都已全部落码,而这个数字
  停在 4 —— 停在 4 的理由写在下面几节里,是"推到 5 会让 11 条列写入欠账
  + 3 条欠账守卫当场逾期变红,而那 14 条都不是阶段 5 的交付项"。

  也就是说:门禁读到的不是"代码推进到哪",是"**能让门禁保持绿的那个数**"。
  自称 A 而实际是 B 的标记,比没有标记更糟 —— 读它的人会以为阶段 5、6 没开工。

  修法不是把 4 改成 6(那会让 14 条欠账当场逾期,而它们确实还没还),
  是**把两件事拆成两个标记**:

    DELIVERY_STAGE   **欠账结算阶段**。语义:还款日 ≤ 它的欠账必须已经还清。
                     `verify_delivery.py` 的「欠账守卫都在还款日之内」读它。
                     它是一条**闸**,不是一份进度报告。
    CODE_STAGE       **已落码阶段**,如实描述代码推进到 PRD §13 的第几阶段。
                     不参与任何闸,只负责让上面那个落差看得见。

  两者的差(6 − 4 = 2)就是欠着的账。`check_stage_markers_are_consistent()`
  钉着 `CODE_STAGE >= DELIVERY_STAGE` —— 反过来意味着"结算跑到了落码前面",
  那只可能是有人为了让门禁绿而调高了闸。

  改 `DELIVERY_STAGE` 之前先想一遍:每往上加一,所有写着「还款日:阶段 N ≤
  新值」的欠账守卫会当场变红。**那是它存在的全部意义。** 变红时该做的是
  还上那笔欠账,或者把还款日往后改并写清为什么改 —— 不是回来把这个数字调回去。

  改 `CODE_STAGE` 的判据只有一条:PRD §13 那一阶段的交付项是不是都落码了。
  它和欠账无关 —— 把它也调低来"保持一致"就是在重犯上面那个错。
-->

> ## 2026-08-09 回归收口：Mock / Simulator 已进入人工测试
>
> 本轮把评审点名但尚未落地的四段链路补齐：CSV 导入只接受已存在的 SPU / 颜色
> （单色可无歧义补齐，多色必须给 `variant_code`）；SPU、颜色和 SKU 支持版本化修改/
> 追加；属性识别改为 Celery 异步执行，具备取消、逐图成绩单、失败颜色精确重试、
> relay/reaper 与重试续跑；导入 preview→commit 使用独立 HMAC 签名，绑定操作者、
> 文件摘要和数据库预览状态。预览现在也读取真实 SPU / 颜色事实，不会再出现
> “预览说新增、提交才说 SPU 不存在”。迁移 head 为 `0054`。
>
> 实跑证据：PostgreSQL + Redis 全量 pytest **3128/3128**；纯测试 **2740/2740**；
> `verify_delivery` 在该次验收中 **19/19**；样例自检 **10/10**；mutation anchors **553/553**，
> 本次受影响的 4 份 mutation runner **87/87 条退化验红**；
> 架构契约 **3/3**；前端 typecheck / lint(0 error，4 个既有 warning) / Vitest
> **100/100** / build / syntax **93/93**；Chromium Playwright **6/6**；隔离 Redis
> 逻辑库上的真实 Celery worker `health.ping` 返回 SUCCESS。真实 HTTP 启动后
> `/api/health=ok`，SPU 列表返回 3/3。
>
> 已创建独立 `swimwear_imagegen_uat` 并升到 `0054`，播种后有 3 个 SPU、6 个颜色、
> 19 个 SKU、21 条素材；启动命令与人工步骤见 `LOCAL_MANUAL_TEST.md` §1.1 / §6。
> **Mock Provider + mock evaluator + Simulator 渠道现在可以开始人工测试。**
>
> 外部环境项仍有 Docker：本机没有 Docker CLI，`docker build` 两个镜像及
> compose 六服务未执行；真实 FASHN / 真实渠道也没有凭据，因此未调用、未计费。
> 另外，本轮文档复验发现仓库根当前存在 `.env`，`verify_delivery` 因此为
> **18/19**；本轮未读取或删除它。交付前必须把凭据移出仓库树，若里面是真实凭据
> 还要先轮换。正式 P0 冻结不能把这项和 Docker 一起写成已关闭。
>
> **这份 Mock 人工测试准入不等于 PRD 阶段 2 验收关闭。**
> `MODEL_REFERENCE` 绕行缝已于 2026-08-11 关闭(见下方「已知限制」那一行与
> `AC-VERIFICATION.md` §3):`generation_service` 那条分支从"告警后直接返回"
> 改成拒绝创建任务,worker 里那条"拿不到模板图就退回自由上传模特图"的兜底
> 也一并删掉 —— 后者更隐蔽,它发生在四道检查都过了之后。
> 阶段 2 仍未关闭的是**别的**项(见「已知限制」),别把这一条读成整段验收关闭。

> ## 2026-08-09 评审修复:F-12/F-4 颜色维可操作
>
> 完整决定见 `DECISIONS.md` §3.64。七步向导的 `?color=` 现在是操作上下文,
> 不再只是展示标签:颜色选择器写入 URL,聚合接口由后端把颜色码解析成同 SPU 的
> 确定 SKU,文案、属性、素材与图片集面板都使用这个目标。没有显式颜色时继续以
> 后端 `wizard.focus_color` 为默认;目标颜色没有 SKU 时明确停住,不借用别色数据。
>
> 生成方案默认预选当前颜色作用域,素材上传默认挂到当前颜色;跨色写入后失效入口
> 商品的聚合缓存,刷新仍留在同一个向导 URL。真实 PostgreSQL 全量 pytest
> **3109/3109**、前端 Vitest **97/97**、Playwright **3/3** 均通过。
> 本项不需要数据库迁移,`DELIVERY_STAGE` 仍是 **4**。

> ## 2026-08-09 评审修复:F-17 方案编辑、纯测试执行器与 mutation anchors
>
> 完整决定见 `DECISIONS.md` §3.63。生成方案的作用域现在有两个明确槽位:
> 一个 ACTIVE + 一个可编辑 DRAFT。重复保存相同内容复用同一行,修改草稿原位更新;
> 并发首次保存由 savepoint + 唯一索引收口,不再把 `UniqueViolation` 暴露成 500。
>
> 数据库迁移为 `0052_generation_plan_draft_slot`;降级到 `0051` 时若两个槽位并存,
> 归档 DRAFT、保留 ACTIVE。纯测试执行器恢复 **2728/2728**,mutation anchors
> **553/553**。真实 PostgreSQL + Redis 环境下全量 pytest **3106/3106** 通过;
> 前端门禁结果见本批交接记录。
>
> `DELIVERY_STAGE` 仍是 **4**。本批只修复评审中的 F-17 与两项验证阻断;
> §3.62 的 F-12/F-4 颜色维交互等其余评审项没有借此宣称完成。

> ## A45-batch29 / batch30:阶段 6 收官(6-4 七步向导 + 6-5 费用与影响提示)
>
> 逐条说明见 `DECISIONS.md` §3.59(6-4)与 §3.60(6-5)。
> **阶段 6 的五批到此全部落码**,分批表见 `REVIEW-STAGE6-CONCLUSION.md`。
>
> ```
> app/workbench/wizard.py            七步向导判定 + AC-05 动作闸(零依赖)
> app/workbench/impact.py            AC-17 影响提示(口径全取自 stale_matrix)
> app/workflows/cost_estimate.py     费用预估判定(未配价 = None,不是 0)
> app/workbench/cost_rollup.py       预估取数:ColorView -> Demand
> /products/{id}/flow 的 wizard 块   七步 + 阻塞 + 费用,**不新开端点**
> /workbench/change-sources          变更源清单(GET)
> /products/{id}/impact              这次修改会让什么失效(GET,只读)
> src/pages/WizardPage.tsx           /wizard/:id,?step= / ?color= 承担刷新恢复
> ```
>
> ### ⚠ 两处口径变更,都会改运营看得见的数字
>
> **1. 没配生成方案的商品,阻断数从 0 变成 1。**
>
> batch27 把 `SETUP` / `PLAN` 两步的 `Issue` 误传进了 `StepResult` 的第三个
> 位置参数(那是 `summary`),于是:出参里 `summary` 是一个**对象数组**
> (前端类型写的是 `string`,React 渲染它会抛错 —— 归属未挂或没配方案的
> 商品,**总览标签页整块打不开**);`blocking_count` 漏数那两步的阻断。
> 本批修好了,那条阻断第一次被数进去。按阻断数排序的列表顺序会变。
>
> **2. HTTP 上的「生成文案」变严了。**
>
> 原来只要求"有任意一个已确认属性",现在要求属性步整体 DONE ——
> 与向导上那个按钮亮不亮的判据完全一致(AC-05 的原话:服务端按同一份
> 判定拒绝,而不是仅前端置灰)。**批次执行不受影响**(它走 service 层,
> 有自己的跳过判定)。
>
> ### 费用预估:未配价显示「未配价」,不显示 ¥0
>
> 默认 Mock 一个都不在价目表里,所以开发环境的向导会说"1 项未配价"。
> 那是对的 —— 它正是"这套配置下我们不知道要花多少"。把它显示成 ¥0
> 是一句有具体数值的假话。
>
> ### 跑过的
>
> ```
> 纯逻辑              2710/2710(batch28 时是 2667)
> 交付自检            17/17      样例数据 10/10   Ruff 全绿   架构契约 3/3
> 前端                typecheck / lint 0 错 4 warning(与上批同) /
>                     Vitest 95/95(batch28 时是 82)/ build / syntax-check 全绿
> ```
>
> **没跑**:真库 pytest 与 Redis / Celery 集成 —— 本轮按仓库约定
> (根 `CLAUDE.md`「本地真实基础设施验证须由用户明确触发」)未执行。
> 需要复跑的范围与命令见本节末「怎么验证」。**浏览器未实测**,
> 与任务 20-A 同一状态(Playwright 在任务 24)。
>
> **`DELIVERY_STAGE` 仍是 4。** 阶段 5 的到期账(11 条列写入 + 3 条守卫)
> 一条都没还 —— "阶段 6 五批落码完毕"与"标记可以推进"是两件事。

> ## A45-batch28:阶段 6 批次 6-3 —— 聚合工作流 API(颜色子态终于有人读了)
>
> 逐条说明见 `DECISIONS.md` §3.58。
>
> 6-1 交付 `color_flow` 时明说「今天零生产调用点,等 6-3」。这一批接上了三跳:
>
> ```
> app/workbench/color_rollup.py     取数层:上游 -> ColorView(新增)
> /products/{id}/flow 的 colors 块  端点:不新开第二个(AC-15 的原话)
> OverviewTab 的颜色维矩阵          界面:每个颜色一行,拦路的点名
> ```
>
> **三跳各有守卫,最后一跳另有 4 条前端组件用例** —— batch24 刚为
> 「后端算好、前端零渲染点」付过账,只钉到端点为止就是同一个坑再来一次。
>
> ### 接线时当场发现并修的一处不一致
>
> `color_flow._plan` / `_copy` 把「上一步没就绪」判成 BLOCKED,而 `_image_set`
> 判 TODO。后果:一个刚建档的 SPU **每个颜色都在拦路**,阻断清单退化成噪音,
> 真正缺图的那个颜色淹在里面。已统一为 TODO 并补守卫。
>
> 它是**接真库聚合时**被顶出来的 —— 纯层 16 条守卫当时全绿。
>
> ### 跑过的
>
> ```
> 全量 pytest(真库)   3016 passed, 0 failed, 0 skipped
> 纯逻辑              2667/2667   Ruff 全绿   契约 3/3
> 前端                typecheck / lint 0 错 4 warning / Vitest 82/82 / build 全绿
> ```
>
> **没有做**:费用展示(6-5)、刷新恢复与向导页面本身(6-4)。
> 今天的颜色维挂在总览页,不是向导。`DELIVERY_STAGE` 仍是 4。

> ## A45-batch27:阶段 6 批次 6-2 —— 七步增维(**完成度口径变了,见下**)

>
> 逐条说明见 `DECISIONS.md` §3.57(含这一批的来历:判定层的 205 行来路不明,
> 由本批接管补齐),两个新步骤「完成」的定义见 PRD §14.1。
>
> ### ⚠ 口径变更公告:完成度的百分数会变
>
> 五步等权(每步 20 分)变成七步不等权:
>
> ```
> SETUP 建档 5 / MATERIAL 素材 15 / ATTRIBUTE 属性 25 / PLAN 方案 10
> IMAGE_SET 图片集 20 / COPY 文案 15 / DRAFT 草稿 10   = 100
> ```
>
> **同一件商品的百分数会变。** 素材与属性都做完的那一件,从 40% 变成 45%;
> 起点从 0 变成 5(归属挂好本身就是一件已经做完的事)。
>
> 这不是显示细节:完成度驱动**列表排序、批量筛选与审阅队列**,运营按它找
> "快好了的那些" —— 口径一变,他昨天记住的那批商品今天不在原来的位置。
> 上线当天请提前告诉运营一句,不要让他自己发现。
>
> **两个新步骤没有宽松默认值**:归属没挂好、方案没选的商品拿不到那 15 分,
> 由 `test_the_two_new_steps_are_not_free_points` 钉着 —— 否则所有存量商品
> 会凭空涨分、列表整体上移,而没有任何人做过任何事。
>
> ### 七步就是 PRD §16 那条管线逐行
>
> ```
> 1 SETUP      创建 SPU / ColorVariant / SKU
> 2 MATERIAL   按颜色上传原始样品
> 3 ATTRIBUTE  异步识别并确认共享事实与颜色事实
> 4 PLAN       选择模特与生成方案
> 5 IMAGE_SET  按颜色生成、核对并批准图片
> 6 COPY       生成并批准 SPU Listing 与颜色字段
> 7 DRAFT      形成完整 Draft
> ```
>
> ### 两条埋在前面批次的守卫,在这一天红了
>
> ```
> 6-1 埋的   PLAN 一进 FlowStep 就红 -> 提醒 color_flow.COLOR_STEPS 也要加
>            (方案是颜色级的)。加完换方向:钉两处同时有 PLAN
> 26 埋的   「今天是五步」红 -> 失败信息里列着要同批交付的四件事,四件都做了
>            现在换成钉七步与 PRD §16 管线逐行对应
> ```
>
> ### 跑过的
>
> ```
> 全量 pytest(真库)   3002 passed, 0 failed, 0 skipped
> 纯逻辑              2658/2658   交付 17/17   Ruff 全绿   契约 3/3
> 前端                typecheck / lint 0 错 4 warning / Vitest 78/78 / build 全绿
> ```
>
> **`DELIVERY_STAGE` 仍是 4。** 阶段 5 的到期账一条都没还。

> ## A45-batch26(6-2 前置):照着自己写的方法跑了一遍,红名单是空的

>
> 逐条说明见 `DECISIONS.md` §3.56。
>
> 6-1 给 6-2 定的开工方法是「先跑一遍让穷举契约测试变红,红的清单就是要
> 改的清单」。照做了:给 `FlowStep` 加上 `SETUP` 与 `PLAN`,跑 2650 条纯用例
> —— **后端一条都没红**(3 条红的里 2 条是前端契约测试,1 条是 6-1 自己
> 埋的 `_PLAN_PENDING`)。
>
> ```
> STEP_ORDER    加了成员不进来 -> 这一步在流程里不存在
> STEP_LABELS   加了成员不进来 -> 界面上没有名字
> 完成度        `STEP_WEIGHT = 20.0  # 5 步 × 20 = 100` 是**算术,不是断言**
>               七步之后合计 140,而完成度驱动列表排序与审阅队列
> evaluate()    `by_step` 是手写的五项字典
> ```
>
> ### 交付
>
> ```
> 守卫   tests/pure/test_a45_batch26_step_table_exhaustiveness.py,7 条
> 改动   STEP_WEIGHT(常量)-> STEP_WEIGHTS(逐步表)。**口径一个字没改**
>        (仍五步等权、合计 100),换的是它能不能被断言
> 反证   恢复那两个枚举成员,四条守卫同时变红(此前是零);恢复后全绿
> ```
>
> 其中一条守卫(`..._still_number_five_until_6_2_says_otherwise`)会在增维
> 那天红,它的失败信息里写着 6-2 要**同批**交付的四件事。
>
> ### 跑过的
>
> ```
> 全量 pytest(真库)   3001 passed, 0 failed, 0 skipped
> 纯逻辑              2657/2657   make check-offline 退出 0   交付 17/17
> ```
>
> **七步增维本身仍未开始**,`FlowStep` 今天是五步,`DELIVERY_STAGE` 仍是 4。

> ## A45-batch25:阶段 6 开工(批次 6-1)—— 先立判据,再动最底下那一层

>
> 逐条说明见 `DECISIONS.md` §3.55,判据签认、五批分批与 6-2 的风险清单见
> `docs/REVIEW-STAGE6-CONCLUSION.md`。
>
> ### 交付
>
> ```
> 判据    PRD §14.1 增补「阶段 6 仓内执行版」六条(AC-01/05/14~17,签认 2026-08-09)
>         —— 原文不可得,沿用阶段 5 对 AC-10/11/18/19 的同一处理方式
> 代码    app/workbench/color_flow.py:颜色子态判定层,零依赖纯函数
> 守卫    tests/pure/test_a45_batch25_color_substate.py,16 条
> ```
>
> ### 明说的那件没做的事
>
> `color_flow` **今天零生产调用点**,消费者是 6-3 的聚合 API。
> 本仓为"算好了没人读"付过十二次账,而那十二次的共同点不是落了一半,
> 是**没有人说它落了一半**。
>
> ### 为什么七步没在这一批
>
> `STEP_WEIGHT = 20.0`(5 步 × 20 = 100)改成七步之后,同一件商品的完成度
> 从 80% 变成 71%,而完成度驱动列表排序、批量筛选与审阅队列。
> 它单独作为 6-2,连同必须同批给出的新权重表、口径变更公告与合计守卫。
> `FlowStep` 有 113 处引用,6-2 开工的正确顺序是**先跑一遍让穷举契约测试
> 变红,红的清单就是要改的清单**。
>
> ### 跑过的
>
> ```
> 全量 pytest(真库)   2994 passed, 0 failed, 0 skipped
> 纯逻辑              2650/2650,0 跳过
> make check-offline  退出 0,14 项全跑   交付 17/17   Ruff 全绿   契约 3/3
> ```
>
> **`DELIVERY_STAGE` 仍是 4。** 阶段 5 的到期账(11 条列写入 + 3 条欠账守卫)
> 一条都没还 —— 阶段 6 开工不改变这件事。

> ## A45-batch24:三位外部评审的复核 —— 三条接缝各接了一半,已收口

>
> 逐条说明见 `DECISIONS.md` §3.54(缺口证据链、方向选择、没做的事),
> 五项与批次的对应、AC 证据与仍欠的账见
> `docs/REVIEW-STAGE5-5-1-CONCLUSION.md`(§四 / §五已由 batch24 订正)。
>
> ### PRD §13 阶段 5 的五项交付
>
> | 交付项 | 批次 | 状态 |
> |---|---|---|
> | 颜色结构化字段注册表项与确认流 | 5-4 / batch23 + **24** | ✅ 真库验过。**两条确认路径都验过**:batch23 只接了人工确认,自动确认(识别合并)出来的颜色 `display_name` 仍恒空;batch24 把投影挪到事实的写入边界 |
> | 来源追溯 | 5-4 / batch23 | ✅ 事实行保留 source / confirmed_by / 指纹 |
> | `color_sku_image_map` 与 `upstream_versions` 快照 | 5-1 + 5-2B | ✅ 真库验过(0048↔0051 升降级、JSONB 落库读回、STALE 派生) |
> | READY 门禁扩展 | 5-2B / batch20 | ✅ `ready_problems()` 合取入口,真库验过 |
> | 导出预览 | 5-5 / batch22 + **24** | ✅ **预览与最终导出同源**。batch22 只保证了预览不重新推断,而最终导出仍在自己挑图(显式主图 vs 排序第一张);batch24 让行级图片只从已存映射翻译,并补上运营侧渲染 |
>
> ### batch24 收口的三条(全部有真库用例 + 反证)
>
> ```
> F-1    自动确认的颜色事实不投影 display_name —— 投影挂在 confirm() 而不是写入边界
> F-6    文案的键按颜色分、存储按 SPU 分 —— 多颜色 SPU 每次点生成都重新付费,
>        两个颜色的文案互相挤成历史。迁移 0051 把存储下沉到颜色粒度
> AC-19  预览读已存映射、导出自己按 sort_order 挑图 —— 合法且 READY 的图片集
>        也会让预览显示 A 图、Excel 用 B 图
> ```
>
> 三条都做过反证:把代码改回缺口形状,对应用例确实变红
> (F-1 → display_name 恒空;F-6 → 三次点击落三版;AC-19 → 两边取到不同的图)。
>
> ### batch24 同时修的账目与门禁
>
> ```
> F-2   DECISIONS.md 的 §3.52 逐字重复两遍 → verify_delivery 判负、
>       make check-offline 在这一步中止,后面五道门禁一次都没跑过。已删重复段
> F-7   preview.images 后端算好、前端零渲染点 → DraftTab 接上,含 4 条组件用例
> F-8   display_name 已还但结论文件仍列在「仍欠(12 条)」→ 订正为 11 条;
>       template_checksum 与 batch14_9 到期未还且未重新认领 → 改判阶段 6 并写清理由
> F-3/F-5/F-9  三处过期的前向引用注释、两处俄语词
> ```

>
> ### 三笔账各自的形状
>
> ```
> 5-3  幂等单元不含尺码。S/M/L 三行各点一次 = 三次真实 LLM 调用、三个版本,
>      输入完全相同。批量那条路早就对了,缺口只长在**单件入口**上 ——
>      而那是运营日常用的那一个。迁移 0050,真库计数证明少了两次调用
> 5-5  预览的正确性不在"算得准",在**"算不了"**:`image_preview()` 的签名里
>      只有落库的那份映射,拿不到图片集与颜色表,想重新推断也无从下手
> 5-4  `ColorVariant.display_name` 恒空躲了几批,靠的是**一个不存在的字段名**
>      (`standard_color_name`;注册表里是 `primary_color`)。后端 5 处读、
>      前端 7 处显示,而值恒为空串
> ```
>
> ### D1:`DELIVERY_STAGE` 仍然是 **4**
>
> 前几批是「交付项没落完」。batch21/22/23 是**落完了**而标记仍不能推;
> batch24 之后这个判断不变,而理由多了一层:落完的那五项里有两项此前
> 只做了一半(见上表),说明"落码完毕"这个说法本身要靠守卫兜着才可信。
> 标记仍不能推的直接原因照旧:

> `还款日:阶段 N` 的语义是「推进到 N 之前必须还清」,推到 5 会让
> **11 条列写入欠账 + 3 条欠账守卫当场逾期变红**。
>
> 那 14 条**都不是阶段 5 的交付项** —— 识别侧 token 计量四列、原始响应留存
> 四列、几个缺入口的 UI 字段,当初被随手写上"阶段 5"这个还款日。
>
> **「五项交付落码完毕」与「标记可以推进」是两件事。** 把标记推上去再回来
> 改还款日,是本文件顶部那段注释明令禁止的做法。
>
> ### D2:同一个坑的第七、第八、第九次 —— 而三次都是守卫咬自己
>
> ```
> batch21  docstring 里「`_content_plan_or_raise` 必须留在判定之后」被当成代码位置
> batch22  按文本扫 `coverage`,而模块文档正写着「不许出现它」
> batch23  docstring 里「这里不 commit」让 `assert "commit" not in body` 假红
> ```
>
> 出现九次说明它不是疏忽,是**注释密度高的必然副作用**:凡是读源码的守卫,
> 第一步就该剥注释,而不是等它咬。
>
> ### D3:一条变异证伪了一条自己写的理由
>
> batch23 的 B3 无论怎么写都验不红 —— 它对应的缺陷不存在:
> SQLAlchemy 的脏检查本来就会折叠"赋一个和当前值相同的值"。
> 处理方式是**删掉那条变异并把 `needs_update` 的理由改对**,
> 不是把断言改松让"10/10"这个数字好看。
>
> ### 跑过的(A45-batch24 本轮实跑)
>
> ```
> 全量 pytest(真库)   2978 passed, 0 failed, 0 skipped
> 纯逻辑              2634/2634,0 跳过
> 迁移               0048 → 0051 升;0051 → 0050 → 0048 降;再升。单 head = 0051
> 变异               batch24 12/12(新增)  batch23 9/9   batch22 10/10
>                    batch21 13/13   batch20 15/15   batch19 13/13
> 反证               三条缺口各改回原形,对应用例确实变红
>                    (display_name 恒空 / 三次点击落三版 / 两边取到不同的图)
> 锚点               553/553(31 份脚本)   源码守卫 589   列写入 542(28 条台账)
> 交付               17/17   样例 10/10   imports 457   架构契约 3/3   Ruff 全绿
> make check-offline 退出 0,14 项全跑(此前停在第 2 步,后面五道一次没跑过)
> 前端 typecheck / Vitest 78/78 / build / syntax 89/89 全绿,lint 0 错 / 4 条既有 warning
> ```
>
> **变异脚本本轮改过一处口径。** `run_db` 原来把 pytest 退出 0 一律读成
> GREEN,而 `requires_db` 在库连不上时**跳过**整份用例、同样退出 0 ——
> 本轮容器里的 PostgreSQL 中途挂过一次,batch21 于是报出 6/13,
> 看起来是七条守卫失效,实际是七条根本没跑。现在"没跑"单独一档、不计入分母。
> 这正是 `vitest.config.ts` 里那句「空结果不能读作通过」的同一条道理,
> 只是这一次轮到 Python 侧。

>
> **仍未执行**:Docker build、Playwright、真实平台环境。
>
> **AC-10/11/18/19 四条现在都有真库自动化证据,但一条都不能标完整通过。**
> 「自动化验过」不等于「真环境验收过」—— 后者要真实样品、真实平台、真实模型,
> 一步都没动。AC-01～AC-22 完整通过仍是 **0/22**,阶段 P0 未关闭。

> ## A45-batch20:阶段 5 批次 5-2B —— 颜色维接线,以及这台机器上第一次跑到真库
>
> 逐条说明见 `DECISIONS.md` §3.52,排期与认领见
> `docs/REVIEW-STAGE5-5-1-CONCLUSION.md`(**那份文件此前被三处引用却从未提交**,
> 本批补上,`audit_doc_refs` 因此从红转绿)。
>
> ### 本批开工时先数了一个数,它决定了整批的形状
>
> **全仓零条测试调用过 `build_draft`。** 也就是说接线做完之后,整套真库用例
> 照样全绿 —— 它们一条都不走这条路。所以本批交付的不只是接线,
> 还有这条链路的**第一份**执行证据(`tests/test_a45_batch20_draft_color_axis_db.py`,
> 9 条真库用例)。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `app/workbench/upstream_collect.py`(新,取数层) | ✅ 落码并真库跑过 |
> | `build_draft` 写两列 | ✅ 真库验过落库与读回 |
> | `refresh_draft` 的颜色轴(`diff_upstream`) | ✅ 真库验过新增/停用/PLANNED 三种动线 |
> | READY 门禁走 `ready_problems()` 合取入口 | ✅ 两侧各有真库用例 + 变异 |
> | 图片集轴从「被比较」搬到「记下但不比」(§3.50 D3 第四次应用) | ✅ 正反两条守卫 |
> | `image_set_service._to_view` 改公开 `to_view` | ✅ |
> | 欠账偿还:2 条 LEDGER 列 + 2 条欠账守卫(翻转,不删) | ✅ |
> | 守卫 31 条 + 变异 15/15 | ✅ 第一轮 11/15,见下 |
>
> ### D1:`DELIVERY_STAGE` 仍是 4
>
> 阶段 5 五项交付里,落码的是第三项(快照 + 接线)与第四项(READY 门禁扩展)。
> 颜色结构化字段注册表项与确认流(5-4)、来源追溯、导出预览(5-5)都没开工。
> `audit_column_writers.LEDGER` 上还有 12 条写着「还款日:阶段 5」。
>
> ### D2:变异第一轮 11/15,四条 GREEN 各是一种守卫自己的毛病
>
> ```
> B2  按文本扫写入点   `pass  # row.xxx = ...` 里的注释喂真了断言(同型第六次)
> C4  跑错运行器       门禁读错映射这件事纯层看不见,该由真库套件跑
> D4  变异写得不对     写成"删掉早返回",而要防的是"反查 SPU 行"那个形状
> E2  守卫够不着       钉的是判定层的 builder,而变异改的是取数层
> ```
>
> B2 是**同一个坑在本仓的第六次**(§3.26 / 14-13 / 14-22 / batch18 C2 /
> batch19 caller 守卫)。修法沿用:从输入里把注释去掉,判据换成 `ast` 的赋值目标。
> D4 改成插入 `select(Spu).where(Spu.spu_code == product.spu)` 那个真形状,
> 并**跑拥有那条规矩的套件**(`test_a45_batch14_26_decisions.py`)——
> 在本批再抄一份守卫等于给同一条规矩造第二个判定点。
>
> ### D3:两条既有守卫第一次拦下的是**新代码**,不是回退
>
> 接线第一版有两处写法被当场判红,两处都是"看起来最自然"的:
> 从 `spu` 字符串码反查 SPU 行(§3.39),以及自己拼一遍
> `evidence_assets_for + views + fingerprints`(§5.1 的唯一取数入口)。
> 守卫分别是 14-26 与 14-21 留下的。
>
> ### 跑过的与没跑过的 —— 这一栏本批变了很多
>
> **这台机器装得上 PostgreSQL 16,所以真库那一整类第一次真的执行:**
>
> ```
> 全量 pytest(真库)   2891 passed, 0 failed, 0 skipped
> 纯逻辑              2582/2582   0 失败,0 跳过(装了三方依赖,68 条 SKIP 消失)
> 迁移升降级          0049 → 0048 → 0049 空库演练通过,单 head
> 本批变异              15/15    第一轮 11/15
> batch19 变异          13/13    判定层改动后复验
> 锚点              509/509     27 份脚本    源码守卫 568    列写入 540(27 条台账)
> 交付               17/17      样例 10/10   imports 439    架构契约 3/3
> Ruff 全绿
> 前端 typecheck / Vitest 74/74 / build / syntax 89/89 全绿,lint 0 错 / 4 条既有 warning
> ```
>
> **仍未执行**:Docker build、Playwright、真实平台环境。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过,阶段 P0 仍未关闭。**
> **真库 ≠ 真环境** —— 本批把「写好了没实测」关掉了一大半,没关掉验收那一半。
> AC-18/19 现在有部分证据(见结论文件第四节),仍不能标完整通过。

> ## 阶段 5 / 5-1 评审复核结论(2026/08/08)
>
> 详细结论与后续排期见 `docs/REVIEW-STAGE5-5-1-CONCLUSION.md`，决策见
> `DECISIONS.md` §3.51。**5-1 可以收，阶段 5 不能收，`DELIVERY_STAGE` 仍为 4。**
>
> 评审后补齐：事实差异返回字段键而非 UUID；区分 PLANNED 转正与新建 ACTIVE；
> READY 通过 `ready_problems()` 合取图片集规则和 SKU 映射；补 8 个前端标签并做
> 前后端集合契约；写死 SKU 采集口径；将两列 schema version 拆开。AC-10/11/18/19
> 已在 PRD §14.1 写成明确标注来源的仓内执行版，不能冒充已找回的 v3.0 原文。
>
> 本轮把下方 batch19 初始记录中的环境敞口实际补跑完：守卫 **28/28**、变异
> **13/13**、全量纯逻辑 **2581/2581**、全量后端 pytest（真实 PostgreSQL +
> Redis）退出码 0、迁移 **6/6**，并完成 `0049 → 0048 → 0049` 真库演练；前端
> typecheck / lint / Vitest **74/74** / build 全部通过；Ruff 正式范围通过、依赖
> 架构 **3/3**、交付 **17/17**、样例 **10/10**。因此下方“25 条”“2560 条”
> 和“真库/前端/ruff/lint-imports 未执行”只代表 5-1 首轮落码时点，不再代表现状。
>
> 后续切法增加 5-2A（`owner_id` UUID / `variant_key` / 真库身份回填）、5-2B
> （草稿接线与存量重生成演练）和 5-C（AC 与欠账收口）。14 条阶段 5 列欠账、
> 3 条欠账守卫以及身份级动作已逐项认领，不能等 5-5 后再“顺手收尾”。

> ## A45-batch19:阶段 5 开工,批次 5-1 —— 草稿的颜色维上游快照(基线 batch18,2026/08/08)
>
> 逐条说明见本节,决策见 `DECISIONS.md` §3.50。**阶段 4 的交付项到 batch18 全部落码,
> 本批是阶段 5 的第一个批次。**
>
> ### 阶段 5 的五项交付怎么切
>
> PRD §13 阶段 5 交付:颜色结构化字段注册表项与确认流;来源追溯;
> `color_sku_image_map` 与 `upstream_versions` 快照;READY 门禁扩展;导出预览。
> **不是** `REVIEW.md` 的 P5 那一格(那里只有任务 24 Playwright)。
>
> ```
> 5-1  草稿上游快照(两列 + 迁移 + 纯判定层)          ← 本批
> 5-2  接线:build_draft 写快照、refresh_draft 读它、READY 门禁按颜色轴推广
> 5-3  文案幂等键(收口 S/M/L 重复付费)+ 入口改 SPU 粒度
> 5-4  颜色结构化字段注册表项 + 确认流 + 来源追溯
> 5-5  导出预览
> ```
>
> 5-1 排第一有两条理由:5-2 与 5-5 都要读它;而它是**这台机器上唯一能真验的
> 一层** —— 没有网络、没装后端依赖,pytest / ruff / lint-imports 一条都跑不了。
> 5-4 排最后是因为动属性注册表会波及识别 Schema 与校准,盲改风险最高。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `app/workbench/upstream_snapshot.py`(新,零依赖纯函数) | ✅ 落码并跑过 |
> | `ListingDraft.upstream_versions` / `.color_sku_image_map` 两列 | ✅ 落码,**SQL 未在真库跑过** |
> | 迁移 `0049_draft_upstream_snapshot`(只加列,不回填) | ✅ 落码,**未升降级演练** |
> | 两列进 `audit_column_writers.LEDGER`,还款日:阶段 5 | ✅ 门禁跑过 |
> | 守卫 25 条 + 变异 13/13 | ✅ 第一轮 22/25,见下 |
>
> ### D1:`DELIVERY_STAGE` 仍是 4 —— 不是忘了改
>
> 标记的含义是「本仓当前落码到 PRD §13 的第几阶段」,而把它推到 5 会让三条
> 还款日写着「阶段 5」的欠账守卫**当场变红**:
>
> ```
> test_a45_batch14_13_scope_outcome.py    按作用域的失败归集缺一列
> test_a45_batch14_23_..._dedupe_ledger   兜底去重键要等 spu_id NOT NULL
> test_a45_batch14_9_scope_fingerprint.py §8.1 还欠四个变更源
> ```
>
> 本批一条都不还(第三条的四行里,「新增/停用 ACTIVE 颜色」的 DRAFT 那一格
> 由本批的判定层补上了,但它的 COPY 那一格要等 5-4 的颜色结构化字段 ——
> **补一行要四格都有着落**,这条口径是 14-20 定的,本批不推翻)。
>
> `audit_column_writers.LEDGER` 里另有 12 条写着「还款日:阶段 5」；加上本批
> 新记的两列，共 14 条，同样没还。
> 标记推到 5 的前提是那批账清完,**不是本批**。
>
> ### D2:两列可空、不给 server_default —— 与同表其它 JSONB 列刻意不同
>
> `listing_drafts` 上的 `canonical_snapshot` / `manual_payload` 一律
> `nullable=False, server_default='{}'`。照抄在这里是错的:那几列从第一版起
> 就有写入点,`{}` 的含义是「这份草稿确实没有手填字段」。
>
> 这两列不是。0049 之前建的每一行草稿都**一次都没算过颜色维**,而 `{}` 的
> 含义是「算过,颜色维是空的」—— READY 门禁读到它会在颜色轴上**静默放行**,
> 表现是一份缺了整个颜色的草稿显示「上游全部有效」,不报错。
>
> 留 NULL,判定层把 None 判成不可证明、不放行。与 0042 / 0045 / 0048 是同一条
> 规矩的第四次应用:**默认值不许让任何人被静默放行或静默拦下。**
>
> ### D3:§4.10 点名的三个轴落进列里,但**不在这一层比较**
>
> §4.10 说 `upstream_versions_json` 含 Copy 版本。而 copy / spec / mapping
> 三个轴已经在 `canonical_snapshot["components"]` 里,由 `stale.diff_components`
> 负责解释。做法是:值从 `components` **抄**过来(单一写入点、两个读取方),
> 比较仍然只由 `diff_components` 做一次。
>
> 两边都比的话,改一次文案版本会出两条过期提示,运营看到「这份草稿有 2 个
> 上游变了」—— **「变了几处」这个数字从此不可信,而且不会有任何东西报错。**
> 正反两条守卫钉着:值真的落进去了,以及改了它一条变化都不出。
>
> ### D4:范围口径的**反向那一半**才是本批最要紧的守卫
>
> §6.7:只检查 `sellable_status=ACTIVE` 的颜色与其 SKU。正向漏掉时,有人会在
> 导出文件里看见错的行;**反向漏掉时**,一个还没到样的 PLANNED 颜色会让整个
> SPU 的草稿永远处在过期态 —— 运营重新生成一次它又过期,而提示里说的是一个
> 他压根还没开始做的颜色。第二种没有任何东西会报错,只会让人不再相信过期提示。
>
> 所以快照分两格:`colors` 只放 ACTIVE 且带全部版本引用,`inactive` 只记
> `{颜色 id: 可售状态}` 且**一个版本都不记**,只参与措辞不参与判定 ——
> 「被停用了」和「被删了」对运营是两句不同的话。
>
> ### 变异第一轮 22/25,三条都是守卫自己的毛病
>
> ```
> 夹具用 `code or variant_id.upper()`   空串被默认值吃掉,回落分支从没走到
> `ast.unparse` 把引号统一成单引号      按源码原文比,假红
> caller 守卫按文本扫源码               模型的列注释里写着模块路径,守卫说「已接线」
> ```
>
> 第三条是同一个坑在本仓的**第五次**(§3.26 / 14-13 / 14-22 / batch18 的 C2):
> 解释性文字喂了断言。修法沿用第四次那一版 —— 不是把锚点钉得更死,是
> **从输入里把注释去掉**,判据换成 `ast` 的 import 节点。
>
> ### 跑过的与没跑过的
>
> ```
> 纯逻辑    2560/2560   0 失败,8 跳过(与基线同名单)
> 本批变异     13/13    第一轮 12/13(B1 的变异写坏了,不是守卫的问题)
> 锚点       494/494    26 份脚本
> 交付        17/17     列写入点 540(台账 31 条)
> ```
>
> **仍未执行**:前端四条、真库 pytest、**Alembic 升降级(本批有迁移,head 由
> `0048` 变为 `0049`,一次都没在真库跑过)**、Docker build、Playwright、
> ruff、lint-imports。
>
> 迁移与 ORM 的不一致由 `tests/test_migrations.py::test_upgrade_matches_orm_metadata`
> 兜着,**而那条要真库**。本批把它能兜的那一半(列名)之外的一半(可空性)
> 用纯层守卫补了 —— 那条测试只比列名,比不了 nullable。
>
> **本批没有接线,判定层在 `app/` 下零调用。** 这一条不是遗漏,是 5-1 的边界,
> 由两条欠账守卫记着(`..._have_no_writer_yet` / `..._has_no_production_caller_yet`),
> 还款日阶段 5。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**
> 阶段 5 五项里落码的是第三项的一半,AC-10/11/18/19 一条没验 —— 别合并计算。

> ## A45-batch18:阶段 4 收口 —— 方案面板终于有了宿主页(基线 17-2,2026/08/08)
>
> 逐条说明见 `docs/MERGE-A45-BATCH18-STAGE4-PLAN-HOST.md`,决策见 `DECISIONS.md` §3.48。
>
> ### 还的是一笔躲了四批的账,而它躲过去的方式是「理由写错了」
>
> `GenerationPlanPanel.tsx` 与 `api/generationPlans.ts` 从 14-20 起就写完了,
> 之后四批全树零 import。三种理由里只有最后一种成立:
>
> ```
> 14-20    「跑不了 tsc / Vitest」          真的,但那是环境,不是这一笔
> 14-23    「等有人写一行 import」          **错的** —— 无处可写
> 14-23 后 「缺一个知道 SPU 主键的宿主页」  对的,本批落的就是它
> ```
>
> 面板要 `spuId`(UUID),而前端此前没有任何一条路径拿得到 SPU 主键。
> 照着第二条去做的人会发现无处可写,然后多半把 `spu` 字符串码传进去 ——
> 接口 422,而错因指向后端。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `SkuRow.spu_id` + `SpuGroup.spu_id`(冲突/缺失给 None,**不猜**) | ✅ 落码并跑过 |
> | `serialize()` 并排给出主键与字符串码 | ✅ |
> | `list_spus` 传真实的 `products.spu_id` | ✅ 落码,**SQL 未在真库跑过** |
> | `pages/SpuDetailPage.tsx`(新)+ `/spus/:spuId` 路由 | ✅ 落码,**未编译、没点开过** |
> | 两条入口:建档完成页 / 聚合页(有主键才给链接) | ✅ 落码 |
> | 顺带:聚合页空态按失败/真空集分岔,空态台账 3 → 2 条 | ✅ 反验过会红 |
> | 守卫 12 条 + 变异 14/14 | ✅ 第一轮 13/14,见下 |
> | 「面板进不去任何路由」那条欠账守卫 | ✅ **退役**(按它自己的文档字符串) |
>
> ### D1:主键推不出来时给 None,不挑一个
>
> 两行商品共用一个字符串码却指向不同主键时,挑一个的代价不是"显示错一个 id":
> 运营点进去看到的是**另一个款**,方案面板会照着那个主键列方案、启用、
> 把别人的图片集判过期 —— 每一步都不报错。与 `FieldConsistency.value` 同口径。
> **半迁移的一组仍然给主键**(非空的那些一致时),否则迁移中间态里这一页会说
> "这个 SPU 配不了方案"而不解释为什么。变异 A2 是反方向。
>
> ### 变异第一轮 13/14,漏的那条是同型第四次
>
> C2(把宿主页那行 import 注释掉)第一轮 GREEN,两层:注释里那几个字喂真了断言
> (batch13-3 M11 / batch14-2 N15 / 14-22 L5 之后第四次);剥掉注释之后
> **仍然绿** —— JSX 里的 `<GenerationPlanPanel .../>` 接着喂真,而那一刻组件
> 根本不存在。断言最终钉在 **import 语句**上。修法这次不是"锚得更死",
> 是**从输入里把注释去掉**:解释性文字本来就不该参与判定。
>
> ### 按 PRD §13 重新清点
>
> | 阶段 | 交付项 | 完整落地 | 部分落地 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | 5 | 0 | 1(三步建档 UI 已落,余 owner_id 相关收尾) |
> | 阶段 2 素材归属 | 6 | 5 | 0 | 1(按颜色上传 UI) |
> | 阶段 3 真实识别 | 6 | 4 | 2(异步化 + cancel;按颜色重试) | 0 |
> | 阶段 4 多颜色图片 | 5 | **5** | **0** | 0 |
> | 阶段 5 / 阶段 6 | 11 | 0 | 0 | 11 |
> | **合计** | **34** | **19** | **2** | **13** |
>
> 阶段 4 那一行 4 → 5:唯一那项部分落地补齐。**阶段 4 的交付项到此全部落码。**
>
> ### 门禁
>
> ```
> 纯逻辑    2510/2510   0 失败,7 跳过(与基线同名单)
> 本批变异     14/14    第一轮 13/14
> 锚点       481/481    25 份脚本
> 守卫窗口      554 个
> 交付        16/16     样例 10/10   导入 430   列写入点 537
> 前端语法     89/89    **只解析不判型,不是 typecheck**
> ```
>
> **仍未执行**:前端四条(tsc / ESLint / Vitest / build)、真库 pytest、
> Alembic 升降级(本批**没有迁移**,head 仍 `0047`)、Docker build、Playwright。
>
> **本批最大的一处缺口是宿主页没有组件用例。** 补它的第一件事是给测试
> 包装器加 QueryClient —— §3.46 第 3 条延期的那一半,现在宿主页真的存在了。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**
> 阶段 4 五项**落码**齐了,AC-08/09/12/13 仍一条没验 —— 别合并计算。

> ## A45-batch17-2:1442 包审阅意见的修复(基线 1505,2026/08/08)
>
> ### P2-1:发布投递的落库端没有归属校验
>
> `publish_service._save()` 取三行、**无条件**写三行。租约过期被重领之后,
> 迟到返回的那次调用会把较新的结论盖回去:
>
> ```
> DONE → DEAD        SUCCEEDED → UNKNOWN        LISTED → SUBMIT_RESULT_UNKNOWN
> ```
>
> 商品在平台上真的上架了(`external_spu_id` 还留着,幂等三道防线保证不会
> 多出一件),而界面会说"结果未知、需人工"—— 人工唯一能做的 RECONCILE
> 是一次本来不必发生的付费调用。批次链路的同型缺口 A43 / BLOCK-02 已修,
> 发布这条当时没跟着改,`STATUS.md` 的已知限制里也没有对应的一行。
>
> 修法:迁移 0047 给 `publish_outbox` 加 `lease_token`,领取时生成、
> 续租与落库都认它、落终态与人工重投时吊销。失去执行权时结果只进审计
> (`_record_stale_outcome`),`run_due` 新增 `stale` 分桶。
>
> **合入复审又拦下一处安全默认值写反。** 来包声称空令牌比较会按
> `lease_token = NULL` 判假,但 SQLAlchemy 实际生成 `IS NULL`,会命中存量无主行。
> 现在续租与落库两处都在发 SQL 前显式拒绝空令牌,守卫与 M10/M11 分别钉住;
> 取舍见 `DECISIONS.md` §3.47 第八节。
>
> **评审给的"退一步最小修法"(`lease_until == 领取值`)在本仓是错的**,
> 而且错的方向更贵:`_renew_lease()` 在发请求之前就把库里那个值改掉了,
> 按它写会让**每一次**投递的结论都落不了库。详见 `DECISIONS.md` §3.47 第二节。
>
> ### 顺带修掉的两处(报告没点到)
>
> | | |
> |---|---|
> | `reconcile_unknown()` 从不领租约 | 加了 WHERE 之后它的结论会被判成 stale —— "点了确认、界面纹丝不动"。本批让它领令牌,但**不动 status**(改成 LEASED 会让 DEAD 悄悄变回自动重试,见 §3.47 第三节) |
> | `model_license` 只归一了比较的一侧 | `license_expires_at` 是 `timestamptz`,换个 session 查出来是 aware 的,和 naive 的 `now` 比会 `TypeError`。落在 §11.3 硬阻断上,表现是「点生成 → 500」 |
>
> ### P3-1 ~ P3-4:四条文档失实,修法一律是"引述并驳斥"
>
> | | 原话 | 事实 |
> |---|---|---|
> | `CLAUDE.md:233` | 未做:任务 7、9 | 两者 A45-batch14 / 14-18 就落码了(从未连真端点 ≠ 没做) |
> | `frontend/CLAUDE.md:71` | 任务 5、6 尚未实现 | a37 落地 + A42 修正上报口径 |
> | `core/clock.py` 台账 | 14 处;"唯一一处被禁形式已由 A34 收掉" | 实数 16;`model_license.py:53` 原样长着那个形式 |
> | `run_state.py` / `attributes/service.py` | `color_variant_id`「今天还不存在」 | 迁移 0037 起是真列,失败作用域会真实产出 |
>
> 第三条是最贵的一种(§3.42:说缺口已关比说过期贵),而且它长在**教别人
> 怎么数**的那一段旁边。本批把两处漏账的收掉,并加一条守卫每次现数一遍
> 与台账比对 —— 数字从"被记着"换成"被守着"。
>
> ### 本批的门禁
>
> ```
> 当前工作树纯测试 2506/2506         本批变异 11/11 验红
> verify_delivery 16/16              audit_anchors 467/467(24 份脚本)
> audit_source_guards 547            audit_column_writers 537 列
> verify_imports 428 文件            样例数据 10/10
> Ruff 全绿                          import-linter 3/3
> 前端 typecheck / Vitest 74/74 / build / syntax 88/88 全绿
> 前端 lint 0 error / 4 个既有 warning
> ```
>
> **没跑的**:`tests/test_publish_lease_concurrency_db.py` 7 条双 session 用例
> (本机无 `TEST_DATABASE_URL`,pytest 明确显示 7 skip)、迁移 0047 的真实升降级。
> 静态迁移链已确认单 head `0047`。见「二、证据缺口」。

> ## A45-batch14-28:阶段 1 身份收口 + 三条「算好了没人读」的最后一跳(基线 14-27,2026/08/08)
>
> ### 本批最先做的事是**装依赖**,而它当场证伪了一条卡点
>
> 上一版清点把三条前端缺口归给「环境:无 node_modules **且无网络**」。
> 本机 `npm ping` 通 —— 依赖装上,前端门禁第一次真正执行:
>
> ```
> typecheck  3 条错误      test  74 条里 1 条失败      lint  0 错 / 6 警告
> ```
>
> **`前端语法 86/86` 一直给的是假绿**:`tools/syntax-check.mjs` 只解析不判型。
> 四条存量缺陷因此躲过了每一次清点:
>
> | | 形状 |
> |---|---|
> | `useWriteError()` 少一个必传实参 | 结果未知时 `onUnknown()` 抛 `TypeError` —— **错误处理器自己炸掉**,而那一刻正是最需要它说话的时候。方案面板没有宿主页,从未被挂载 |
> | 两处分页把裸 `number` 喂进白名单 codec | `write` 原样写进 URL、`read` 不认退回默认 —— 下拉框显示 50、列表是 20 条 |
> | 「后退保留」用例用 `MemoryRouter` 却调 `window.history.back()` | 两个后退栈无连接,断言恒真。全仓唯一守 §8.2 的用例,**两个方向都绿** |
>
> 变异验证:`patch` 改 `{ replace: true }` —— 修前 19/19 绿,修后 1 failed。
>
> ### 三条「算好了没人读」,同一个形状的三次重演
>
> 上游算出正确结论、接线也在,**最后一跳没有人接**,于是两侧测试全绿:
>
> | | 断在哪 |
> |---|---|
> | `variant_gate_roles` | 14-15 就递到 `MaterialFacts` 上,`_evaluate_material` **一次都没读过** —— 颜色完整度看起来是一道生效的门禁,实际不产出任何 issue |
> | `facts_stale` | 14-21 起逐行在回,`frontend/src/` 零引用 |
> | `color_variant_id` | 上传接口收得下,前端表单不发 —— 界面传出来的归属恒为 null,**AC-21 在界面上没有对象可验** |
>
> ### D1:颜色维的来源(决定)
>
> `variant_gate_roles` 的键原来从素材反推。那条理由对**指纹**成立(没有素材的
> 颜色算不出指纹),对**门禁**是反的:一张图都没传的颜色在素材里留不下痕迹,
> 于是永远不进表、永远不被判缺图 —— **门禁最该拦的那一种,恰好是它唯一
> 看不见的那一种。**
>
> | | |
> |---|---|
> | 备选 | 继续从素材反推(零改动) |
> | 选错的后果 | 只传了 A 色图的三色商品一路走到刊登,B 色图片集拿不到样品,而没有任何一步报错 |
> | 守卫 | `test_the_colour_dimension_comes_from_the_declared_list_not_the_assets` |
>
> 取**并集**而不是只取声明清单:挂在已下架颜色上的存量图不该凭空消失。
>
> ### 阶段 1 身份收口:`owner_id` 切 UUID + 命名空间 hack 与 `variant_key` 退役
>
> 上一版把它挂在「执行卡真库」。本批拿到显式授权,而**做法不是赌顺序装对了**
> —— 是先拆掉那个"静默"。
>
> **① 静默的确切位置找到了。** `apply_evidence` 里一句
> `except AttributeValueError: logger.warning + continue`。它对「值不合契约」
> 是对的(不该炸掉另外 7 个正常字段),但同一个 `except` 也接住了
> 「owner 坐标算不出来」—— 而那一档意味着**整个 VARIANT 层静默消失**,
> 界面显示「识别完成」加一个空主色。§3.22 警告的"识别侧静默丢颜色字段",
> 那个"静默"就是这一行。拆出 `AttributeOwnerError`,不被吞。
>
> **② hack 不是被删掉,是被它的存在理由抛弃了。** `<len>:<spu>/<key>` 的前提
> 是变体 id 取值为颜色名;换成 UUID 后跨 SPU 撞不上。这个区别重要:前者
> 随时有人加回来,后者要先把 UUID 换回颜色名。函数体留成 `raise`。
>
> **③ 迁移 0046 四件事同事务,回填不上就 `raise`**(姿态同 0044)。
> 只拦名下真的挂着 VARIANT 属性或变体图片标签的商品。`variant_key` 列
> **不 drop** —— 它是唯一的回头路,drop 掉降级就是空话(台账已记)。
>
> **④ 切 UUID 下面最深的一处连带伤,被守卫咬出来了。**
> `fingerprint_scope()` 靠解析命名空间取变体段;切成裸 UUID 后
> `split_variant_owner_id` 对每行返回 None,于是**每条颜色事实落回共享作用域**
> —— 给 A 色补图 stale 掉 B 色事实,**D1 从后门原样回来**,而 AC-21 正为它写。
> 它不报错:返回值合法、类型正确、`facts_stale` 照常算。变异 R8b 钉住它。
>
> ### 七条守卫钉着已退役的形状,全部**改指新不变式**而不是删
>
> 顺带补上一个一直存在的弱点:那几条 round-trip 用例拿**铸造函数的返回值**
> 当期望值 —— 铸造和解析一起漂的话它照样绿,而库里真正躺着的字节谁都读不了。
> 铸造退役之后一律改钉**字面量**(真实字节)。
>
> ### 2026-08-08 补丁审核与本机真库复验
>
> `patch/swimwear-imagegen-batch14-28.patch` 合入前做了逐文件审查。原补丁有四处
> 不能直接合入的风险,已在当前树修正并补回归:
>
> 1. 0046 原先按裸 `variant_id` 全表改图片标签,两个 SPU 都有 `BLK` 时会串档；
>    现按 `listing_image_sets.spu` 收窄 upgrade 与 downgrade。
> 2. 同一 SPU 的 `working_name` 可能重复；现候选超过一个就停止迁移,不让
>    PostgreSQL 在 `UPDATE ... FROM` 中任选一个。
> 3. 共享事实指纹与素材完整度原先只查当前 SKU；真库证明这会把同 SPU 另一颜色
>    误报过期/缺图,现都按 `spu_id` 查询整个 SPU。
> 4. 显式空颜色重试范围 `[]` 原先等同于“不限范围”,会扩大成全量调用；现保持为空
>    并在第一次 Provider 调用前拒绝。
>
> 当前验证结果（带日期的快照,不是永久总数）:
>
> ```text
> 后端纯逻辑       2492/2492
> PostgreSQL 0046  6/6（含 upgrade / downgrade / 同名跨 SPU / 歧义拒绝）
> 相关真库回归     60/60（SPU、颜色归属、AC-21、产品接口）
> Ruff              app + tests 全通过
> 前端              typecheck 通过；Vitest 74/74；build 通过；lint 0 错/4 警告
> 交付/结构门禁     delivery 16/16；样例 10/10；imports 424 文件；
>                   架构契约 3/3；前端语法 88/88；锚点 455/455；
>                   源码守卫 539；活文档引用全通过
> ```
>
> `.env.example` 打包缺失已修复：打包先排除 `.env.*`,再只补回根目录与前端的
> 两个精确模板路径；复验也只豁免这两个路径。审核时还发现仓库内 `.venv` 会被
> 当作普通目录打进交付包,现 `.gitignore`、打包排除与交付扫描三处一起收口。
> Windows 入口现为 `tools/pack.ps1 [-Version aNN]`,不依赖 `zip` / `unzip`,使用
> .NET ZIP API 执行同样的“排除 → 重新打开成包复验 → 失败删包”。本机实跑生成
> 690 文件、3.8 MiB 的验证包；`verify_delivery` 会逐项比较 Bash 与 PowerShell
> 的五组清单,防止两套入口后续分叉。
>
> 未在本次范围内执行：真实模型调用、Docker build、Playwright E2E、完整 Redis/Celery
> 集成套件。前端 lint 的 4 条 warning 是既有 Hook 依赖提示,本次无新增 error。
>
> ## 2026-08-08 · A45-batch17-1 补丁审核
>
> 来包是基于 A45-batch17 的增量，而当前树没有该前置批次；直接应用会在
> `GenerationPlansPage`、宿主页组件用例、`mutate_batch17.py` 等处失败，并让
> 来包的 `DECISIONS.md` §3.44 与当前已有 §3.44 撞号。因此本次按语义移植，
> 没有把整包“应用成功”写进状态。
>
> 已移植：分页白名单由 codec 自带 `narrow()` 统一收窄；失败空态新增棘轮台账。
> 已延期：依赖方案宿主页的空态修复与 QueryClient 包装器修复。宿主页当前仍未
> 落地，阶段 4 的该项状态不变。审核依据与边界见 `DECISIONS.md` §3.46。
>
> 复验快照：纯测试 2495/2495；URL 变异 14/14 RED；前端 typecheck、Vitest
> 74/74、build 通过，lint 0 错/4 条既有 warning；delivery 16/16、样例 10/10、
> imports 424、锚点 456/456、源码守卫 542、文档引用、Ruff、架构契约 3/3、
> 前端语法 88/88 均通过。`make check-offline` 的 Windows 入口因 `python3.exe`
> 为 Microsoft Store 占位符未启动，以上为按 Makefile 字面顺序的等价逐项执行。
>
> ## A45-batch14-26:三笔决定类欠账 + 老建档路径切 SPU(基线 14-25,2026/08/07)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-26-DECISIONS.md`,方法论见 `DECISIONS.md` §3.41。
>
> ### 这一批越过了 14-23 划的那条线,而且是有意的
>
> 14-23 立的规矩:分界线是**缺代码**还是**缺一个决定**,后者写得出来也不该
> 由写的人拍板。本批拿到显式授权,补上它的另一半:
>
> **决定可以由写的人做,但它必须以决定的身份出现,不能以实现的身份混进去。**
>
> 四个决定各自单列(备选方案 / 选错时会怎样 / 一条会在它被悄悄撤回时变红的守卫)。
>
> | | 决定 | 落点 |
> |---|---|---|
> | D1 | 素材挂 SPU 用**真外键**,`spu` 字符串降为反规范化读列 | 迁移 0043 |
> | D2 | §4.8 去重键落成**两条互斥局部唯一索引** | 迁移 0044 |
> | D3 | `evidence_class` 存储列 + CHECK,**回填走脚本不走迁移** | 迁移 0045 + 回填脚本 |
> | D4 | `create_product` 必须**解析**出 `spu_id`,查不到就拒绝 | `product_service` |
>
> ### D2 是本批的重心:按字面落有一个洞
>
> §4.8 逐字要求 `UNIQUE(spu_id, COALESCE(color_variant_id,''), sha256)`。
> 而 `spu_id` 可空,唯一索引里 **`NULL <> NULL`** —— 一条式落法会让所有
> 没有 SPU 的素材**彻底失去去重**,比今天更糟且静默:约束在,只是不拦任何东西。
>
> 落两条互斥且合起来覆盖全表的局部索引。**守卫钉的是两个谓词互补这个前提,
> 不是"有两条索引"** —— 退化回去最省事的路径是把某一条谓词改窄,那之后
> 三样东西都还在,而中间裂开一批两条都不管的行。变异 K3 / K4 是两个方向。
>
> ### D4 有一个真实的行为变更
>
> **历史边界:**D4 当时只关了 `create_product`；CSV 导入路径尚未同步，所以旧版
> 把两条入口一起写成已关是错误的。**当前边界已由 §3.65 收口:**
> `import_products()` 现在同样要求已存在 SPU，单颜色可补唯一颜色，多颜色必须给
> `variant_code`；入库统一回填 `spu_id` / `color_variant_id` / SPU 权威受众与品类，
> 找不到身份的行进入 `errors`，不自动造最简 SPU。旧的反向注释已清除，
> `test_a45_batch16_doc_truth.py` 同时守正向契约和"不得残留反向断言"。
>
> ### 三处被工具当场证伪的判断
>
> | | 形状 |
> |---|---|
> | "守卫太严" | 单写入点守卫拦下回填脚本,第一反应是它误报。**它没有。** 最后改的是代码,不是守卫 |
> | 变异 P1 GREEN | 守卫被**同一函数里另一处对的代码**喂真(`if not spu_code` 那条也含 `ValidationError`)。与 14-22 L5 同型,改成按 AST 算 |
> | 锚点断两条 | 插一段代码就让 14-11 W3(靠相邻性)与 14-16 W5(两行 kwargs)一个过期一个不唯一。W3 改代码、W5 改锚点,分界线见 §3.41 第五节 |
>
> ### 14-23 的 D 组退役
>
> 它验的是"欠账守卫会红",而欠账已还清、守卫已翻正向。按 14-24 的**自净**
> 规矩删掉而不是注释掉 —— 留着会在下次清点里以"已有变异覆盖"的身份被数进去。
> 继任者是本批 K 组。
>
> ### 按 PRD §13 重新清点
>
> | 阶段 | 交付项 | 完整落地 | 部分落地 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | **5** | 0 | 1(owner_id 切 UUID + `variant_key` 退役;三步建档 UI) |
> | 阶段 2 素材归属 | 6 | **5** | 0 | 1(按颜色上传 UI) |
> | 阶段 3 真实识别 | 6 | 4 | 2(异步化 + cancel;按颜色重试) | 0 |
> | 阶段 4 多颜色图片 | 5 | 4 | 1(方案面板无宿主页) | 0 |
> | 阶段 5 / 阶段 6 | 11 | 0 | 0 | 11 |
> | **合计** | **34** | **18** | **3** | **13** |
>
> 阶段 1 那一行 4 → 5:受众那条 NULL 兼容缝关掉(D4)。剩下的两项合并成
> 一项计数 —— 它们**是同一个动作**(见文末)。
> 阶段 2 那一行 3 → 5:`evidence_class` 存储列 + CHECK(D3)、§4.8 新去重键(D2)
> 两项各自补齐后半句。
>
> ### 离线门禁
>
> ```
> 纯逻辑      2459/2459   0 失败,7 跳过(缺 pydantic / sqlalchemy)
> 本批变异      13/13     第一轮 12/13,P1 暴露守卫太松,修完全红
> 锚点         454/454    23 份脚本(14-23 D 组退役 -2,本批 +13)
> 守卫窗口       521 个
> 交付         16/16
> 列写入点      536 列    `spu_id` / `evidence_class` 都有写入点,不进台账
> 样例数据      10/10
> 前端语法      86/86
> 导入         419 个文件
> ```
>
> **三条迁移(0043 / 0044 / 0045)一次都没执行过。** 这是本批最大的一处不确定。
> 0044 建唯一索引在存量库上**可能失败**(已有"同 SPU 跨颜色的同一张图"时),
> 而那是对的 —— 那批行正是 §11 场景 1 要求人工确认的对象,迁移里刻意没有
> `ON CONFLICT`。
>
> **仍未执行**:前端四条、`alembic upgrade/downgrade`、真库 pytest(38 条,
> 本批 +0)、Ruff / lint-imports、Docker build、Playwright。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**
>
> ### 本批**没有**做的三类
>
> | 没做 | 类别 | 理由 |
> |---|---|---|
> | 三步建档 UI / 按颜色上传 UI / 方案面板宿主页 | 环境 | 无 node_modules **且无网络**(egress `host_not_allowed`,装不了依赖)。仓库自己的规矩:给这个仓库加不可验证的前端等于扩大 D 类缺口 |
> | 识别 run 异步化 + cancel / 按颜色重试 | 环境 | 卡 Redis + worker |
> | `owner_id` 切 UUID + `variant_key` 退役 | 决定已定、执行卡真库 | 四步里第一步「`products.color_variant_id` 行有值」要真库回填,恰是这台机器唯一验不了的那步,而**单独切第一步会让识别侧静默丢颜色字段**。D4 已把老路径切到写 `spu_id`,替这笔账推进一格。剩余三步 + 删兜底去重键是同一个动作,挂阶段 5 |

> ## A45-batch14-24:「每一列都答得出谁写它」门禁(基线 14-23,2026/08/07)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-24-COLUMN-WRITER-AUDIT.md`。
> **本批不改业务代码**,交付的是一份审计 + 它自己的守卫。
>
> ### 为什么做这个
>
> §3.38 那条规矩(一列落库了、被判定读取,就必须回答「谁写它」)是对的,
> 但它是一句话。这个仓库为同一个类付过两次代价:`color_variant_id` 躲了
> 五批、§4.8 去重键躲了六批,**两次都靠人逐条对着 PRD 核才核出来**。
> 第三次不会有人这么核。
>
> ### 首跑找到第三例
>
> | 列 | 现状 |
> |---|---|
> | `PublishAttempt.provider_request_id` | 列注释写着「出事时这是唯一能和平台对账的东西」,接口在返、前端有显示位,**全仓零写入点** |
> | `ColorVariant.display_name` | 后端 5 处读、前端 7 处显示,恒为默认值。列注释承诺的写入点要等 `owner_id` 切 UUID —— **注释里的承诺不是守卫** |
> | `ProductAttributeValue.schema_version` | 恒为 `'1'`;它存在的意义是改版时能分辨新旧,而恒定的版本号让那次改版无法分辨 |
> | `GenerationCandidate.height` | 恒为 NULL,而 `width` 有写入点 —— 两列本该成对 |
> | `ModelTemplate.disabled_reason` | 前端有显示位,恒为 NULL:运营看到模板不可用,看不到为什么 |
>
> 共 23 列,全部进 `LEDGER` 台账(29 条,含此前已知的),分三类:
> 整套特性未接线 11 条、运维灌数据 6 条、**真欠账 12 条(全部还款日:阶段 5)**。
>
> ### 台账不是白名单,区别只有一条:自净
>
> 某一列一旦有了写入点,它的条目就失效并被点名,必须删掉。少了这一条,
> 条目会在还清之后继续留着 —— 那时它不再是"已知欠账",是"我们不看这一列",
> 而两者在文件里长得一模一样。
>
> ### 审计自己的失效方式是"安静地少查"
>
> 第一版当场演示过:全仓四处动态 `setattr(obj, name, ...)`,第一版判成
> "整个仓库都判不了",40 个模型全部出局,输出「**0 列都答得出**」——
> **而退出码是 0**。那种绿和"全都查过了"在报告上长得一模一样。
>
> 所以守卫钉五件事:台账条目有原因、真欠账有还款日、**台账自净真的会报**、
> 覆盖面不许塌到地板以下、审计挂在 `verify_delivery.CHECKS` 与 CI 上。
>
> 变异验证暴露的两条同型问题(见 §3.40 第五节):守卫**读源码**断言判定
> 存在,而判定被搬走后源码断言安静地继续为真;守卫断言"文件里出现过脚本名",
> 而从 `CHECKS` 摘掉之后名字仍在函数体里。两条都改成**算**而不是**读**。
>
> ### 离线门禁
>
> ```
> 纯逻辑      2450/2450   0 失败,7 跳过(缺 pydantic / sqlalchemy)
> 本批变异       7/7      一次全红
> 锚点        443/443     22 份脚本
> 守卫窗口       516 个
> 交付         16/16      新增「每一列都答得出谁写它」
> 列写入点     534 列     29 条在台账上,1 个模型判不了(Product,**kwargs 构造)
> 样例数据     10/10
> 前端语法      86/86
> ```
>
> **阶段清点与 14-23 相同** —— 本批没有落任何 PRD 交付项,它落的是一条
> 让下一笔同类欠账当场可见的门禁。
>
> **仍未执行**:前端四条、`alembic upgrade/downgrade`、真库 pytest(38 条)、
> Ruff / lint-imports、Docker build、Playwright。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**

> ## A45-batch14-23:§6.5 两列的写入路径 + §4.8 去重键拆账(基线 14-22,2026/08/07)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-23-ITEM-COLUMNS-AND-DEDUPE.md`。
>
> ### 这一批只做"只缺有人写"的那一类,不替任何人做决定
>
> 分界线是**缺代码**还是**缺一个决定**。前者今天这台机器上写完就能验,
> 后者写得出来也不该由写的人拍板 —— 替人做完的决定不会有人复核,
> 它会以"已完成"的身份进下一轮清点。
>
> ### 还清:阶段 4 那两列的写入路径
>
> 14-20 记的账:`listing_image_items.shared_opt_in` / `angle` 落库、
> `_to_view` 读、§6.5 四条规则用,**而全树没有写入路径**。
>
> | 项 | 状态 |
> |---|---|
> | 入参 `ImageItemIn.shared_opt_in` / `angle`(角度是枚举,入严) | ✅ |
> | 出参 `ImageSetItemOut` 带回这两列(出宽,存量值不炸详情接口) | ✅ |
> | `create_set` 构造点写两个 kwarg | ✅ |
> | 归一化 `_angle_value()`:空串收成 NULL | ✅ 两种"没标注"不并存 |
> | 欠账守卫换成正向守卫(断言**每一个**构造点都写) | ✅ 「欠账已还」 |
> | 变异 `tools/mutate_batch14_23.py` | ✅ **9/9 一次全红** |
>
> 正向守卫断言"每一个"而不是"存在一个":退化回去最省事的路径不是删 kwarg,
> 是**加第二个构造点**(候选入集、复制图片集、导入)而漏掉这两列。
>
> ### 记账:§4.8 去重键,一笔**逾期**欠账
>
> PRD §4.8 逐字要求 `UNIQUE(spu_id, COALESCE(color_variant_id,''), sha256)`,
> 今天仍是 `UNIQUE(product_id, sha256)`(迁移 0011 那一版)。`0037` 自己写过
> "这一版**不含**……新去重键",随后六批没有一批补上。
>
> **它躲过六批的方式与 14-22 那一列不同,值得单独记住:它有守卫,而守卫是绿的。**
> 原 `test_dedupe_key_is_product_scoped` 把「不许全局唯一」(永久不变量)与
> 「今天是 product 作用域」(欠账)写在同一组断言里。混在一起时它钉住的是
> **旧键本身** —— 将来谁按 §4.8 落新键,它会红,而让它变绿最省事的做法
> 正好是把 PRD 那条要求撤回去。本批拆成两条,欠账那一半挂上还款日:阶段 5。
>
> **为什么是记账不是还账**:`media_assets` 今天**没有 `spu_id` 列**,
> 它有的是 `spu: String(64)`(反规范化字符串码)与 `color_variant_id`。
> §4.8 的键要 `spu_id`,所以第一步不是改约束,是回答「素材挂在 SPU 上,
> 挂的是字符串码还是真外键」—— 而那个答案连带决定 `variant_key` 退役
> 往哪切。两件事分开做的中间态是:一批素材按字符串码去重、另一批按外键,
> 而两者对"同一个 SPU"的判断可以不一致。**这是决定,不是代码。**
>
> ### 改准一条过期的理由:方案面板不是"缺一行 import"
>
> 14-20 那条欠账守卫写着"没接进路由是接线欠账……后者等有人写一行 import"。
> **本批逐条核路由时核出那句话是错的。** 面板要 `spuId`(UUID),而:
>
> | 想从哪拿 | 实际有什么 |
> |---|---|
> | `api/spus.ts` | 不存在 |
> | `batchApi` 的 `SpuGroup` | 只有 `spu: string`,反规范化字符串码 |
> | workbench 那几组出参 | 同样只有 `spu: string` |
> | `publish.ts` 的 `external_spu_id` | **平台侧**外部 id,不是本系统主键 |
>
> 唯一在出参里给 `spu_id` 的 schema 是 `generation_plan.py` 自己 ——
> **要拿 spu_id 得先有方案,要列方案得先有 spu_id。** 所以这笔账与
> 「三步建档 UI」(阶段 1 剩余项)是同一笔:都卡在"前端要有一个知道
> SPU 主键的宿主页"。按 §3.33 改准措辞:照着"写一行 import"去做的人
> 会发现无处可写,然后多半把 `spu` 字符串码传进 `spuId`,那时接口 422,
> 而错因指向后端。
>
> ### 按 PRD §13 重新清点
>
> | 阶段 | 交付项 | 完整落地 | 部分落地 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | 4 | 1(受众:SPU 层必填 ✅,`products` 那条 NULL 兼容缝还在) | 2(owner_id 切 UUID + `variant_key` 退役、三步建档 UI) |
> | 阶段 2 素材归属 | 6 | **3** | **2**(`evidence_class` 存储列 + CHECK;**§4.8 新去重键**) | 1(按颜色上传 UI) |
> | 阶段 3 真实识别 | 6 | 4 | 2(异步化 + cancel;按颜色重试) | 0 |
> | 阶段 4 多颜色图片 | 5 | **4** | **1**(方案面板无宿主页) | 0 |
> | 阶段 5 / 阶段 6 | 11 | 0 | 0 | 11 |
> | **合计** | **34** | **15** | **6** | **13** |
>
> 阶段 2 那一行 **4 → 3**:上一版把交付第一项「media_assets 归属外键**与新
> 去重键**」记成完整,而 14-22 补的是前半句。**这是一次向下修正,不是回退** ——
> 那一列的写入路径确实落了,记错的是"这一项完了"。
>
> 阶段 4 那一行 3 → 4:两列写入路径落地。
>
> ### 离线门禁
>
> ```
> 纯逻辑      2445/2445   0 失败,7 跳过(缺 pydantic / sqlalchemy)
> 本批变异       9/9      一次全红
> 锚点        436/436     21 份脚本
> 守卫窗口       514 个
> 交付         15/15
> 样例数据     10/10
> 前端语法      86/86
> ```
>
> **仍未执行**:前端四条(tsc / ESLint / Vitest / build)、
> `alembic upgrade/downgrade`、真库 pytest(38 条一次都没跑过,本批 +0)、
> Ruff / lint-imports、Docker build、Playwright。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**
>
> ### 本批**没有**动的两类,以及为什么
>
> | 没动 | 类别 | 理由 |
> |---|---|---|
> | `variant_key` 退役 + `owner_id` 切 UUID | 决定 | 不是数据迁移,是**身份变更**:回填 `color_variant_id` 会让已确认的颜色属性、图片标签、14-21 落的事实指纹同时指向不存在的变体。必须与素材归属改写在同一个动作里做 |
> | §4.8 新去重键 | 决定 | 见上,先要 `media_assets.spu_id` 那个决定 |
> | `evidence_class` 存储列 + CHECK | 决定 | 要回填,而回填规则在 `derive_evidence_class` 里会演进。迁移不许 import `app.*`(全仓 42 份没有一份破例),所以要么把规则冻成 SQL CASE(第二个判定点),要么不回填 —— 这是 0040 当时逐条权衡过的同一道题,本批不替它答 |
> | 三步建档 UI / 按颜色上传 UI / 方案面板宿主页 | 环境 | 写得出来,但这台机器没有 node_modules:tsc / Vitest / build 一条都跑不了。**给这个仓库加不可验证的前端等于扩大 D 类缺口**,不是缩小它 |
> | 识别 run 异步化 + cancel | 环境 | 阶段 1-3 里唯一一条真的卡在 Redis + worker 上的 |

> ## A45-batch14-22:素材颜色归属的写入路径 + 新结构样例数据(基线 14-21,2026/08/07)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-22-COLOUR-ATTRIBUTION.md`。
>
> ### 这一批是被一句提问核出来的,不是被守卫抓到的
>
> 上一批交完之后逐条对着 PRD §13 原文核阶段 1-3,核出一处**任何清单里都
> 没有的欠账**:
>
> > `media_assets.color_variant_id` 落库了(0037)、被两处判定读着,
> > **而全仓没有任何写入路径。**
>
> 它躲过了整整五批,原因不是没人上心,是**它不属于任何一条守卫盯的类别** ——
> 接线门禁盯"函数有没有被调用",这一列不是函数;欠账守卫盯"有人写过一句话
> 说它欠着",而没有人写过。
>
> ### 后果:上一批报的 AC-21 是虚的
>
> 这一列恒为 NULL 时,**颜色维整个塌成一维** —— 每张图都算通用图,于是
> 「给 A 色补图」和「补一张通用图」在指纹上完全一样。上一批刚接的
> `facts_stale` 会一路绿着,而 **AC-21 那条全称命题在真数据上永远平凡成立**。
>
> 也就是说:上一批 STATUS 写的「AC-21 算得出来了」,在没有写入路径的前提下
> 是虚的。本批补上之后那句话才成立(仍未在真库验过)。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | 归属写入链:接口 → asset_service → 影子写 → ingest | ✅ 四层都收得下并往下传 |
> | 去重命中只**补**空归属,不改已定的(`_fill_missing_colour`) | ✅ 与 `_fill_missing_role` 同口径 |
> | 跨 SPU 颜色 id 拦在写之前(`assert_colour_belongs_to`) | ✅ |
> | `sample-data/spus.json`:三色九 SKU + 单色对照 + 男装受众对照 | ✅ |
> | 生成器按颜色出图 + 通用图(+21 张,共 51) | ✅ |
> | seed 走 `spu_service.create_spu()`,**不另写建档路径** | ✅ |
> | `verify_sample_data` 新增 5 条(含「三颜色九 SKU 必须存在」) | ✅ 5/5 → **10/10** |
> | 守卫 `tests/pure/test_a45_batch14_22_colour_attribution.py` | ✅ **13 条** |
> | 变异 `tools/mutate_batch14_22.py` | ✅ **23/23 一次全红** |
> | 真库验证 `tests/test_a45_batch14_22_colour_attribution_db.py` | ⚠️ **9 条,已写、一次都没跑过** |
>
> ### 一处刻意的收紧
>
> `assert_colour_belongs_to` 对**没有 `spu_id` 的商品一律拒绝按颜色上传**。
> 老建档路径(`create_product` / CSV 导入)今天还不写这一列,所以那批商品
> 暂时用不了颜色上传 —— 它们本来也没有颜色可选(`POST /spus` 才建颜色)。
>
> 放行的代价更贵:允许把图挂到一个查不出关系的颜色上,而那会让**另一个
> SPU** 的事实过期 —— 一个没人动过的款突然一批字段回到待确认。
>
> ### 变异第一轮 5 条 GREEN,四条是守卫太松、一条是变异写错
>
> | | 问题 |
> |---|---|
> | L5 | `assert "color_variant_id=color_variant_id" in body` 被**同一函数里另一处正确的代码**喂真(§3.26 的新方向:这次不是文档喂假断言,是另一段对的代码)。改成按 AST 找 `MediaAsset(...)` 的构造 |
> | S6 / S8 | `assert "_first_sku(" in body` 被 `_first_sku(variant.id)` 喂真,而被掐掉的是 `_first_sku(None)`。**前缀断言只在那个前缀全仓唯一时才成立** |
> | V2 | 缺 pydantic 时整条跳过没人管 —— 补一条守卫钉住"只跳字段那一层,展开规则照验" |
> | S3 | **变异自己写错了**:改的是 `spu_code`,而对照组的判据是颜色个数,改名之后守卫照样绿。一条不做它自称做的事的变异,比没有变异更糟 |
>
> ### 按 PRD §13 重新清点
>
> | 阶段 | 交付项 | 完整落地 | 部分落地 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | **4** | 1(受众:SPU 层必填 ✅,`products` 那条 NULL 兼容缝还在) | 2(owner_id 切 UUID + `variant_key` 退役、三步建档 UI) |
> | 阶段 2 素材归属 | 6 | **4** | 1(`evidence_class`:派生单点 ✅,存储列 + CHECK ❌) | 1(按颜色上传 **UI**;后端这一批通了) |
> | 阶段 3 真实识别 | 6 | 4 | 2(异步化 + cancel;按颜色重试) | 0 |
> | 阶段 4 多颜色图片 | 5 | 3 | 2 | 0 |
> | 阶段 5 / 阶段 6 | 11 | 0 | 0 | 11 |
> | **合计** | **34** | **15** | **6** | **13** |
>
> 阶段 1 那一行 3 → 4:「测试数据与 Fixture 重置为新结构」完整落地。
> 阶段 2 那一行 3 → 4:「media_assets 归属外键」这一项此前只落了列,
> **本批补上写入路径才算落地** —— 上一版把它记成完整,那是错的。
>
> ### 离线门禁
>
> ```
> 纯逻辑      2441/2441   0 失败,7 跳过(缺 pydantic / sqlalchemy)
> 本批变异      23/23     一次全红
> 锚点        427/427     20 份脚本
> 守卫窗口       510 个
> 交付         15/15
> 样例数据     10/10      新增 5 条
> 导入        407 个文件
> ```
>
> **仍未执行**:前端四条、`alembic upgrade/downgrade`、真库 pytest
> (前几批 29 条 + 本批 9 条,共 38 条一次都没跑过)、Ruff / lint-imports、
> Docker build、Playwright。
>
> **AC-21 现在算得出来、也有真数据可算了,但仍未在真库上验过。**

> ## A45-batch14-21:`facts_stale` 派生接线 + 欠账还款日门禁(基线 14-20 合并包,2026/08/07)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-21-FACTS-STALE.md`。
> **阶段 3 交付第五项那半笔欠账还清了** —— 阶段 3 从 4 完整 / 1 部分 变成 5 完整 / 0 部分。
>
> ### 这一批还的是什么
>
> 上一版 STATUS 自己记着:「`facts_stale` 派生全仓零调用点,**AC-21 今天
> 演示不出来** —— 不是会答错,是没有任何地方在算」。本批把它接上:
>
> | 项 | 状态 |
> |---|---|
> | 迁移 0042:`product_attribute_values.input_fingerprint`(不回填) | ✅ 落码,**一次都没执行过** |
> | 作用域判定 `validation.fingerprint_scope()`(纯层,`OwnerType` 穷举) | ✅ |
> | 取数单点 `attributes.service.scope_fingerprints_for()` | ✅ 写入侧与读取侧唯一的共同口径 |
> | `facts_stale` 唯一调用点 `stale_fields()` | ✅ 档位判定复用 `queue_policy` 那张穷举表 |
> | `apply_evidence` / `confirm` 按 owner 记指纹 | ✅ **不是 run 行那一列**,见下 |
> | `changed_scopes` 进隔离 / 放行 / 改角色审计 | ✅ AC-21 在真环境唯一能被直接观察的地方 |
> | 工作台 `ATTR_FACT_STALE` + §6.3 完成条件带上指纹那一句 | ✅ |
> | 接口出参 `facts_stale` | ✅ |
> | 守卫 `tests/pure/test_a45_batch14_21_facts_stale.py` | ✅ **22 条** |
> | 守卫 `tests/pure/test_a45_batch14_21_delivery_gate.py` | ✅ **12 条**(盯门禁自己) |
> | 变异 `tools/mutate_batch14_21.py` | ✅ **32/32 一次全红** |
> | 真库验证 `tests/test_a45_batch14_21_facts_stale_db.py` | ⚠️ **9 条,已写、一次都没跑过** |
>
> ### 最要紧的一条口径:事实的指纹不是 run 行那一列
>
> 0040 落的 `input_fingerprint` 在 `ProductAttributeExtraction` 上,记的是
> 「这次 run 吃进去的是哪批素材」—— 共享作用域那一个。0042 这一列在
> `ProductAttributeValue` 上,记的是「这条事实按哪批素材立起来」。
>
> 拿前者填后者是让接线门禁"最省事变绿"的做法,而后果是共享事实与各颜色
> 事实**共用同一个哈希** —— 给颜色 A 补一张图会 stale 掉 B 色事实,
> **D1 从后门原样回来**,而每一条事实都带着一个看起来很像的 64 位指纹。
> 欠账守卫当初逐字点名的就是这条捷径,本批把它翻成反向断言钉住。
>
> ### 新增第 15 条交付门禁:欠账守卫都在还款日之内
>
> **这一条是被本批那笔欠账换来的。** `facts_stale` 的还款日写在 docstring
> 里(「那是阶段 4 的事」),阶段 4 落码了、事情没做,而守卫**没有变红** ——
> 它钉的是"没接线"这个事实,不钉"什么时候该接"。
>
> 落法:欠账守卫声明 `还款日:阶段 N`,当前阶段从本文件顶部那行
> `<!-- DELIVERY_STAGE: N -->` 读,`N <= 当前阶段` 即逾期。反验过 ——
> 把标记推到 5,四条欠账当场被逐条点名;而本批还的这一笔还款日正是"阶段 4"、
> 当前阶段也正是 4,**这条门禁早一批存在的话会当场红**。
>
> 仓库现存四条欠账全部补上了还款日(都是阶段 5),一条已还清的补了
> `欠账已还` 标记。
>
> ### 三处被自己的门禁咬出来的改动
>
> **一、迁移链守卫瞎了十二批。** `test_the_migration_chain_has_exactly_one_head`
> 的正则只认 `revision: str = ...`,而 0041 是裸赋值,从来没进过链表 ——
> 当时**碰巧绿**(漏掉的那条在链末端)。0042 挂在 0041 下面把断点移到链中间
> 才照出来。修法不是放宽正则,是加**覆盖率断言**:解析出的条数必须等于
> `versions/` 下的文件数。`verify_delivery` 那条同名门禁一直是对的 ——
> 同一条不变式两个实现,瞎的是纯层那个。
>
> **二、还款日门禁把已还清的守卫和盯着它自己的元守卫判成了欠账。** 前者是
> 过去时叙述("原来记的是欠账"),后者是元守卫必须说出那句标记才讲得清自己
> 守什么。§3.26 的第三个方向:文档把判据喂成**假阳**,而假阳最省事的消法是
> 把门禁整条删掉。修法:三种状态各有一句话 + 声明只认 docstring 开头那一段
> (仓库里四条欠账守卫的声明本来就全在第一行)。
>
> **三、我自己打断了别人的变异锚点。** 改 `flow.py` 那行状态判定时,
> `mutate_batch14_11.py` 的 F2 失锚 —— 而失锚的变异不报错、不变红,
> 只是安静地什么都没验。锚点审计当场抓到,已重锚。
>
> ### 两处陈旧理由(§3.33)
>
> `verify_delivery` 的登记项注释说 `color_variant_id`"今天不存在"(0037 早落了)
> 并引用一个已改名的守卫;`variant_gate_roles` 的 docstring 说"今天没有调用点"
> (14-15 起就在 `_material_facts` 里被调着)。都改了。
> `variant_confirmable_roles` 的理由换成真的那一条:缺的不是列,是按颜色上传
> 的界面(阶段 2 剩余项)。
>
> ### 按 PRD §13 重新清点
>
> | 阶段 | 交付项 | 完整落地 | 部分落地 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | 3 | 0 | 3 |
> | 阶段 2 素材归属 | 6 | 3 | 0 | 3 |
> | 阶段 3 真实识别 | 6 | **5** | **0** | 1(§4.6 异步化 + cancel,**唯一真要环境的一条**) |
> | 阶段 4 多颜色图片 | 5 | **3** | **2** | 0 |
> | 阶段 5 Listing 与草稿 | 5 | 0 | 0 | 5 |
> | 阶段 6 一体化向导 | 6 | 0 | 0 | 6 |
> | **合计** | **34** | **14** | **2** | **18** |
>
> 阶段 4 那一行从 2/3 变成 3/2:「任务绑定 color_variant_id 与双指纹」缺的那
> 一瓣(共享侧的比较对象 + "一条事实继承哪次 run 的指纹"这个决定)本批补齐 ——
> 决定是**不继承**,事实自己按 owner 算自己作用域的指纹。
>
> ### 离线门禁
>
> ```
> 纯逻辑      2428/2428   0 失败,7 跳过(缺 pydantic / sqlalchemy)
> 本批变异      32/32     一次全红
> 锚点        404/404     19 份脚本
> 守卫窗口       505 个    反向断言都吃着封闭窗口
> 交付         15/15      新增「欠账守卫都在还款日之内」
> 样例数据       5/5
> 导入         404 个文件
> ```
>
> **仍未执行**:前端四条(tsc / ESLint / Vitest / build)、
> `alembic upgrade/downgrade`(0037～**0042** 从未执行)、**真库 pytest**
> (前两批那 20 条 + 本批 9 条,共 29 条一次都没跑过)、Ruff / lint-imports 本体、
> Docker build、Playwright。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**
> **AC-21 从"没有任何地方在算"变成"算得出来、但没在真库上验过"** ——
> 这是两件事,别合并计算。

> ## A45-batch14-20 并线合并:阶段 3 识别 run 身份 + 阶段 4 多颜色图片生产(基线 14-19,2026/08/06)
>
> **这一格是两条并行线合并后的状态,不是一批的成果。**两条线都自称
> `A45-batch14-20`、基线都是 14-19,逐条说明各自在:
>
>     docs/MERGE-A45-BATCH14-20-RUN-IDENTITY.md          阶段 3,§4.6 五列 + §9.2 幂等
>     docs/MERGE-A45-BATCH14-20-STAGE4-IMAGE-PRODUCTION.md  阶段 4,§6.5 + 生成方案
>
> **迁移链 head `0039` → `0041`。**阶段 4 那份原写作 `0040`,与阶段 3 撞号,
> 合并时改为 `0041`(`down_revision = "0040"`)。两份动的是互不相交的表,
> 所以是串起来不是合表。**同号不是"两个 head",是 alembic 加载期直接报
> `Multiple revisions with the same identifier`,整条链一步都跑不了。**
>
> **这一处是门禁抓到的,不是人看出来的** —— 「迁移链单一 head」那条交付门禁里
> 恰好有一句 revision 唯一性检查(A24 那轮 `0021` 撞号返工换来的),
> 合并时当场变红。
>
> 另外两处撞车没有任何东西会响:`tools/mutate_batch14_20.py` 同名不同内容
> (后写的覆盖先写的,拆成 `_run_identity` / `_stage4`);`docs/DECISIONS.md`
> 两份 §3.34(永远不会炸,只是让「§3.34 说了什么」有两个答案;阶段 4 那节
> 改编为 §3.35)。**三处里只有一处有门禁,因为只有它有人付过代价** ——
> 本次给决策编号补了一条,交付项 13 → **14**。
>
> ### 按 PRD §13 重新清点(逐条对着合并后的代码核过)
>
> | 阶段 | 交付项 | 完整落地 | 部分落地 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | 3 | 0 | 3(老建档路径切新表、owner_id 切 UUID + `variant_key` 退役〈仍 41 处〉、三步建档 UI + Fixture 重置) |
> | 阶段 2 素材归属 | 6 | 3 | 0 | 3(`evidence_class` 存储列 + CHECK、新去重键、按颜色上传 UI) |
> | 阶段 3 真实识别 | 6 | **4** | **1** | 1(§4.6 异步化 + cancel) |
> | 阶段 4 多颜色图片 | 5 | **2** | **3** | 0 |
> | 阶段 5 Listing 与草稿 | 5 | 0 | 0 | **5** |
> | 阶段 6 一体化向导 | 6 | 0 | 0 | **6** |
> | **合计** | **34** | **12** | **4** | **18** |
>
> **「部分落地」这一列是本次合并新开的,它不是「判定落地但接不上线」换个名字。**
> 那一列(曾压着 5 项)说的是"判定写完了,但没有列可写";它在阶段 3 那批清零,
> **现在仍是零**。这一列说的是另一件事:**一个交付项本身有几瓣,落了其中几瓣。**
>
> 开这一列是有风险的 —— 中间列天然是藏东西的地方。所以每一项必须写清
> **缺的是哪一瓣**,写不清的就不许放进来:
>
> | 阶段 4 交付项 | 落了哪一瓣 | 缺哪一瓣 |
> |---|---|---|
> | GenerationPlan 实体与 UI | 表 / 服务 / API / 指纹判定 | 面板与 API 客户端**全树零 import**,进不去任何路由;一次都没编译过 |
> | 任务绑定 color_variant_id 与双指纹 | 颜色那一半(比较对象在同一批) | 共享那一半:`ProductAttributeValue` 上没有 `input_fingerprint`,且"一条事实继承哪一次 run 的指纹"这个决定没做 |
> | 图片集 variant 绑定入口 + §6.5 混排 + 覆盖率门禁 | §6.5 四条规则 + `coverage()` 单一判定点 + 门禁 | `shared_opt_in` / `angle` 两列**没有任何写入路径** —— 读得到、判得了、写不进 |
> | 评分器事实一致性维度组接一票否决 | 全部 | — |
> | 图片版本失效接扩展矩阵 | 全部(6×4 → 8×4) | — |
>
> ### 阶段 3 那一行从 5 改成 4:`facts_stale` 派生没有任何调用点
>
> **这是合并复审重新核出来的,上一版 STATUS 记错了。**阶段 3 交付第五项原文是
> 「双作用域指纹计算**与 `facts_stale` 派生**」。计算那一半接了
> (`fingerprints` / `views` 在 `_run_identity` 里,也登记进了接线门禁);
> **派生那一半全仓零调用点** —— `scope_fingerprint.facts_stale` 与
> `changed_scopes` 只出现在文档字符串和 §8.1 矩阵的机制串里。
>
> 后果是**系统今天回答不了"这条商品事实还成不成立"**。`flow.py` 里那几个
> `stale` 是 `CopyFacts` / `DraftFacts`(文案与草稿的下游过期),不是商品事实。
> 于是 **AC-21(传 A 色图不 stale B 色事实)今天演示不出来** ——
> 不是会答错,是没有任何地方在算。
>
> 这条欠账早有守卫记账
> (`test_the_wired_half_is_registered_and_the_unwired_half_is_not`),
> 而**它写的还款日是"那是阶段 4 的事"**:要等属性值行也带上指纹列。
> 阶段 4 落码了,这件事没做 —— 属于 §3.34 说的那种情况:
> 欠账守卫的还款日到了,而还款没发生,守卫本身不会因此变红。
>
> 三条欠账各有一条点名守卫记账,**接线那天它们会红**,那是还款日:
>
>     test_the_plan_panel_is_written_but_not_reachable_yet_and_this_is_the_ledger
>     test_the_two_new_item_columns_have_no_writer_yet_and_this_is_the_ledger
>     _variant_sample_fingerprint 的 docstring(共享半)
>
> ### 第三条欠账值得单独读一遍:它的失效方式是"报错报在别的地方"
>
> `angle` 恒为 NULL,而 §6.5 的角度验收已经是门禁。后果不是"少了个功能":
> **某个颜色一配上方案,那个颜色的必要角度就永远覆盖不了,图片集再也批不过**
> —— 而门禁报的是「缺正面图」。运营会去补图,补多少张都没用,
> 因为新图的 `angle` 同样是 NULL。
>
> 阶段 4 原稿把这件事记在「验不到什么」里,措辞是"接线点在服务层,而服务层要
> sqlalchemy"。合并复审时改了:在 `create_set` 里多写两个 kwarg 不需要**运行**
> sqlalchemy,只需要有人写。**缺的不是环境,是代码** —— 混成一句话的后果是
> 下一个人以为"等机器就行"。
>
> ### 一处既有行为被翻转
>
> `image_set_rules.primary_for()` 不再回落到 SPU 通用图。回落在单色时代是对的,
> 多色下同一段代码的含义变成"缺这个颜色的图就用别的颜色的" ——
> 后果是红色 SKU 挂着黑色主图上架。三条老守卫因此翻转,由变异 R1/R2 重咬。
>
> BLOCK-02 等的从来不是 schema(`variant_id` 与 COALESCE 唯一约束从 0013 起
> 就在库里),是"通用图与颜色图混排以谁为准"这一句业务决定。§6.5 定死了它,
> 所以这一批**才敢**把 `variant_coverage` 从诊断变成门禁。
>
> ### 离线门禁(合并后重跑,不是两批的数字相加)
>
> ```
> 纯逻辑      2394/2394   0 失败,7 跳过(缺 pydantic / sqlalchemy)
> 阶段 4 变异    41/41    一次全红
> 阶段 3 变异    20/20    一次全红
> 锚点        372/372    18 份脚本
> 守卫窗口       495 个   反向断言都吃着封闭窗口
> 交付         14/14     含「迁移链单一 head」(已认 0041)+ 新增「决策日志编号不重复」
> 样例数据       5/5
> 导入         400 个文件
> ```
>
> **仍未执行**:前端四条(tsc / ESLint / Vitest / build)、
> `alembic upgrade/downgrade`(0037 / 0038 / 0039 / **0040** / **0041** 从未执行)、
> **真库 pytest**(阶段 3 那 11 条 + 阶段 4 那 9 条,共 20 条一次都没跑过)、
> Ruff / lint-imports 本体、Docker build、Playwright 浏览器。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**

> ## A45-batch14-20 §4.6 五列落库,§9.2 幂等与 §5.3 指纹接线(基线 14-19,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-20-RUN-IDENTITY.md`。
> **PRD v3.1 阶段 3 的两条「判定落地但接不上线」还清了**;阶段 3 六项里五项落码。
>
> ### 这一批还的是一笔八批的欠账
>
> `run_state.py`(14-12)与 `scope_fingerprint.py`(14-9)两个判定模块写完并
> 穷举验过之后**接不上线整整八批**,每一批的理由都是同一句:§4.6 的五个列
> 一个都不存在。没有列就没有地方存键、也没有行可以查重。
>
> 迁移 0040 把列落下去,本批接线。**判定本身一个字没动。**
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | 迁移 0040:`status` / `spu_id` / `input_fingerprint` / `requested_scope` / `idempotency_key` | ✅ 落码,**一次都没执行过** |
> | 部分唯一索引 `uq_attr_extractions_idempotency_key` | ✅ 谓词由判定生成,迁移里是冻结孪生 |
> | `run_extraction` 接上查重 → `reuse_verdict` → 保存点兜底 | ✅ 落码 |
> | 终态写回 `status` 列,派生属性删除 | ✅ 判定调用点从读取方挪到写入方 |
> | `declared_versions()` 钩子(Mock + Vision) | ✅ 版本必须在**付钱之前**报得出来 |
> | 守卫 `tests/pure/test_a45_batch14_20_run_identity.py` | ✅ **14 条,20 次变异一次全红** |
> | 真库验证 `tests/test_a45_batch14_20_run_identity_db.py` | ⚠️ **11 条,已写、一次都没跑过** |
>
> ### 按 PRD §13 重新清点(逐条对着代码核过,不是照抄任务表)
>
> **⚠️ 下面这张表是并线合并之前的清点,阶段 4 那一行已经作废** ——
> 它写「未开工 5」,而合并进来的阶段 4 那批把其中 2 项完整落地、3 项部分落地。
> 现行清点见本文件顶部那一格。这张表留着不改,因为它记录的是**当时**的判断。
>
> | 阶段 | 交付项 | 完整落地 | 判定落地但接不上线 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | 3 | 0 | 3(老建档路径切新表、owner_id 切 UUID + `variant_key` 退役〈仍 41 处〉、三步建档 UI + Fixture 重置) |
> | 阶段 2 素材归属 | 6 | 3 | 0 | 3(`evidence_class` 存储列 + CHECK、新去重键、按颜色上传 UI) |
> | 阶段 3 真实识别 | 6 | **5(本批 +2)** | 0 | 1(§4.6 异步化 + cancel) |
> | 阶段 4 多颜色图片 | 5 | 0 | 0 | **5**(`GenerationPlan` 表都不存在) |
> | 阶段 5 Listing 与草稿 | 5 | 0 | 0 | **5** |
> | 阶段 6 一体化向导 | 6 | 0 | 0 | **6** |
> | **合计** | **34** | **11** | **0** | **23** |
>
> 「判定落地但接不上线」这一列**本批清零** —— 它曾经压着 5 项,是这个项目
> 最大的一堆存货。剩下的每一项都不是「写完了没接」,而是**真的没写**。
>
> 阶段 4 逐条核过仍是零改动:`GenerationPlan` / `plan_fingerprint` 全仓无引用,
> `generation_tasks` 无 `color_variant_id`,评分器无事实一致性维度,
> `variant_coverage()` 明写「诊断,不阻断」,`ChangeSource` 仍是 6 个值。
>
> **验收侧照旧:AC-01～AC-22 没有一条在真环境验收过。阶段 P0 仍未关闭。**
>
> ### 三个判断
>
> 1. **`status` 不回填。** 回填要把判定重写成 SQL CASE(第二个判定点),
>    或把它 import 进迁移(把演进中的判定冻进历史脚本)。改用
>    `server_default='FAILED'` —— 存量行落在不放行那一侧,既不进事实合并
>    也不占键。全仓 40 份迁移没有一份 import 过 `app.*`,这条不由本批开口子。
> 2. **建不出键就留空,不编一个。** 三种情况:增量识别、商品没有 `spu_id`、
>    抽取器报不出调用前版本。留空 = 退回本批之前的行为;编一个 = 两个不同
>    SPU 的同型请求撞同一个键,第二个商品会填上第一个商品的属性,接口 200。
> 3. **唯一索引必须是部分的。** 全表唯一的后果是一次失败之后同样的输入再也
>    建不出第二个 run —— 而那正是重试的定义。
>
> ### 变异第一轮只红 12/20,8 条 GREEN 全是守卫上的真洞
>
> 补完才到 20/20。三个值得记的形状:
>
> - **docstring 被当成代码**:`declared_versions` 的文档里写着「不取
>   `result.model_name`」,一条查 `result.` 的断言把那句**解释**当成了实现,
>   守卫红在完全正确的代码上;反方向同样成立 —— 文档里出现
>   `imported_url_trusted=True`,把断言喂成了平凡真。§3.26 第四节第七次成立。
> - **宽断言在宿主搬家后变松**:`"row.status" in body` 原来只可能命中那个比较,
>   本批之后终态自己也写这一列,于是它变成平凡真。
> - **模块级赋值绕过类体扫描**:`ProductAttributeExtraction.status = property(...)`
>   写在文件底部同样能盖住那一列,而只扫类体的守卫一个字都不会说。
>
> ### 五条欠账守卫按它们自己的约定被换掉
>
> 14-12 与 14-9 的两条「接不上线」删掉换正向;14-13 的登记守卫反向断言下移到
> `facts_stale` / `changed_scopes`;14-13 的失败作用域守卫**收窄** ——
> `requested_scope` 落库了但不是它要的那一列(一个记「请求哪个作用域」、
> 一个记「跑完之后哪个全军覆没」);终态宿主守卫改成参数化(「点名做法」第六次)。
>
> ### 门禁
>
>     纯逻辑    2345/2345(7 跳过均为缺 pydantic/sqlalchemy)
>     锚点      331/331(17 份脚本)
>     交付      13/13
>     变异      本批 20/20,14-12 搬家后 34/34
>
> ### 本批之后仍然欠着的
>
> - §4.6 的**异步那一半**:Celery + cancel + `QUEUED` 档,要 Redis 与 worker
> - §11 的失败作用域清单:欠 `failed_scopes` 一列
> - 事实侧指纹列:`facts_stale` 接线要等属性值行也带指纹(阶段 4)
> - 老建档路径不写 `spu_id`(阶段 1 剩余项)—— 在它落地前,CSV 导入与
>   `create_product` 建出来的商品拿不到幂等保护

> ## A45-batch14-19 §5.1 白名单成为取数入口(基线 14-18,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-19-EVIDENCE-QUERY.md`。
> **PRD v3.1 阶段 2 交付第四项**;阶段 2 六项里五项落码,剩一项要真库。
>
> ### 先更正一条过期的「已知限制」
>
> 旧版「已知限制」写着「今天 `run_extraction` 走的仍是 `usable_assets()`,
> 只过滤 `status = READY`,**后果是 AI 图会进识别输入并产生真实付费调用**」。
> **这句话从 A45-batch14-7 起就不成立了** —— 那一批接了白名单,AI 图进不来。
> 一条过期的"正在烧钱"警报比没有警报更糟:它会让人去查一个已经不存在的问题,
> 也会让人以为这条路还没通。本批一并改掉。
>
> ### 真正没做完的是那半句「唯一取数入口」
>
>     取数    usable_assets()              未过滤,谁都能调
>     过滤    run_extraction 里一段推导式   只保护这一个调用点
>
> 任何一个新写的调用点只要照着 `usable_assets()` 抄一行,就能把 AI 图喂进
> 付费抽取器,而**没有任何地方会红**。§5.1 防的不是今天的代码,是明天的调用点。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `media.evidence_assets_for(product_id, scope=…)`:SQL 粗筛 + 同一个判定 | ✅ 落码,**SQL 一次都没执行过** |
> | `usable_asset_count()`:识别路径拿不到未过滤的**行** | ✅ 拿不到行就不会有人用错行 |
> | `run_extraction` 改走入口,两条空错误分开报 | ✅ 落码 |
> | 守卫 `tests/pure/test_a45_batch14_19_evidence_query.py` | ✅ **10 条,15 次变异一次全红** |
> | 真库验证 `tests/test_a45_batch14_19_evidence_query_db.py` | ⚠️ **6 条,已写、一次都没跑过** |
>
> ### 判定仍然只有一处
>
> SQL 只做粗筛,每一条都被判定蕴含(`_COARSE_FILTER_COLUMNS` 那张表),
> 最终一律由 `asset_is_extraction_input()` 说了算。全写成 WHERE 子句等于给
> `evidence_class` 造第二个判定点 —— 而两个判定点漂移时没有人会发现。
>
> ### 「点名做法」第五次,外加一个门禁瞎点
>
> 14-7 那两条守卫把「过滤写在 `run_extraction` 里」当成了不变式,搬家那天
> 双双变红而口径其实更严。第五次的一般化见 `DECISIONS.md` §3.33。
>
> 顺带修了 `verify_delivery.py` 接线门禁的一个盲点:它按**文件名**去重,
> 于是 `app/media/service.py` 的函数被 `app/attributes/service.py` 调用时
> 被当成"模块内部互调"。`service.py` 是本仓最常见的文件名 ——
> **这条门禁在最容易发生接线遗漏的那一批模块上恰好是瞎的。**
>
> ### 阶段 2 还剩什么(都要真库,本轮按要求跳过)
>
> | 项 | 卡在哪 |
> |---|---|
> | `evidence_class` 存储列 + CHECK | 要给存量行**回填**派生值,回填只能在有库的机器上当场验 |
> | 新去重键 `(spu_id, sha256)` | 要先给 `media_assets` 加 `spu_id` 外键,那是又一次归属改造 |
> | 按颜色上传 UI | 无 node_modules,写了也跑不了 Vitest |
>
> ### 离线门禁
>
> 纯逻辑 **2325/2325**(0 失败,7 跳过)、本批变异 **15/15**、14-7 重跑 **23/23**、
> 锚点 311/311(16 份脚本)、audit-guards 482 个守卫、交付 13/13、
> 样例数据 5/5、导入 388。
>
> **仍未执行**:前端四条(tsc / ESLint / Vitest / build)、`alembic upgrade/downgrade`
> (0037 / 0038 / 0039 从未执行)、**真库 pytest(含本批那 6 条)**、
> Ruff / lint-imports 本体、Docker build、Playwright 浏览器。

> ## A45-batch14-18 计费量问厂商要,并记下是谁说的(基线 14-17,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-18-PROVIDER-USAGE.md`。
> **阶段 1 的最后一条代码项(任务 9)**;剩下的都要真 Key、真账单、真浏览器。
>
> ### 修的是一个「算了、存了、没人读」的缺陷
>
> `providers/fashn.py` 早就从 `x-fashn-credits-used` 解析出厂商实际扣的额度,
> 抄进候选图的 metadata —— 然后**全仓再没有第二处读它**。台账记的一直是
> `max(provider_count, 1)`(我们收到几张图)。
>
> 官方参考表(`docs/vendor/fashn-skill/reference.md`「Credits」)里 `tryon-max`
> 一张图是 **2~5 个额度**,取决于 `FASHN_RESOLUTION` 与 `generation_mode` ——
> 两个旋钮运营在设置页都能改。所以少记的倍数不是常数,是跟着配置浮动的系数,
> **方向恒定是少记**:预算横幅一路绿着,账单是它的好几倍。
> §10.2 第 5 条准入(用量与厂商账单一致)按旧写法不可能对得上。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `ProviderUsage` + `settle_billable_units()`(唯一一处"谁说了算"的判定) | ✅ 落码并跑过 |
> | 基类钩子 `usage_from_candidates()`,默认**不**冒充厂商权威 | ✅ 只有 fashn 覆写,守卫钉着 |
> | FASHN 覆写:按 `prediction_id` 去重求和,缺一个数就整笔退回推算 | ✅ 落码并跑过 |
> | `provider_usage_records.units_source` 列 + CHECK + 迁移 `0039` | ✅ 落码,**迁移一次都没执行过** |
> | 接线:`_await_and_collect` 先问厂商,两数不一致记 info 日志 | ✅ 落码 |
> | 守卫 `tests/pure/test_a45_batch14_18_provider_usage.py` | ✅ **19 条,19 次变异一次全红** |
>
> ### 一条老守卫因为「点名做法」第四次变红
>
> `test_the_ledger_bills_what_the_provider_produced_not_what_we_saved` ——
> **名字说的是不变式,断言查的是 `billable_units=` 实参里有没有 `provider_count`
> 这几个字**。换成"先问厂商、问不到再退回 `provider_count`"之后它红了,
> 而口径其实更严。已改成钉不变式(摊平局部变量后追来源,另加反向断言禁止追到
> `stored`)。第四次的一般化见 `DECISIONS.md` §3.32 第四节。
>
> ### 还报不出真实用量的两类付费调用(欠账,守卫记着)
>
> | 调用 | 今天的来源 | 为什么本批不接 |
> |---|---|---|
> | FASHN 轮询 / 取结果 | `inferred` | 厂商每次真的又调了一次,但那几次不单独计费 |
> | 评分(vision) | `inferred` | 响应体里有 `usage`,**是 token 不是次** |
>
> ~~属性识别(vision)~~ **已销账(A45-batch18 / P1-2)**:
> `attributes/service.py` 现在传真实的 `units_source`,单位是"发出去了几个请求"
> (`call_accounting.settle_extraction_units`),与价目表的每次调用口径一致,
> 接线不改变任何已配价格的含义。守卫
> `test_the_paid_paths_that_still_cannot_report_are_named_here`
> 已同步收窄到只盯评分那一条 —— 不收窄的话它会在正确实现上稳定报红。
>
> **只剩评分那一类刻意不接。**(上一版这里还写着"vision 那两类"、还把
> `attribute_extract` 和 `vision_score` 并排列着 —— 与紧邻上面那段"属性识别已销账"
> 直接矛盾。同一节里两句话互相打架时,读的人信哪一句是随机的,而信错的那一半
> 会去改一处已经改对的代码。A45-batch18 / P3 改掉。)
>
> 理由是单位:评分的计价单位是 token,而价目表 `PROVIDER_PRICE_BOOK` 里
> `vision_score` 今天配的是**每次调用**的价。改成 token 会在不改任何配置的
> 情况下**静默改变已配价目表的含义** —— 金额一夜之间差几个数量级,
> 而没有任何地方会说为什么。那是另一批的活。
>
> 识别那一类能先接、且已经接了,正是因为它记的不是 token:
> `call_accounting.settle_extraction_units` 数的是"发出去了几个请求",
> 与价目表的每次调用口径同一个单位。
> `test_the_paid_paths_that_still_cannot_report_are_named_here` 记着剩下这笔账
> (它今天只扫 `services/evaluation_service.py`),接线那天它会红。
>
> ### 第一次接真端点时要盯的三件事(本批验不到)
>
> 判定是纯的、验得很干净,但**厂商到底会不会那么答,这里一个字都证明不了**:
>
>     一、每一次 status 响应是不是都带 `x-fashn-credits-used`
>     二、带的是**本次**消耗还是**累计**消耗(是累计的话去重逻辑要换)
>     三、失败的 prediction 是不是真的不计费(文档这么说,没验过)
>
> 三条里任何一条不成立,`units_source='provider'` 的那些行就是错的 ——
> 而它们比 `inferred` 更容易被当成权威。**真连之前先跑一次单张、拿后台账单对一次。**
>
> ### 离线门禁
>
> 纯逻辑 **2315/2315**(0 失败,7 跳过)、本批变异 **19/19**、锚点 297/297(15 份脚本)、
> audit-guards 476 个守卫全绿、交付 13/13、样例数据 5/5、导入 385。
>
> **仍未执行**:前端四条(tsc / ESLint / Vitest / build)、`alembic upgrade/downgrade`
> (0037 / 0038 / **0039** 从未执行)、真库 pytest、Ruff / lint-imports 本体、
> Docker build、Playwright 浏览器。

> ## A45-batch14-17 筛选状态住进 URL,阶段 0 最后一条代码项收口(基线 14-16,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-17-URL-FILTERS.md`。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `useUrlFilters`(URL 是唯一真相)+ 五个 codec,默认值不写进 URL | ✅ 落码 |
> | 四页搬完:工作台列表 / 生成任务 / 逐件快审 / **操作审计** | ✅ 参数名一字未改,旧链接照旧能用 |
> | `useUrlSeed.ts` 删除 | ✅ 留着它下一个人会照着抄回「用完擦掉」 |
> | 守卫 `tests/pure/test_a45_batch14_17_url_filters.py` | ✅ **12 条,13 次变异一次全红** |
> | Vitest `nav-and-url-filters.test.tsx` 重写 | ⚠️ **13 条,已写、一次都没跑过** |
>
> ### GAP-033 关了,§8.2 四条要求现在全满足
>
> 从前只有「点进来能带条件」一条成立。URL 可复制 / 刷新保留 / 后退保留
> 三条都死在 `clear()` 那一行上 —— 而那一行修不成补丁:**只要 URL 是初值,
> 组件内那份 state 就是第二处真相**,擦参数正是为了不让 URL 顶掉手选而做的取舍。
>
> ### 一条变异漏网,补了守卫
>
> `flagParam` 的 `read` 退回写死的 `false` 而不是自己的默认值,第一轮没抓住。
> 后果是**参数完全缺席**时审计页的「只看上架闭环」默认变成关着 ——
> 降噪没了,而开关和数据一致、没有任何地方报错。
> `write` 守得住而 `read` 守不住:默认值漏进 URL **看得见**(地址变长),
> 默认值读错只是**行为变了**。
>
> ### 三条守卫因为点名做法而红(第三次)
>
> 14-4 点名 `import { useUrlSeed }`、14-10 点名 `"setAudience" in page`,
> 加上 14-16 记过的 `heads == {"0037"}` —— 同一个形状三批三次,
> 红的原因都是「有人把这件事做得更好了」。全部改成不变式,一般化见 `DECISIONS.md` §3.31。
>
> ### 离线门禁
>
> 纯逻辑 **2296/2296**(0 失败,7 跳过)、本批变异 13/13、锚点 278/278(14 份脚本)、
> audit-guards 470 个守卫全绿、交付 13/13、样例数据 5/5、导入 383、前端语法 84/84。
>
> **仍未执行**:前端四条(tsc / ESLint / **Vitest** / build)、`alembic upgrade/downgrade`
> (0037 / 0038 从未执行)、真库 pytest、Ruff / lint-imports 本体、Docker build、
> Playwright 浏览器。

> ## A45-batch14-16 溯源列落库,候选落盘写入(基线 14-15,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-16-PROVENANCE.md`。与 14-15 是同一件事
> 的两半:那一批落归属外键,这一批落溯源列。**阶段 2 的存储层还剩两样**
> (`evidence_class` 列 + CHECK、新去重键),都要回填或约束,等真库。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `media_assets.generation_task_id` / `generation_candidate_id` + 迁移 `0038` | ✅ 落码,**迁移一次都没执行过** |
> | `ingest()` 收并写这两列;`shadow_from_candidate` 候选落盘时写入(§4.8 原话) | ✅ 落码并跑过 |
> | §11 批那条「过渡记号是今天唯一会命中的一路」收掉「唯一」 | ✅ 三路现在都命中 |
> | 守卫 `tests/pure/test_a45_batch14_16_provenance.py` | ✅ **9 条,9 次变异一次全红** |
>
> ### 刻意不回填
>
> 存量 AI 影子行只有 `legacy_kind` / `legacy_id`。回填要**信任 `legacy_id`
> 指得到候选行**,而那是迁移期约定不是外键 —— 回填错的表现是一张人工样品
> 被标成 AI 生成,从此永久掉出识别输入。判定认「任一信号命中即算带溯源」,
> 所以过渡记号那一路必须留着(变异 W3 钉着)。
>
> ### 两次是守卫自己先红的
>
> 一是**上一批**那条 head 守卫点名了 `{"0037"}`,本批加 0038 时当场红 ——
> 「规矩要写成不变式不是当时那个值」,CLAUDE.md 早写过,我上一批刚写下
> 就违反了一次;二是本批一条守卫把列名查在了 `upgrade()` 那一段里,
> 而列名由模块级常量驱动。第二条是**正向**断言,所以它红了、有人看见了 ——
> 正是 14-14 那条门禁只管反向断言的理由。
>
> ### 离线门禁
>
> 纯逻辑 **2284/2284**、本批 9/9、14-7 重跑 24/24、锚点 265/265(13 份脚本)、
> audit-guards 457 个守卫全绿、交付 13/13(迁移链单一 head = 0038)、导入 381。
>
> **仍未执行**:`alembic upgrade/downgrade`(0037 / 0038 都没跑过)、真库 pytest、
> Ruff / lint-imports 本体、前端四条、Docker build。

> ## A45-batch14-15 素材归属外键落库,四批欠账收其二(基线 14-14,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-15-ATTRIBUTION.md`。
>
> ### 先说清点:按 PRD §13 还剩多少
>
> 逐条对着代码核过(不是照抄任务表):
>
> | 阶段 | 交付项 | 完整落地 | 判定落地但接不上线 | 未开工 |
> |---|---|---|---|---|
> | 阶段 1 身份规范化 | 6 | 3 | 0 | 3(owner_id 切 UUID、`variant_key` 退役〈全仓仍 41 处〉、Fixture 重置) |
> | 阶段 2 素材归属 | 6 | **2(本批 +1)** | 2 | 2 |
> | 阶段 3 真实识别 | 6 | 3 | 3 | 0 |
> | 阶段 4 多颜色图片 | 5 | 0 | 0 | **5**(`GenerationPlan` 表都不存在) |
> | 阶段 5 Listing 与草稿 | 5 | 0 | 0 | **5** |
> | 阶段 6 一体化向导 | 6 | 0 | 0 | **6** |
> | **合计** | **34** | **8** | **5** | **21** |
>
> **验收侧更要紧:AC-01～AC-22 没有一条在真环境验收过。**AC-21 是穷举证过的
> (14-9),AC-22 落码了(§11 批),两条都没在真库跑过。阶段 P0 仍未关闭。
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `media_assets.color_variant_id` + 迁移 `0037`(纯增列、可空、SET NULL) | ✅ 落码,**迁移一次都没执行过** |
> | 14-10 的颜色作用域接线(`_material_facts` 按颜色分桶) | ✅ 落码并跑过 |
> | `scope_fingerprint.AssetView`:列名翻译(id→asset_id、sha256→content_hash) | ✅ 落码并跑过 |
> | 两条欠账守卫 → 接线守卫(14-9 / 14-10) | ✅ 它们**自己红了**,这正是当初写它们的理由 |
> | 守卫 `tests/pure/test_a45_batch14_15_attribution.py` | ✅ **9 条,6 次变异全部验红** |
>
> ### 变异第一轮 2/7,而门禁又抓了我一次
>
> 七条里五条绿 —— 接线守卫只覆盖了两种坏法,补齐后 7/7。**然后
> `audit-anchors` 抓住第二件事**:本批 `SUITE_FILTER` 写成了前缀,一次挑中
> 6 个套件 —— **归因是假的**。改成精确文件名重跑,W2(分桶改读
> `variant_hint`)当场变绿:它只有 14-10 的守卫在管。处理不是补一条重复守卫,
> 是把那条变异**搬到 14-10 的脚本**里。两份脚本现在归因都是真的。
>
> ### 离线门禁
>
> 纯逻辑 **2275/2275**(跳过 7)、本批 6/6、14-10 重跑 24/24、锚点 256/256
> (12 份脚本)、audit-guards 全绿、交付 13/13(含迁移链单一 head = 0037)、
> 导入 379、样例 5/5、前端 syntax-check 84/84。
>
> **仍未执行**:`alembic upgrade/downgrade`(0037 与 0035/0036 一样没跑过)、
> 真库 pytest、Ruff / lint-imports 本体、前端四条、Docker build。

> ## A45-batch14-14 守卫的窗口必须是封闭的(基线 14-13,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-14-SOURCE-WINDOWS.md`,决策沉到 `DECISIONS.md` §3.30。
> **本批不加判定,查守卫** —— 理由见下面「为什么换方向」。
>
> **阶段 P0 仍未关闭。§14.3 的人工测试准入依旧不满足。**
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | **新门禁** `make audit-guards` / `tools/audit_source_guards.py`:反向断言不许吃切窄的源码 | ✅ 落码并跑过,按硬规则 3 在三处登记 |
> | 全树 10 处开着的窗口逐处收口(其中 2 处是 14-13 撞见的真假绿) | ✅ 10 → 0 |
> | `_helpers` 新增 `window()` / `braced_block()` / `only_line()` 三个取窗口的工具 | ✅ 落码并跑过 |
> | 守卫 `tests/pure/test_a45_batch14_14_source_windows.py` | ✅ **19 条,15 次变异全部验红** |
> | 真库用例 | **本批 0 条**。池子仍是 60 条。**前端一个字没动** |
>
> ### 为什么换方向(对前四批的反思)
>
> 14-12 与 14-13 的计划产物都是「判定落地 + 接线欠账」,而两批**真正值钱的
> 发现都是副产品**:四条假绿/钉错层的守卫,一条都不在计划里。这不是巧合 ——
> 判定有变异验证兜底(每批 20~34 条),**而守卫本身只有「它今天跑绿」这一条
> 证据,可假绿守卫的定义就是「它跑绿」。**
>
> 顺带一条自我批评:14-13 的接线**今天是个空转**(归属外键不存在,失败清单
> 恒为空)。理由成立,但连着四批(14-9 / 14-10 / 14-12 / 14-13)落的都是
> 「验完了、动不了真实行为」的判定,而真正的堵点一次都没挪动。
> **再往那个方向堆第五批,边际价值是递减的。**
>
> ### 判据为什么只管反向断言
>
> 全树按字符串定位切窗口的地方有 **87 处**,一刀切会把绝大多数正当用法一起
> 判红。而失效方向不一样:正向断言切窄了会变假、会红、有人看得见;
> **反向断言切窄了会变真、会绿、没有人看得见**。缩到反向那一侧之后只剩
> 10 处,零容忍做得到 —— 而一条有出路的门禁才不会被加白名单。
>
> ### 变异第一轮 13/15
>
> **H4** 第一轮绿:守卫样本里断言的东西在嵌套块**里面**,按「第一个 `}`」切
> 也照样包得住 ——「构造了一对输入 ≠ 构造了一对会分叉的输入」(batch14-9 的
> H1 同型,第二次)。**A6** 第一轮绿:窗口变量传染(`narrowed = body`)
> 根本没有守卫覆盖,而那是被门禁挡住的人最先会试的那一下。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **2266/2266**(跳过 7)、本批变异 15/15、锚点 249/249(11 份脚本)、
> 交付 13/13、导入 377、样例 5/5、**新门禁 445 个守卫全绿**、前端 syntax-check 84/84。
>
> **仍未执行**:真库 pytest、Alembic 升降级、Ruff / lint-imports 本体、
> 前端四条、Docker build。

> ## A45-batch14-13 §11 第一行:按作用域的失败归集(基线 14-11/14-12 合树,2026/08/06)
>
> 逐条说明见 `docs/MERGE-A45-BATCH14-13-SCOPE-OUTCOME.md`。
> §11「新增三行」到此三行齐:另外两行由 14-11 的 §11 批做完,
> 第三行它刻意避开(PARTIAL_SUCCESS 属 §4.6 状态机),归本条线。
>
> **阶段 P0 仍未关闭。§14.3 的人工测试准入依旧不满足。**
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `run_state.outcome_by_scope` / `scope_lost_everything` / `scope_of`:按作用域归集这一次 run 的成绩(零依赖) | ✅ 落码并跑过 |
> | 接进 `run_extraction` 的逐图循环,失败作用域与重试范围进 warning 日志 | ✅ 落码,**今天一定给出空清单**(归属外键不存在,接的是形状) |
> | §11「失败颜色明确列出」**欠一列** —— 且事后反推不出来 | ❌ 规格写成守卫,不在本批加迁移(见下) |
> | 守卫 `tests/pure/test_a45_batch14_13_scope_outcome.py` | ✅ **24 条,18 次变异全部验红** |
> | 真库用例 | **本批 0 条**。池子仍是 60 条。**前端一个字没动** |
>
> ### 事后反推不出来,所以它欠一列
>
> 「哪几张没留下证据行」推不出失败清单:`plan_evidence` 在模型只答了目标清单
> **之外**的字段时返回**空计划**,于是一张**扣了钱的成功调用**同样一条证据行
> 都没有 —— 和一张失败的图在库里逐字段相同,而它们一个不该重试、一个该重试。
>
> ### 为什么不在本批加迁移
>
> 三条线同时在同一个基线上开工,而**迁移链是单写者资源**(交付自检盯着
> 「单一 head」)。各自加一条 0037,合树时谁都改不动对方的 revision ——
> 这是并发的几条线里唯一一种合树的人无法在本地解决的冲突。
> 那一列属于 §4.6 那一批(五列 + 一条迁移),由它一次性带走。
>
> ### 变异第一轮 16/18,两条都值得记
>
> **F2**:「没有图的颜色不算失败」这条规则的载体 `total > 0` 写在循环里,
> 而循环里的 total **恒大于 0** —— 一段没有任何用例够得着的防御代码,
> 删掉不会有东西变红(与 batch14-9 的 S5 同型)。修法是**把判定挪到能穷举的
> 地方**:提成 `scope_lost_everything()` 之后逐组钉死。
> **S6**:变异脚本把一次**真红**误报成「不算数」—— 它的「导入期就炸了」检测
> 是在整段输出里找异常名字的子串,而 S6 的正确失败方式就是一条 AttributeError。
> 判据已改成结构性的。
>
> ### 顺带修掉两条自己人的假绿守卫
>
> 两条守卫都在用**字符串切**读 `WIRED_MODULES`。14-12 那条用的是定长窗口
> `[:200]`,本批往那个登记项里加一行之后窗口切在半路上,于是它的**反向**断言
> (「`idempotency_key` 不许被登记」)被残段喂成平凡真 —— **那条守卫在那一刻
> 悄悄不设防,并且照样显示 PASS。**两条都改成用 AST 读,并验过临时登记一个
> 不该登记的函数时它们真的变红。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **2247/2247**(跳过 7)、本批变异 18/18、**14-12 的 34/34 与 §11 批的
> 32/32 重跑仍全红**、锚点 234/234(10 份脚本)、交付 13/13、导入 374、
> 样例 5/5、前端 syntax-check 84/84。
>
> **仍未执行**:真库 pytest、Alembic 升降级、Ruff / lint-imports 本体
> (本批新增文件已按门禁批订正的口径人工对齐,**没有跑过本体**)、
> 前端四条(本批没动前端,但前三批动过)、Docker build。

> ## A45-batch14-12 识别 run 的终态、取消与幂等键(基线 batch14-10,2026/08/06)
>
> **原本也叫 batch14-11。**同一个号被三条线同时占用(门禁批、§11 两条新场景批、
> 本批),硬冲突只有一处 —— `tools/mutate_batch14_11.py` 两份内容不同。
> 按 §3.11 的老办法补号:先到的两批留在 14-11,本批改成 **14-12**,
> 守卫与变异脚本一并改名。理由记在 `DECISIONS.md` §3.28。
>
> 按 **PRD §4.6 / §9.2** 推进阶段 3 第二批里**不需要真库**的那一半。
> 逐条说明见 `docs/MERGE-A45-BATCH14-12-RUN-STATE.md`。
>
> **阶段 P0 仍未关闭。§14.3 的人工测试准入依旧不满足。**
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | `attributes/run_state.py`:终态派生 + 转移表 + 复用判定 + §9.2 幂等键(零依赖) | ✅ 落码并跑过 |
> | 终态派生**接线**:`ProductAttributeExtraction.status` 派生属性 + 出参新增 `status` | ✅ 落码并跑过 |
> | 全部失败的 run 补写 `error_code` —— 此前它和全部成功的 run 在库里长得一模一样 | ✅ 落码,**未在真库跑过** |
> | 合并证据的判据换成 `run_is_authoritative`(今天结论与旧判据逐条相同) | ✅ 落码并跑过 |
> | 前端删掉自己判三档那段(硬规则 4),改成读后端给的那一档 | ✅ 落码,**无 node_modules,未编译** |
> | §9.2 幂等键:部分唯一索引孪生、作用域归一、键组成 | ✅ 判定验完,**接不了线**(五个列都不存在) |
> | 守卫 `tests/pure/test_a45_batch14_12_run_state.py` | ✅ **37 条,34 次变异全部验红** |
> | 真库用例 | **本批 0 条**。池子仍是 60 条 |
>
> ### 修的是一个正在漏的东西
>
> 「这次识别算成功、部分成功还是失败」此前由**前端**回答
> (`AttributeTab.tsx` 拿两个计数判三档),而后端从来没有派生过这个值。
> 前端那三档还判漏了一种:循环在跑完之前停下时 `failed_count` 是 0,
> 那串三元运算落在「识别完成」上 —— **一次跑了一半的识别显示成功**。
>
> ### 本批最值钱的两条不在本批的守卫里
>
> 变异脚本只跑本批那一份套件,所以它**天生看不见「本批改动打破了别人的守卫」**。
> 两条既有 A2 守卫是跑全量时掉出来的:一条**钉错了层**(断言前端源码里出现
> `succeeded_count ?? 0) === 0`,也就是硬规则 4 禁止的那段代码 —— 判定搬到后端
> 之后它变红,而变红的原因是病治好了);另一条**此前是假绿**(断言源码里出现过
> `succeeded_count`,而换判据时留下的**注释**里正好写着这个词 —— 同一个坑的第五次)。
> 两条都已改成 AST 钉性质,并各自用手工变异验过咬得住。
>
> ### 三批合树之后重跑的门禁(不是本批单独的数)
>
> 纯逻辑 **2223/2223**(跳过 7)= batch14-10 的 2148 + 门禁批 1 + §11 批 37 + 本批 37。
> 交付 13/13、导入 372、样例 5/5、锚点 216/216(9 份脚本)、前端 syntax-check 84/84。
> 三份变异脚本重跑:本批 34/34、§11 批 32/32、batch14-9 的 16/16、batch14-10 的 23/23。
>
> **仍未执行**:`tsc` / ESLint / Vitest(三批都改了前端,**而本批新增的联合类型
> 正是编译期比较的依据**)、真库 pytest、Alembic 升降级、Docker build。
> Ruff 与 lint-imports 由门禁批在**它那台机器上**跑过,本批新增的文件**没有**
> 被那次执行覆盖 —— 已按它订正的口径(UP035 / I001 / E501)人工对齐,但没有跑过本体。

> ## A45-batch14-11 §11 两条新场景:确认队列口径 + AI 图伪装拦截(基线 batch14-10,2026/08/06)
>
> 按 **PRD v3.1 §11「新增三行」**推进,**与阶段 3 第二批(§4.6 异步化)零交集** ——
> 那一批由另一条线在做,本批完全没有碰 `ProductAttributeExtraction` 模型与迁移、
> `run_extraction` 的事务形状、Celery 任务、取消语义、`PARTIAL_SUCCESS`。
> 逐条说明见 `docs/MERGE-A45-BATCH14-11-QUEUE-AND-PROVENANCE.md`。
>
> **阶段 P0 仍未关闭**,`make p0-gate` 在这台机器上仍是 1 项通过 / 5 项未验证。
> **§14.3 的人工测试准入依旧不满足。**
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | §11「未校准字段大量涌入」:CANDIDATE 不再混进确认队列。新建零依赖 `attributes/queue_policy.py`(状态→去向穷举归档表),`workbench/service` 按它分桶 | ✅ 落码并跑过 |
> | 属性步新增阻断码 `ATTR_EVIDENCE_ONLY`;全 CANDIDATE 的商品状态从 NEEDS_CONFIRM(0.6 分)改判 IN_PROGRESS(0.3 分) | ✅ 落码并跑过 |
> | 新动作码 `FILL_ATTRIBUTES` + 批次异常类别 `ATTRIBUTE_NOT_CREDIBLE`(**`retryable=False`** —— 重试等于再花一次钱换回同一批不采信的证据) | ✅ 落码,前端四处镜像**只过了 syntax-check** |
> | §11「AI 候选文件被当作原始样品重新上传」/ AC-22:新建零依赖 `media/provenance_conflict.py`,接进 `ingest()` 与 `_fill_missing_role()` | ✅ 落码,**未在真库跑过** |
> | 溯源冲突取数改按 **SPU** 而不是去重键(差出来的那一块正是「A 色的图传给 B 色 SKU」) | ✅ 落码,**SQL 未在真库跑过** |
> | 冲突落 `QUARANTINED` + 共享理由文案,由既有 `release()` 人工放行 | ✅ 落码,**隔离态的下游没有走过一遍** |
> | 删掉死字段 `MaterialFacts.has_primary_image`(算了没人读,且读的是 `usable_roles` 而门禁读 `gate_roles`) | ✅ |
> | 守卫 `tests/pure/test_a45_batch14_11_queue_and_provenance.py` | ✅ **37 条,32 次变异全部验红** |
> | 真库用例 | **本批 0 条**。池子仍是 60 条 |
>
> ### 变异第一轮是 30/32,两条 GREEN 都是守卫自己写松了
>
> W2(把 `if product.spu` 换成 `if True`)与 W6(删掉「理由已存在就不覆盖」那一问)
> 第一轮全绿。两条都是同一型:**「出现了某个语法结构」不等于「那个结构问的是对的问题」**
> —— `if True` 照样是 `IfExp`;`shadow_from_product_asset` 本来就有 `not deduped`、
> 本来就要给 `quarantine_reason` 赋值。与 batch14 的 M10、batch13-3 的 M2 同型。
> 已改成断言打在三元表达式的 `test` 与 `If` 节点的条件本身上。
> **别把这一批读成一次干净的 32/32。**
>
> ### 这一批**没有**做的
>
> - §4.6 的异步化及其全部附属(五列 / Celery / cancel / 幂等键唯一约束 /
>   PARTIAL_SUCCESS / 按颜色重试)—— 另一条线在做。
> - 「有证据但不采信」的界面(属性页分组、一键人工填写入口)。无 node_modules。
> - 溯源冲突没有落审计:`ingest()` 手里没有 actor,今天只记 `logger.warning`。
> - 没有加索引:`(spu, sha256)` 走既有的 `ix_media_assets_sha256` 再过滤,
>   代价可忽略是**推理**不是实测。素材量上来后这里第一个要看。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **2185/2185**(跳过 7,基线 2148)、本批变异 32/32 全红、
> **batch14-10 的 23 条变异重跑仍 23/23 全红**、锚点 182/182(8 份脚本)、
> 交付 13/13、导入 369 个文件、样例 5/5、`lint_offline` 350 文件无发现、
> 前端 syntax-check 84/84。
>
> **仍未执行**:真库 pytest(含 `ingest()` 的新查询与并发)、Alembic 升降级、
> Ruff 全规则、lint-imports、前端 tsc / Vitest / build、Docker build。

> ## A45-batch14-11 首次在装齐依赖的机器上跑门禁(基线 batch14-10,2026/08/06)
>
> 不是新功能,是把此前四个批次"本机缺 pydantic/sqlalchemy"欠下的门禁真的跑了一遍
> (离线 wheelhouse 装齐后端全部依赖;本机仍无 PostgreSQL / Redis / node_modules / docker)。
>
> **阶段 P0 仍未关闭**,`make p0-gate` 在这台机器上仍是未关闭状态。
> **§14.3 的人工测试准入依旧不满足。**
>
> ### 修了什么
>
> | 项 | 状态 |
> |---|---|
> | **纯测试里藏着 4 条从未跑过、装齐依赖即红的用例。** `test_a45_batch14_stage3_extractor.py` 的台账/上限四条:batch14-7 给 `run_extraction` 接上 §5.1 白名单后判定会**复查** `status=READY`,而 `_FakeAsset` 没有 `status` 属性 —— 这四条恰好整文件躺在"7 跳过"里,于是 batch14-7 到 14-10 每一轮报的"纯逻辑 N/N 全绿"都带着它们。夹具补 `status="READY"`(还原 `usable_assets()` 只给 READY 行的生产前提,不是扩 mock) | ✅ 落码并**真的跑过**(两个运行器) |
> | 新守卫 `test_the_fake_asset_still_passes_the_evidence_whitelist`:白名单再收紧一个条件时,先红的是这一条并把话说全,而不是四条业务断言一起红成"素材全不合格" | ✅ |
> | Ruff **首次执行**,6 条:`scope_fingerprint.py` 的 UP035 + E501,三份 batch14-7/9/10 守卫的 I001(`_helpers` 导入排序;两个运行器各自保证 sys.path,重排安全,14-7 那行的旧注释一并订正)+ 一条 E501 | ✅ 清零 |
> | `lint-imports` **首次执行**:三条架构契约 3 kept / 0 broken | ✅ |
> | 真库用例文件里一处指错路的文档引用(`docs/HANDOVER.md` → 根目录 `HANDOVER.md`) | ✅ |
>
> ### 这次的教训:跳过的用例不算绿的,连"曾经绿过"都不算
>
> 那 4 条不是退化 —— 它们**从写下来那天起没有在接线后的代码上跑过一次**。
> 每一轮 STATUS 都如实写了"跳过 7(本机缺 pydantic/sqlalchemy)",但没有一处
> 写明跳过的**是哪 7 条、覆盖着哪些决定**;于是 §5.1 白名单接线(14-7)动了
> 它们守着的那条路径,没有任何东西变红。教训与"CI 里连不上库直接失败,不跳过"
> 是同一条:**下一台装得齐依赖的机器,开工第一件事是把上一台的 skip 清单跑完。**
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **2156/2156**(装齐依赖,0 跳过)/ 裸 python3 **2149/2149 + 7 跳过**、
> 全量 pytest **2190 passed / 207 skipped**(207 全是 requires_db)、
> Ruff 0 违规、lint-imports 3/3、交付 13/13、样例 5/5、导入 365、
> 锚点 150/150、前端 syntax-check 84/84。
> Alembic 离线 `--sql`:0001→0032 可完整生成(1257 行);0033 起是数据迁移,
> 离线模式按设计不支持 —— **这不等于升降级验过了**。
>
> **仍未执行**:真库 pytest 那 207 条(池子里累计的 60+ 条演练在内)、
> Alembic 真实升降级、前端 tsc / ESLint / Vitest / build(`npm ci --offline`
> 实测 ENOTCACHED,本机无网络无缓存)、Docker build、Redis / P0-6、
> 变异脚本全量重跑(锚点 150/150 已静态验证,不替代重跑)。
>
> ### 文档侧(本轮撞见,先记账)
>
> 本文的版本块此前停在 batch14-2,而 14-7 / 14-8 / 14-9 / 14-10 四批已落码 ——
> 各自只有 `docs/MERGE-A45-BATCH14-*.md`,没有 STATUS 块。**要查那四批做了什么,
> 看各自的 MERGE 文档**;本文档不补写二手转述。`HANDOVER.md` 同步落后
> (停在 batch14),本轮已重写。文档地图漏 PRD 一行,已补。

> ## A45-batch14-2 对 batch14 交付包的走读修复(基线 batch14 + 已合入 batch13-3,2026/08/05)
>
> 不是新功能,是对上一批的走读。四条(两条 P1),逐条见
> `docs/REVIEW-A45-BATCH14.md`;决策沉到 `DECISIONS.md` §3.26。
> batch13-3 的合并记录见 `docs/MERGE-A45-BATCH13-3.md`。
>
> **阶段 P0 仍未关闭**,`make p0-gate` 在这台机器上仍是 1 项通过 / 5 项未验证、
> 退出码非零。**§14.3 的人工测试准入依旧不满足。**
>
> ### 修了什么
>
> | 项 | 状态 |
> |---|---|
> | F1 `plan_evidence` 按字段名去重 —— 模型把一个字段答两遍,这一张图就投两票,而合并阈值 0.34 的推理**假设一图一票**;两条重复能把 3:1(通过)变成 3:2(CONFLICT) | ✅ 落码并**真的跑过**(纯判定,边界穷举) |
> | F2 `missing_reason` 上屏(前端补类型 + 原因→动作映射 + 分组渲染 + 拉逐图证据)—— batch13-3 / R4 在同一天的另一个批次上原样重演,而且更彻底:前端连类型都没有 | ✅ 落码,**无 node_modules,未编译** |
> | F3 单次识别上限的兜底抽成 `effective_ceiling()` —— 原来判定用 12、提示语用 0,那句话没有任何批量满足得了 | ✅ 落码并**真的跑过** |
> | F4 `passive_deletes` 的规矩改成全树不变式(遍历模型层双向匹配),不再点名单条关系 | ✅ 落码并**真的跑过** |
> | 顺带:`tools/mutate_batch14.py` 的 M33/M34 重新锚定 + 失锚分支的元组补齐(原来锚点过期时脚本自己 ValueError) | ✅ |
> | 守卫 `tests/pure/test_a45_batch14_2_fixes.py` | ✅ **14 条,16 次变异全部验红** |
> | 真库用例 | **本批 0 条**。四条全在纯层可判定,池子仍是 60 条 |
>
> ### 变异验证这次又抓到了自己
>
> N15(前端类型里删掉 `missing_reason`)第一轮 **GREEN**:守卫的正则没锚在
> 行首,变异把那行注释掉之后 `// missing_reason: ...` 照样匹配。
> **同一个陷阱在这个仓库里第三次出现**(batch13-3 的 M2 与 M11 是前两次)。
> 已改成整行锚定。前端另外三条(N10/N11/N12)是先造变异、再写断言的。
>
> ### F2 在修它的那次改动里复发了一次
>
> 新加的 `useQuery` 第一版没有失败分支,被既有门禁
> `test_every_query_handle_is_checked_for_failure` 咬住 —— 查询挂掉时界面
> 渲染成"模型这次没有对任何字段说没有值",**正是 F2 要修的那个病灶**换了个入口。
> 已补失败分支并收进守卫(N16)。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **2023/2023**(跳过 7,本机缺 pydantic/sqlalchemy)、本批变异 16/16 全红、
> **batch14 的 34 条变异重跑仍然 34/34 全红**、交付 13/13、导入 351 个文件、
> 样例 5/5、前端 syntax-check 84/84、`make p0-gate` 仍为 1 通过 / 5 未验证。
>
> **仍未执行**:`tsc` / ESLint / Vitest(本批改了 `AttributeTab.tsx` 与
> `api/attributes.ts`)、Ruff 全规则、lint-imports、Alembic 升降级、
> 池子里那 60 条真库用例、Docker build。

> ## A45-batch14 阶段 3 第一批:真实多模态抽取器(基线 batch13-2,2026/08/05)
>
> 按 **PRD v3.1 §13「阶段 3:真实多模态识别」**推进 —— §13 明写它**可与阶段 1
> 并行**,而阶段 1 第二批的每一步都要在真库上验过才能走下一步(batch13-2 已定),
> 这台机器仍然没有 PostgreSQL。逐条说明见 `HANDOVER.md`。
>
> **阶段 P0 仍未关闭**,`make p0-gate` 在这台机器上仍是 1 项通过 / 5 项未验证。
> **§14.3 的人工测试准入依旧不满足。**
>
> ### 做了什么
>
> | 项 | 状态 |
> |---|---|
> | 任务 7:`extractors/vision.py` 真实抽取器(双 API 形状,复用 llm/ 传输层与图片层) | ✅ 落码,**从未连过真模型** |
> | §4.5 `missing_reason`:落 evidence 不落 value,两条无值通道归成一条写入路径 | ✅ 落码并跑过 |
> | §6.3 编造过滤:判据从「注册表里有没有」改成「**这次问了什么**」,并计数落列 | ✅ 落码并跑过 |
> | 付费识别调用进花费台账(逐次、成败一视同仁、不传 billing_key) | ✅ 落码,**未在真库跑过** |
> | 付费抽取器单次调用上限(拒绝发生在第一次调用**之前**) | ✅ 落码并跑过 |
> | `describe_extractors()` 的 `configured` / `is_simulator` 改问实现自己,名单删除 | ✅ 落码并跑过 |
> | `llm/endpoint_trust.py`:「要不要 Key」与「账记谁头上」抽出共享,评分器改委托 | ✅ 落码并跑过 |
> | 迁移 `0036`(两列),设置页「属性识别」分组,`.env.example` 一组新键 | ✅ 落码,**迁移未执行** |
> | 守卫 `tests/pure/test_a45_batch14_stage3_extractor.py` | ✅ **55 条,34 次变异全部验红** |
> | 真库用例 `tests/test_a45_batch14_extraction_db.py` | **已写,一次都没跑过**(11 条) |
>
> ### 判定搬进零依赖模块,是变异验证逼出来的
>
> 第一轮变异跑出「32/32 全红」——好得可疑。给验证脚本加上「**到底是哪条用例红的**」
> 这一列之后,四个问题当场暴露,全是 batch13 点名过的形状:
>
> | | 一句话 |
> |---|---|
> | 验证脚本自己错了 | 沙箱只复制 `backend/`,而一条守卫要读项目根的 `.env.example` —— 它在**每一次**变异里都红,把三条真绿伪装成红 |
> | M9 抓不住 | 上限守卫按 AST 找常量 `EXTRACTOR_MAX_IMAGES_PER_RUN`,而变异后的 `..._MOVED` 仍含这个子串 |
> | M10 抓不住 | 断言「记流水出现在 except 里」,而 `if False: 记流水(...)` 照样满足它(与 batch13 的 M7 同型) |
> | M30 抓不住 | 按字符串切「两个列名之间那段」查 `server_default`,而**迁移的文档字符串里恰好按同样顺序提到这两个名字** —— 它比较的是一段文档(与 M15 同型) |
>
> 修法不是把断言改严,是**把判定搬到能穷举的地方**:新增零依赖的
> `extractors/evidence_plan.py`(写入判定)与 `extractors/call_budget.py`(上限边界),
> 服务层只做物化与翻译。理由与 `workflows/publish_view.py` 顶部那条相同。
> 副产品是这两组规则现在在**没有 sqlalchemy 的机器上也真的跑得起来**,
> 不再落进「已写、未执行」的池子。
>
> ### 这一批**没有**做的
>
> - **§4.6 的异步化**(status / spu_id / idempotency_key / input_fingerprint /
>   requested_scope 五列 + Celery 任务 + cancel):要和幂等键唯一约束、取消语义
>   一起落,拆开落一半列只会造出一批永远 NULL 的「看起来支持异步」的字段。
>   迁移 0036 因此只加两列。
> - **§5.1 白名单查询助手**:`media.evidence_assets_for(spu_id, scope)` 要等阶段 2
>   的 `evidence_class` 与归属外键,今天 `usable_assets` 还按 `product_id` 查。
>   **所以「识别输入不含任何 AI 图」这条验收现在不成立** —— 见「已知限制」。
> - **前端**:`frontend/src` 一个字没动。识别详情页展示 `missing_reason` 与
>   编造计数是阶段 3 的界面部分,无 node_modules,改了验不了。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **2001/2001**(跳过 7 条,本机缺 pydantic/sqlalchemy)、交付 13/13、
> 导入 347 个文件、样例 5/5、`make p0-gate` 仍为 1 通过 / 5 未验证。
> 变异验证 **34/34**,且每条都由它自己那条守卫报出来(不是靠炸掉模块)。
>
> **仍未执行**:真库 pytest(含本批新加的 11 条)、Alembic 升降级(链现在到
> `0036`)、Ruff 全规则、lint-imports、前端全部门禁、Docker build。
>
> **最要紧的一条没验**:真实抽取器一次都没连过真模型。守卫验的是
> 「请求长什么样、响应怎么解析」,验不到「这个端点真的会这么回答」。

> ## A45-batch13-3 对 batch13-2 交付包的走读修复(基线 batch13-2,2026/08/05)
>
> 不是新功能,是对上一批的走读。六条(三条 P1),逐条见
> `docs/REVIEW-A45-BATCH13-2.md`;决策沉到 `DECISIONS.md` §3.25。
>
> **阶段 P0 仍未关闭**,`make p0-gate` 在这台机器上仍是 1 项通过 / 5 项未验证、
> 退出码非零。**§14.3 的人工测试准入依旧不满足。**
>
> ### 修了什么
>
> | 项 | 状态 |
> |---|---|
> | R1 `ColorVariant.skus` 补 `passive_deletes="all"` —— ORM 删颜色原来会先把九行外键置 NULL,RESTRICT 根本响不了,「删颜色被拦住」这句验收在 `session.delete()` 路径上是假的 | ✅ 落码,**行为未在真库跑过** |
> | R2 `update_product` 改受众同步 `spus.audience` 权威 + 拒绝 SPU 管理的行清空受众 + 兄弟行传播补外键命中(§4.4) | ✅ 落码,**未在真库跑过** |
> | R3 `spu_id` / `color_variant_id` 补 `info={"identity": True}` —— `_is_identity_column` 那份"唯一名单"漏了最强的两列身份 | ✅ 落码并**真的跑过**(纯守卫) |
> | R4 `identity_shadowed` 上屏(ImageSetTab 渲染 + error 升级)—— 被指定的看门狗此前只存在于类型联合里 | ✅ 落码,**无 node_modules,未编译** |
> | R5 目录名回落 `working_name or variant_code` —— 只填编码建档的颜色原来在目录里是一串 UUID | ✅ 落码,**未在真库跑过** |
> | R6 建档拼 `name` 截到 `limit_for("name")` —— 原来 255+1+16+1+8=281 超列宽,flush 抛 DataError,except IntegrityError 接不住,裸 500 | ✅ 落码并**真的跑过**(纯守卫 + 可达性断言) |
> | 守卫 `tests/pure/test_a45_batch13_3_fixes.py` | ✅ **9 条,12 次变异全部验红** |
> | 真库用例 `tests/test_a45_batch13_3_db.py` | **7 条已写,一次都没跑过**(合入 batch14 后池子累计 **60** 条:12-4 的 6 + 12-5 的 7 + 12-7 的 11 + 13 的 18〈含 13-2 追加〉+ 本批 7 + batch14 的 11;原文/`REVIEW-A45-BATCH13-2.md` 记的 37 是分支内的错算，已按文件实际函数数订正) |
>
> ### 变异验证这次抓到了自己
>
> M11(前端渲染块死代码化)第一轮 **GREEN**:守卫的正则没锚在 JSX 表达式的
> 开括号上,`{false && identityShadowed...` 照样匹配 —— 渲染死了、守卫还绿,
> batch13 M7 的原样重演,发生在**本批次自己的守卫**上。已加锚修正,
> 教训写进守卫注释。M2 则被脚本的"变异目标不唯一就拒改"拦下(注释里
> 也含那串字)—— 两条都是"验守卫,不是验代码"的直接收益。
>
> ### 这一批**没有**做的
>
> - `spu` / `sku` 字符串列仍只靠 schema 挡改(A44 存量口径);收进身份名单
>   等阶段 1 第二批与 `variant_key` 退役一起定。
> - 混合期(老路径给已建档 SPU 补行)的口径差异是 §13 已声明的边界,
>   R2 的并集传播只保证受众不因此分叉,不试图弥合两套行数。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **1955/1955**(跳过 2,本机缺 pydantic/sqlalchemy)、交付 13/13、
> 导入 342 个文件、样例 5/5、`make p0-gate` 仍为 1 通过 / 5 未验证。
>
> **仍未执行**:真库 pytest(含本批 7 条 —— **R1 那条尤其要跑**,它钉的是
> 一句已随两个交付包发出去的假验收)、Alembic 升降级、Ruff 全规则、
> lint-imports、前端全部门禁(本批改了 `ImageSetTab.tsx`)、Docker build。

> ## A45-batch13-2 阶段 1 第一批的走读修复(基线 batch13,2026/08/04)
>
> 不是新功能,是对上一批的走读。**F1 是唯一一条已经随交付包发出去的缺陷。**
>
> **阶段 P0 仍未关闭**,`make p0-gate` 在这台机器上仍是 1 项通过 / 5 项未验证、
> 退出码非零。**§14.3 的人工测试准入依旧不满足。**
>
> ### 修了什么
>
> | 项 | 状态 |
> |---|---|
> | F1 变体身份收成三级(外键 → `variant_key` → 种子),唯一定义在 `identity_of()` | ✅ 落码并**真的跑过** |
> | F1 显示名三级回落(当前颜色 → 变体名 → 身份),建档后界面不打 UUID | ✅ 落码并**真的跑过** |
> | F1 `drift()` 两条判据跟着改,新增 `identity_shadowed` 测量回填那颗雷 | ✅ 落码并**真的跑过** |
> | F2 `GET /spus` 的 `sku_count` 改成分组查询;`_to_out` **去掉默认值** | ✅ 落码,**未在真库跑过** |
> | F3 `AppError.to_payload()` 发 `fields`(抄 422 处理器已有的形状);`_translate` 传下去 | ✅ 落码并**真的跑过** |
> | F4 事务边界注释改对,并补一条"路由必须提交"的守卫 | ✅ 落码,**未在真库跑过** |
> | F5 `list_spus` / `_siblings` 预取,消掉两处 N+1 | ✅ 落码,**未在真库跑过** |
> | 守卫 `tests/pure/test_a45_batch13_2_fixes.py` | ✅ **26 条,20 次变异全部验红** |
> | 真库用例:`test_a45_batch13_spu_db.py` 末尾加 6 条读取侧断言 | **已写,一次都没跑过**(本文件共 18 条) |
> | 前端 `api/imageSets.ts` 的 drift 联合类型与说明跟着改 | ✅ 类型与注释,**无 node_modules,未编译** |
>
> ### 为什么 F1 能带着 42 条守卫发出去
>
> batch13 的守卫里有 `test_three_colors_and_three_sizes_make_nine_skus`,但它
> 断言的是 `sku_matrix.expand()` —— 展开层。验收标准那句话是「可构造**三颜色**
> 九 SKU 的 SPU」,而"三颜色"这半句在读取侧一条断言都没有。上一批 STATUS 里
> 那句「守卫挡得住『有人把决定删掉』,挡不住『决定本身错了』」,F1 是它的
> 第一个实例 —— 补充一句:也挡不住**决定只落了一半**。
>
> 因此这一批的守卫全部从读取方下断言:`ids_of` 看见几个变体、目录里显示什么
> 名字、诊断报不报。新加的 6 条真库用例同理。
>
> ### 阶段 1 下一步的顺序:按 `DECISIONS.md` §3.22 原文,不需要重新拍板
>
> `HANDOVER.md` 记的「owner_id 切 UUID → 老建档路径切到新表」**依赖是反的**:
> 先切 owner_id,老路径建的行会在 `owner_for()` 拿到空坐标,而 `apply_evidence`
> 捕获之后是 `warning + continue` —— 识别侧静默跳过颜色字段。
>
> 但 §3.22 原文写的就是「前提是 **products 先带上 `color_variant_id`**」,说的是
> **行带上值**,交接把它转述成了"列存在"。正确顺序即原文,详见 §3.23。
>
> **回填那一步不是数据迁移,是身份变更**:必须和属性 owner_id、图片标签的改写
> 在同一个动作里做,否则 §3.17 那个 bug 会在全库同时引爆。在那之前由
> `drift().identity_shadowed` 盯着。
>
> ### 这一批**没有**做的
>
> - **老建档路径切到新表 / 两个外键收 NOT NULL**:要动
>   `product_service.create_product` 和 CSV 导入,而这台机器没有 PostgreSQL ——
>   按「每一步都要在真库上验过再走下一步」,在这里只能写不能验,又要进
>   "已写、未执行"的池子。留给有真库的那台机器。
> - **前端建档 UI**:`frontend/src` 只改了 `api/imageSets.ts` 的类型与注释
>   (跟着后端 drift 改,不改就是刚抓完 F3 又造一个同类)。无 node_modules,
>   `tsc` 一次都没跑过。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **1946/1946**(跳过 2 条,本机缺 pydantic/sqlalchemy)、交付 13/13、
> 导入 339 个文件、样例 5/5、`make p0-gate` 仍为 1 通过 / 5 未验证。
>
> **仍未执行**:真库 pytest(含本批新加的 6 条)、Alembic 升降级、Ruff 全规则、
> lint-imports、前端全部门禁、Docker build。
>
> **最要紧的一条仍然没验**:建档链路一次都没有连过真库。这一批把 F1 的判定
> 收敛了,但"外键在 flush 那一刻真的落进去了"这件事,纯守卫看不到。

> ## A45-batch13 阶段 1 第一批:身份骨架(基线 batch12-7,2026/08/04)
>
> 按 **PRD v3.1 §13「阶段 1:身份规范化」**推进。逐条说明见 `HANDOVER.md`。
>
> **阶段 P0 仍未关闭** —— 本批次没有改变这一点,也没有资格改变:P0 剩下的
> 五项全都要一台有 PostgreSQL / Redis / node_modules / docker 的机器。
> `make p0-gate` 在这台机器上仍然是 1 项通过 / 5 项未验证、退出码非零。
> **§14.3 的人工测试准入依旧不满足。**
>
> ### 这一批交付了什么
>
> | 项 | 状态 |
> |---|---|
> | `spus` / `color_variants` 两张表 + 迁移 0035 | ✅ 落码,**未在真库跑过** |
> | `products` 增 6 列(两个外键 + barcode/price/cost/inventory) | ✅ 落码,外键**这一版可空** |
> | 尺码模板 + SKU 展开规则(`listings/sku_matrix`,零依赖) | ✅ 落码并**真的跑过** |
> | 建档链路:一次请求落 SPU + 颜色 + SKU 行(同一个事务) | ✅ 落码,**未在真库跑过** |
> | 建档接口 `POST /spus` / `GET /spus/{id}` / `GET /spus/size-templates` | ✅ 已接线进 `api/router` |
> | 守卫 `tests/pure/test_a45_batch13_stage1_identity.py` | ✅ **42 条,16 次变异全部验红** |
> | 真库用例 `tests/test_a45_batch13_spu_db.py` | **12 条已写,一次都没跑过** |
>
> ### 这一批**没有**做的(阶段 1 未完成)
>
> - **属性 owner_id 切 UUID、命名空间 hack 与 `variant_key` 退役**:顺序上排在
>   本批次之后 —— owner_id 要用 `color_variants.id`,就得先有地方拿到它,
>   而那正是本批次刚建好的东西(理由记在 `docs/DECISIONS.md` §3.22)。
>   本批次只保证**新代码不再长出对 `variant_key` 的依赖**(守卫按 AST 判,
>   注释里提它不算)。
> - **两个外键收成 NOT NULL**:老建档路径(`product_service.create_product`、
>   CSV 导入)还不知道这两张表,立刻收紧会让它们在 flush 阶段整体失败。
>   收紧的前提是三件事同时成立:老路径切过来、真库用例跑绿一次、
>   `products` 里没有外键为空的行。
> - **`products.audience` / `products.spu` 退役**:两列现在是 SPU 的反规范化副本,
>   由建档服务同步写。十来个域按它们取数,一次改完既做不完也验不了。
> - **前端**:`frontend/src` 一个字没动。三步建档 UI 与尺码模板下拉是阶段 1
>   的交付项,但这台机器上没有 node_modules,改了也验不了(硬规则:
>   `make check-offline` 跑绿 ≠ 全都过了)。后端已经把模板下拉项做成接口
>   (`GET /spus/size-templates`),前端不许自己内置一份。
>
> ### 离线门禁(改完之后跑的)
>
> 纯逻辑 **1920/1920**(跳过 2 条,本机缺 pydantic/sqlalchemy)、交付 13/13、
> 导入 339 个文件、样例 5/5、`make p0-gate` 仍为 1 通过 / 5 未验证。
>
> **仍未执行**:真库 pytest(含本批 12 条)、Alembic 升降级(链现在到 `0035`)、
> Ruff 全规则、lint-imports、前端全部门禁、Docker build。
>
> **最要紧的一条没验**:建档链路一次都没有连过真库。守卫是 AST 与纯逻辑断言,
> 它们挡得住「有人把决定删掉」,**挡不住「决定本身错了」**。要验只有一条路:
> 起真库,`POST /spus` 建一个三颜色的 SPU,确认落九行、每行两个外键都不为空、
> 重复编码报 409、删颜色被 RESTRICT 拦住。

> ## A45-batch12-7 阶段 P0(基线 batch12-6,2026/08/04)
>
> 按 **PRD v3.1.1 §13「阶段 P0:基础设施门禁」**推进。逐条说明见 `HANDOVER.md`。
>
> | P0 项 | 状态 |
> |---|---|
> | 1 真库 pytest 全量(12-3/12-4/12-5) | **未验证** —— 本机无 PostgreSQL |
> | 2 Alembic 升降级(0001→0034→回退) | **未验证** —— 本机无 alembic、无库 |
> | 3 前端四条 + Docker build | **未验证** —— 无 node_modules、无 docker |
> | 4 关闭 R-04 与 R-05 | ✅ **已关闭**,守卫 16 条,14 次变异全部验红 |
> | 5 重复扣费残窗真库演练 | 演练用例 **11 条已写,未执行** |
> | 6 租约 fencing 与「过期但 worker 还活着」 | 用例已在树上(10 + 7 条),**未重跑** |
>
> **这份表现在有一条命令能重跑:`make p0-gate`。** 它逐项跑,跑不动的点名说缺
> 什么前提,退出码非零 = P0 未关闭。做它的理由是 §14.3 把「P0 全部关闭」列为
> 人工测试准入,而在此之前「关没关闭」的唯一依据是本文档里的几句话 ——
> 而这类句子过期不会有任何东西报错(本文第五节为这件事立过规矩)。
> 它**不在 CI 里**,理由见 `backend/tools/p0_gate.py` 顶部:它跨 job 边界,
> 放进任何一个 job 都会永远报「未验证」,而恒红的门禁两周内会被加 `|| true`。
>
> ### R-04 / R-05 修了什么
>
> - **R-04**:同一个 `build_outputs`,两条调用路径,只有自动流水线那条做了
>   `source_asset_problem()` 前置检查。人工审核这条落 `INTERNAL_ERROR` ——
>   `retry_task` 眼里和别的崩溃没区别,续跑判定照样成立,于是点重试又读同一个
>   坏文件;终态换图那条更糟,抛出的话写着「请稍后重试」,而文件已经没了。
>   修法是两条路径**共用同一次检查结论**(新增 `_readable_source_problem()`,
>   存储层不通仍按可重试处理):非终态直接落 `FAILED` + 专用码(不经过
>   FORMATTING,免得界面上留一条假的「正在出图」),终态路径点名根因并给出
>   真正的退路,且排在终态白名单**之后** —— 被驳回的任务换哪张都不该上线。
> - **R-05**:`can_retry()` 问的是「QUEUED 在不在出边里」,满足它的状态有两个
>   (`FAILED` 与 **`CREATED`**),而三条续跑边只从 FAILED 出发。修法是让续跑
>   目标先过一次转移表(`sm.can_transition`),到不了就退回重生 ——
>   **不是**把 CREATED 从 `can_retry()` 里摘掉(`CREATED -> QUEUED` 合法且正确)。
>
> 顺带修掉两处:`_rebuild_outputs_for_finished_task` 里重复的终态白名单检查
> (新守卫当场抓住的)、`make help` 的 grep 字符类不含数字导致 `p0-gate` 这类
> 带编号的目标在帮助里**看不见**。
>
> ### 一条与 PRD 对不上的口径(以代码为准)
>
> PRD §0.4 与 §13-P0 末尾把「批次条目租约」列为独立待办,依据是
> 「`_apply_outcome` 不校验持有者、`reap_expired_leases` 不问 owner」。
> **前半句已经过期**:A43 / BLOCK-02 之后落库走 `apply_outcome(owner=, token=)`,
> 不带校验的那个函数是零调用点的废弃函数;双 session 用例也已在树上(10 条)。
> 后半句仍然成立且是刻意的。已同步订正「已知限制」表里那一行。
>
> **离线门禁**:纯逻辑 1878/1878(跳过 2 条,本机缺 pydantic/sqlalchemy)、
> 交付 13/13、导入 332、样例 5/5、前端语法 84/84。
> **仍未执行**:真库 pytest、Ruff 全规则、lint-imports、Alembic 升降级、
> 前端 typecheck/lint/Vitest/build/Playwright、Docker build。
>
> 这一版**仍然不是**正式人工测试基线:`make p0-gate` 报 5 项未验证。

> ## A45-batch12-4 自审(2026/08/04)
>
> 逐条说明见 **`docs/REVIEW-A45-BATCH12-4-SELFCHECK.md`**。
> 复核 batch12-4 那五处修改本身:**修法都成立**,但发现三条问题并已修:
>
> | | 一句话 |
> |---|---|
> | **高** | REG-03 的两条真库用例把故障注入在 `_load_bytes` 上,而那个异常会被 `_persist_candidates()` 逐张的 `try` 吞掉 —— 保存点不会回滚,用例测不到 REG-03。注入点改到影子写 |
> | **中** | 续跑成功之后 attempt 仍留着上一遍的 `error_code` / `error_message` / `regeneration_reason`,界面上是一条「成功但写着全部下载失败」的记录。`_finish_attempt()` 成功时清空 |
> | **低** | 续跑拿回来的候选比上一遍少时,尾巴上那几行以 `DOWNLOAD_FAILED` 滞留。新增 `_drop_shrunk_tail()`,只删没成功的行 |
>
> **本轮没有迁移,没有碰 `frontend/`。** 离线门禁:纯逻辑 1843/1845
> (2 条为本机缺 pydantic)、交付 13/13、导入 326、样例 5/5。
> 3 条新守卫逐条换回旧版验红。
>
> **仍未关闭**:真库测试(含本轮修好注入点的那两条)一次都没跑过,
> batch12-3 不能关闭,不得冻结正式人工测试版本。

> ## A45-batch12 增补(2026/08/04)
>
> 逐条说明见 **`docs/REVIEW-A45-BATCH12.md`**。本批**没有迁移**,
> 改动限于 5 个源文件 + 1 个运维脚本 + 1 份守卫 + 2 份文档。
>
> | 修复 | 一句话 |
> |---|---|
> | **R-01** | 抽检换选候选图撞终态封闭 → 409 且审核决策整笔回滚。改为「只重出图、不动状态」,**没有放松终态封闭** |
> | **R-02** | 抽检项点「重新生成」→ 同一形状的 409。改为按 blocking 分档的可读拒绝 |
> | **R-06** | 批量付费动作在 worker 猝死后**自动**重新付费,一路跑到条目重试上限(最多 3 次)。`batch_action_receipts.executions` 从「事后信号」升级成闸门,超过 `MAX_BILLED_EXECUTIONS=2` 落 `BILLED_RESULT_UNKNOWN`(needs_human),解除通道见 `tools/resolve_billed_unknown.py` |
>
> 顺带修掉一处**本批自己造出来的**坑:R-01 修好之后抽检换图真的会替换
> 网站上的图,而 `ReviewDetailPage` 的横幅写着「不会回滚已通过的图」——
> 修复前那条路径是 409,所以那句话碰巧是对的。已改成按是否换图分档。
>
> **离线门禁**:纯逻辑 1776/1778(2 条为本机缺 pydantic/sqlalchemy)、
> 交付 13/13、导入 322、样例 5/5、前端语法 83/83。20 条守卫,14 次变异全部验红
> (变异脚本必须带 `PYTHONDONTWRITEBYTECODE=1`,理由见 BATCH12 第八节)。
>
> **仍未执行**(与 batch11 完全相同,本批一条都没能补上):真库 pytest、
> Ruff 全规则、lint-imports、Alembic 升降级、
> 前端 typecheck/lint/Vitest/build/Playwright、Docker build。
>
> **遗留未修**:R-04(候选图文件丢失后重试恒失败,错误码不指向根因)、
> R-05(`retry_task` 在 CREATED 下的非法边,构造条件苛刻)、
> 属性识别对每张 AI 候选图都发付费调用而其证据一票投不上(纯成本问题,
> 要收敛得先落 `media_assets` 生成溯源迁移)。
>
> 这一版**仍然不是**正式人工测试基线,理由与下一节同一条:上面那串
> 「仍未执行」是准入的必要条件,不是可选项。


> ## A45 版本状态(2026/08/03)
>
> 本轮实施 **PRD v2(男女泳衣版)**:受众(WOMEN/MEN/UNISEX)提升为与品类
> 正交的一等业务维度。设计决策见 **`docs/DECISIONS.md` §3.21**。
>
> | PRD 阶段 | 状态 |
> |---|---|
> | 阶段 0 `CATEGORY_ID` 硬编码缺陷 | **已修**。草稿类目从商品派生,两条 AST 扫描测试防回潮 |
> | 阶段 1 领域与受众基础(枚举/注册表/迁移 0030/规则包拆分) | 已完成 |
> | 阶段 2 后端护栏(§10.5 筛选、§12.4 阻断、§11.3 授权、§17 断言) | 已完成,§17 第 2 条除外(见下) |
> | 阶段 3 前端受众可见性 | **部分完成**(batch10 补齐十条中的九条;剩一条见已知缺口) |
> | 阶段 4 男装评分校准 | **仅脚手架**。提示词与键已就位,校准未做 —— 需要男装真实商品图 |
>
> **batch10 补的是"后端护栏都在、前端把它们绕开了"这一类**(《前端审视结果》十条)。
> 上一版这一行写的是"已完成(受众标签、体型联动、review_focus 下发)" ——
> 那三项本身没做错,问题是它们恰好绕开了受众改造在前端最要紧的那几处:
>
> - `age_verified` 表单里没有控件,提交恒为 false → **每一个界面建出来的模特
>   都永久挂着"年龄未确认"**。一个 100% 命中的警示等于没有警示;
> - §11 授权字段组只能在创建那一刻写、且创建表单一个都不发,也没有 PATCH
>   → `license_status` 永远停在 UNVERIFIED,而 `assert_usable` 对非 LICENSED
>   提前 return:**那道授权闸口在真人操作的路径上一次都不会触发**;
> - `forProductAudience`(§10.5 硬约束筛选)是死代码,全仓唯一调用点没传受众;
> - `Product` 类型漏了 audience → 界面上无法给商品设置或修改受众,只能靠 CSV;
> - 受众与 `review_focus` 只出现在 `FlowHeader`,而它只被商品详情页使用 ——
>   逐件快审 / 单图评分详情 / 审核队列出现次数都是 0。**「男装该看腰头和裤长」
>   这件事,在实际审图的那块屏幕上不显示**;
> - §8.2「待确认受众」没有动作码,橙色"待确认"标记是个死胡同;
> - 批量导出让运营"按受众分成两个批次",而界面没有受众筛选可用(§9.5 违规)。
>
> 守卫在 `tests/pure/test_a45_batch10_fixes.py`(26 条,多为跨语言),
> 均做过变异验证。上一轮那条"至少一个组件读了 review_focus"的断言挡得住
> "完全没接线",挡不住"接在了不是审阅页的地方" —— 新守卫按页点名。
>
> **交付后自审又修了四处**(前两处是本轮改动自己造成的):
> - `CONFIRM_AUDIENCE` 指向属性标签页,而属性页当时没有受众控件
>   (`ProductFormModal` 只挂在 `/products` 那两页)—— **那只是把死胡同
>   挪了个位置**。补了 `AudienceConfirmCard`,按 §9.4 受众与品类同屏确认;
> - 授权日期走 `model_dump(mode="json")` 到达服务层时是 ISO 字符串,
>   直接进 `DateTime` 列在 SQLite 上 flush 就 TypeError,而
>   `assert_usable` 里 `expires <= now` 遇到 str 或 aware 值同样抛 ——
>   **崩在授权闸口上,而那正是本轮要让它开始生效的东西**。这个坑本来是
>   潜伏的(界面上没有日期输入框),本轮加上输入框就活了,所以归一化
>   (`_naive_utc`)与输入框同一轮落地,create / update 两条路都过;
> - 受众筛选拼错值原本"匹配不上返回空",界面上是「这个受众一件商品都
>   没有」—— 一句错得很有说服力的话。改成抛错,与 `/model-templates`
>   那条一致;`flow` 缺席时同样抛,不静默把全量当筛选结果;
> - `_apply_audience` 进了审核队列的逐行路径,而 `generic.field_spec`
>   每次都重读并校验 YAML(实测约 9ms/次)—— 一屏 20 行 180ms,
>   而屏上最多三种规则包。按规则包键加了请求内缓存。
>
> **进入人工测试前的复核又修了三处 + 文档一处**(守卫追加进
> `test_a45_batch10_fixes.py` 第 8 节,三条均做过变异验证):
> - 模特模板**创建**时空白授权日期以 `''` 闯 `datetime | None`,pydantic 直接
>   422 —— 不填日期(新模板的默认状态)就建不了任何模板。归一在路由层
>   (`_blank_to_none`),curl -F 一类客户端同样受益;PATCH 走 JSON null 不受影响;
> - `ProductFormModal` 清空已确认受众:undefined 被 JSON.stringify 丢键,
>   危险弹窗确认后接口 200 而受众未变。归一成显式 null(后端语义本就是清空);
> - `ProductFormModal` 换受众不清理越界品类:收窄只做了 options,
>   已选中的 BIKINI_SET 会跟着 MEN 一起提交(后端按枚举全集收,不拦)。
>   现与 `AudienceConfirmCard` 同规则联动清空;
> - `LOCAL_MANUAL_TEST.md` 的迁移 head 三处 0029 → 0030(本批迁移没同步文档)。
>
> **已知缺口(不隐身)**:
> - §17 第 2 条(图片模特受众核对)**只有判定函数,未接线** ——
>   `media_assets` 缺生成溯源列,需下一次迁移补 `generation_task_id`。
>   目前由 §12.4 生成前硬阻断兜住。
> - **生成时"不指定模特"仍绕过受众与授权检查**(审视第 10 条,本轮未关闭)。
>   `generation_service._assert_assets_are_usable` 走 `MODEL_REFERENCE` 素材
>   那条分支直接 return。**根因与上一条是同一个**:一张 MODEL_REFERENCE 素材
>   身上没有 `age_verified`、没有 `license_status`、也没有受众,因为
>   `media_assets` 没有那组列。真要关闭它得先落那次迁移,不是在生成服务里
>   加一个 if。界面这一半已经改了:下拉不再把"不指定"说成等价选项,
>   而是明说它跳过 §11 的检查。
> - **§11 授权字段的写入权限没有收归管理员。** PATCH 第一版挂了
>   `require_admin`,已改回 `require_operator` —— 因为 `create` 是 operator
>   且同样接收全部十二个字段(含 `age_verified` / `license_status`),
>   只锁 PATCH 那道门能被"重建一条"绕开,而一道能被日常动作绕过的闸
>   比没有闸更糟。要收归就得两个入口一起收,那会动到现有素材上传动线,
>   属于单独一次改动。
> - 男装分档阈值未校准,男装任务的自动通过应保持关闭,由 MVP 全量人工审阅兜住。
> - 样例数据仍是 10 件女装。男装样例需要真实商品图,不伪造。
>
> **受众未确认是 NEEDS_CONFIRM,不是 BLOCKING**(batch10 的一处取舍)。
> 迁移 0030 不回填受众,存量商品全是 NULL,而 `workbench/audience_rules`
> 的兼容矩阵为「NULL 受众 + 无前缀规则包」留了唯一一条缝:放行 + 警告。
> 列表页若无条件抢先要求确认受众,那批商品就再也走不到导出 ——
> 而导出闸恰恰说它们可以过。所以 `CONFIRM_AUDIENCE` 只在属性步尚未完成时
> 抢先,已走完属性的存量商品不被拉回去。两处的宽严必须一致。
>
> **第二轮复核又发现三处**:UNISEX 规则包缺失(中性款商品打不开工作台)、
> 授权字段 10/12 只写不读、后端下发的受众三件套前端无人消费。
> 新增守卫均做过变异验证(把接线拿掉,测试确实变红)。
>
> **第一轮复核发现并修掉的四处**(声明写了但没接线 / 一处入口改了另一处没跟上):
> 同 SPU 受众冲突静默放行、PATCH 受众不传播、`enabled_hard_fail_codes` 无人读取、
> `response_format_name` 生产调用没传受众。详见 DECISIONS §3.21 后三节。
>
> **离线门禁**:纯逻辑 1735/1738(3 条为本机缺 pydantic/sqlalchemy)、
> 交付 13/13、导入 305、样例 5/5、lint 299。
> 女装评分提示词相对 a44 基线**字节级不变**(已用脚本核对)。
> **仍未执行**:真库 pytest、Ruff 全规则、lint-imports、Alembic 升降级、
> 前端 typecheck/lint/Vitest/build/Playwright、Docker build。
>
> 这一版**不是**正式人工测试基线。


> ## A43 版本状态(2026/08/02)
>
> 本轮针对 A42 联合复查报告整改,逐条答复见 **`docs/REVIEW-A43-RESPONSE.md`**。
>
> | 复查阻断项 | 状态 |
> |---|---|
> | BLOCK-01 合并树无运行时证明 | **未关闭**,整改环境无网络/无真库,无法执行 |
> | BLOCK-02 租约缺 fencing token | 已修(待真库验证) |
> | BLOCK-03 属性不校验类型/枚举/层级 | 已修(待真库验证) |
> | BLOCK-04 多颜色图片变体绑定 | 部分:属性侧完成,图片绑定 UI 与稳定 ID 未做 |
> | BLOCK-05 审核详情状态残留 | 已修(待行为测试) |
> | BLOCK-06 导出超时按读处理 | 已修前端;后端幂等键未做 |
> | BLOCK-07 真实 SHEIN + 发布页 | 未做 |
> | P1-01/02/03/06 | 未做 |
> | P1-04/05 文档口径 | 已修正 |
>
> **离线门禁**:纯逻辑 1525/1527(2 条为本机缺 pydantic/sqlalchemy)、
> 交付 13/13、导入 298、样例 5/5、lint 292、前端语法 78/78。
> **仍未执行**:真库 pytest、Ruff 全规则、lint-imports、Alembic 升降级、
> 前端 typecheck/lint/Vitest/build/Playwright、Docker build。
>
> 这一版**不是**正式人工测试基线。


这份文档回答**现在是什么**:哪些能力可用、哪些是骨架、哪些已知不行、
以及要查什么该看哪份文档。

它刻意**不记录阶段历程**。「阶段 3 做了什么、第二轮评审修了什么」这类内容原先散在
27 份过程文档里,已全部删除;其中仍然生效的决定收进了 `docs/DECISIONS.md`。

---

## 一、能力现状

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| 商品与素材 | ✅ 可用 | CRUD、批量导入(逐行计划 + 行级错误)、上传格式探测、双层哈希去重、原始文件永不覆盖、审计日志 |
| Provider 抽象 | ✅ 可用 | 虚拟试穿与商品转模特图两种模式;9 类错误各带 retriable / switchable / requires_human 策略 |
| Mock Provider | ✅ 可用 | 用 Pillow 真出图;可模拟成功/失败/超时/空结果/限流/内容安全 |
| FASHN | ⚠️ 已实现,未用真实 Key 验证 | 请求/状态/错误映射、轮询、自检齐备。首次验证步骤见 `docs/PROVIDER-FASHN.md` 第八节 |
| ComfyUI / fal.ai | 🚧 骨架 | 能力声明、配置检查、连接测试可用;请求映射待官方文档确认。创建任务时即被挡下,不会跑到 worker 才失败 |
| 状态机与幂等 | ✅ 可用 | 17 个状态完整转移表,非法跳转 409,终态封闭;幂等键由输入派生,每轮 seed 可复现 |
| 异步编排 | ✅ 可用 | Celery 流水线、协作式取消、Outbox + `make requeue` 兜底、每次调用落 attempt 与用量流水 |
| 评分与分档 | ✅ 可用 | 11 个维度;A/B/C/D 三条线取最差(硬错误一票否决、分数区间、问题严重度上限);总分由后端算,模型自报分只留档 |
| 视觉大模型评分器 | ✅ 可用 | 一套代码四种后端(OpenAI Responses / OpenAI 兼容 Chat / 火山方舟 / 阿里云百炼),按 `VISION_MODEL_API_STYLE` 分适配器。生产 fail-closed。详见 `docs/VISION-EVALUATOR.md` |
| 提示词版本管理 | ✅ 可用 | 存库带版本,页面全文编辑、体检、版本历史、一键回滚;每次评分把 `prompt_version` 记进 `raw_response` |
| 人工审核队列 | ✅ 可用 | 审核对象是**任务**不是候选图;A 档按比例随机抽检(不阻塞) |
| 多尺寸成品图 | ✅ 可用 | 五个用途,**绝不放大**;正方形补边不裁剪;重算不删旧图(旧 URL 仍可打开) |
| 渠道 API Simulator | ✅ 可用 | 九种平台行为（创建/更新/幂等冲突/限流/鉴权失败/字段拒绝/异步审核/发布成功/平台驳回）。**无状态**：场景与创建时刻编码在 external_spu_id 里，多进程与重启后行为一致。SPU 以 `SIM-XXX-` 开头即可触发对应场景 |
| 渠道报文构造 | ✅ 可用 | `generic.build_request` 纯函数，干跑与真实提交共用同一段构造逻辑；非创建操作缺 external_spu_id 当场抛错 |
| 自动上架与更新 | ✅ 可用 | 幂等创建/更新,三段事务(业务事务 / 事务外调用 / 新事务保存),投递走 `publish_outbox`。提交超时不猜结果,落 `SUBMIT_RESULT_UNKNOWN` 等人确认 |
| 平台状态轮询 | ✅ 可用 | 到期轮询 + 指数退避(封顶 1 小时,**不通往放弃** —— 轮询是读,停下来等于本地与平台永久分叉)。404 绝不当作已下架。平台驳回自动进既有驳回台账,`located_by=publish_attempt` |
| 下架与测试清理 | ✅ 可用 | `DELIST` 不看草稿状态、不带报文内容;4.1 节 H 清理预案见 `make cleanup`,必须限定作用域,默认只看不做,`verify` 未清干净时退出码 1 |
| 发布接口 | ✅ 可用 | 六个端点(提交/清单/详情/刷新状态/下架/清理清单)。状态派生只有一份,在 `workflows/publish_view.py`:后端给 `display_status` / `next_action` / `blocking_reasons` / `allowed_actions`,前端只展示和触发。多一个状态机里没有的 `STALLED` —— listing 说在途而 outbox 已 DEAD 的组合,少了它界面会一直显示「提交中」。前端在 `pages/PublishPage.tsx`(**任务 20-A**,B-02 已关闭;浏览器未实测,Playwright 在任务 24) |
| 身份与权限提示 | ✅ 可用 | `AUTH_FORBIDDEN` 与 `AUTH_FAILED` 分开:「登录态失效」与「权限不足」在前端是两句不同的话,且 403 不清登录态、不跳登录页。a46-phase6 之后浏览器不持有任何 Token,「口令写入失败」一族已随录入卡退役 |
| 付费调用花费台账 | ✅ 可用 | 按月/provider/天汇总,预算档位与耗尽日外推;金额取整数微单位。**是本系统台账不是厂商余额**,未配价的调用单独计数不计入金额 |
| 导出 | ✅ 可用 | 单件与批量,JSON + CSV + XLSX;CRLF + utf-8-sig;批量上限 1000 行 |
| 属性识别与确认 | ✅ 可用 | 一图一字段一条证据;未校准品类不自动确认;逐字段确认,批量确认排除冲突项 |
| 图片集编排 | ✅ 可用 | 拖拽 + 键盘;批准后不可原地改,重排 derive 出新版本 |
| 渠道文案 | ✅ 可用 | 生成 → 校验(禁词/声明/长度)→ 人工编辑落新版本 → 批准前强制重校验 |
| 上架草稿与导出闸 | ✅ 可用 | 手填字段从渠道 spec 反推;过期提示按 §4.5.1 展开(哪个上游、哪些字段、做什么) |
| 批量运营与异常页 | ✅ 可用 | 计数即筛选;异常按「步骤 → 问题码」两层分组;付费动作执行前确认 + 幂等回执 |
| 批次原子领取与租约 | ✅ 可用 | 任务 17。`claim_items` **一次领一件**(`CLAIM_CHUNK = 1`,A42 从 10 收下来),`FOR UPDATE SKIP LOCKED` + 条目租约(`lease_until`,迁移 0025)。两个 worker 跑同一批次时各领各的,重复投递因此变成安全操作 —— 这是任务 18 敢自动重投的前提。**领取批量参与租约不变量**(`ITEM_LEASE_SECONDS > CLAIM_CHUNK × 单件最长合法耗时`),所以它住在 `batch.py` 而不是服务层;要调大必须先做续租,否则导入期 assert 直接拦住 |
| 批次异常恢复 | ✅ 可用 | 任务 18。租约过期 → `reap_expired_leases` 放回 PENDING(上限 3 次,超过落 `WORKER_LOST`);有 PENDING 却很久没动的批次 → `redispatch_stalled_batches` 重投一次。beat 每 60 秒一拍,没 beat 时退回 `make requeue`。**没有新建 outbox 表** —— 条目本身就是意图记录,理由见 `docs/DECISIONS.md` §3.12 |
| 请求的事务边界 | ✅ 可用 | 任务 19 后半。`get_session()` **不再替所有请求提交**,事务归接口函数所有;写端点各自 commit,GET 一律不提交(唯一例外是 `download_batch_file` 的下载审计流水,只增不改)。批次执行跨付费调用的长事务是**署名例外** —— 事务级 advisory 锁必须活到回执可见那一刻,见 `docs/DECISIONS.md` §3.19。门禁在 `tests/pure/test_transaction_boundaries.py` 的「HTTP 边界」一节 |
| 平台侧状态与驳回回流 | ⚠️ 手工录入 | 平台状态 + 驳回台账;定位把握分 audit / current_draft / unlocated 三档如实标注。**不接平台 API** |
| 逐件快审 | ✅ 可用 | `/workbench-review`,一屏一件、J/K/A/R 键盘流(R 退回,弹窗开着时其余键让位)。**依然没有批量批准,同理也没有批量退回** |
| 后台设置页 | ✅ 可用 | 改完不重启即生效(TTL 覆盖层);密钥 Fernet 加密落库,明文不出后端。详见 `docs/SETTINGS.md` |
| 对象存储 | ✅ 可用 | `S3ObjectStorage` 兼容 S3 / MinIO,与本地存储共用路径推导,可 `mc mirror` 迁移 |
| 今日待办首页 | ✅ 可用 | `/today`(默认落点),七张待办卡片 + 其余动作码收成一行;每张卡带筛选参数跳转,计数全部来自后端 |
| 快审退回 | ✅ 可用 | A10。九个受控原因 + 可选说明(选「其他」必填),退回后由后端按原因推出唯一下一步——「其他」与认不出的原因码按**被退回的对象**兜底,不按原因猜。图片集与文案各一个退回接口,原因清单走 `GET /workbench/reject-reasons`,中文与下一步只有后端一份。两个编辑页都有退回回执(原因/说明/时间),文案的 `REJECTED` 按 `reject_reason` 分「快审退回」与「校验失败」两种显示 |
| 未保存离开保护 | ✅ 可用 | A11。六个编辑面(属性/图片集/文案/草稿/设置/提示词)统一挂 `<UnsavedGuard>`;站内导航靠 `useBlocker`,刷新与关标签靠 `beforeunload` |
| 运营菜单收敛 | ✅ 可用 | 四组分区。**「只对管理员显示」这半句已过期**(a46-phase2 订正):侧栏「系统管理」整组(adminOnly: true)只对管理员显示;顶栏用户菜单的「系统设置」也只给管理员。operator 手输 /settings 打得开,页面上是一句 403 —— 路由不按角色裁剪,只有菜单裁。真正的权限边界在后端 require_admin。a46-phase6 落地;a46-phase2 曾经把始终可见的设计写进代码,phase6 撤掉了它。**a47 又收了一轮**:运营那三组按「同一件事只留一个入口」收敛,`/reviews`(并进审核中心)、`/products`、`/spus/new`、`/workbench-import`、`/workbench-spus` 五项撤出菜单,`/tasks` 移进「系统管理」组(对运营即消失)——前提是工作台详情的生成任务页签先能答出状态/轮次/失败原因,那一步同轮落地。**六条路由一条都没删**,手输仍可打开。项数不写在文档里,判据是 `App.tsx` 的 `OPERATOR_NAV_ITEM_COUNT` + `nav-and-url-filters.test.tsx` 那条一致性断言 |
| 出图按方案执行 | ✅ 可用 | **a47 唯一一处业务正确性修复。** 此前六个 `GenerationPlan` 参数里只有 `budget_cap` 生效:`provider` / `model_template_id` 取调用方传的,`scene` / `pose` / `angles_json` 在建任务与执行链路里一次都没被读过 —— 而验收链路按 `gp.required_angles` 检查,于是「配了方案 → 出图不按它 → 验收按它判不完整」,运营看到不完整而他没做错任何事。现在方案解析后由后端决定 provider / 模板 / 一轮张数,scene+pose+angles 进提示词,幂等键用解析后的值,冲突在审计里记「请求 X,方案 Y,按 Y 执行」。**没有给 `generation_tasks` 加列**,追溯靠已有的 `generation_plan_id` + `plan_fingerprint`。管理员可传 `override_plan` 绕过(非管理员 403,不是静默忽略),绕过后两个 plan 列留空、**但预算照查**。四条等式的真验证在 `tests/test_a47_plan_governs_db.py` —— **写了、一次都没跑过**(无 PostgreSQL) |
| 建任务入口分层 | ✅ 可用 | a47 §7。运营版只看:商品、模特、出图方案(只读解析结果,来自 `GET /generation-plans/effective`)、角度、张数、预计付费调用次数。Provider / 每轮候选数 / 最多轮次 / 基础 seed / 提示词 / `provider_params` / `override_plan` 收进「高级选项」,默认收起且仅 `isAdmin` 可见。方案指定了 Provider 或模特时那两项在界面上**只读** —— 后端已按方案执行,再摆一个可选下拉是在界面上撒谎。**浏览器未实测**(无 node_modules) |
| 工作台按款视图 | ✅ 可用 | a47 §8。`[按 SKU] [按款]` 切换,**默认按 SKU,本轮不切默认**(切默认的判据是测 10~20 款后的使用数据)。状态写进 URL,跟这一页另外七个筛选同一规矩。按款显示 SPU 码、款名、SKU 数、颜色数、完成度(旗下最小值)、阻断数(求和)、**卡点分布**、可展开 SKU 明细。**没有款级 `next_action`,而且不该有** —— 不同 SKU 卡在不同步骤时任何单选都是编的,理由见 `DECISIONS.md` §3.72 第四节。口径提醒:`blocking_count` 数问题条数,`blocked_steps` 数 SKU 个数,两者不相等。**后端 `/workbench/spus` 只认搜索词与分页**,另外六个筛选在按款下不生效,界面上明说 |
| 审核中心 | ✅ 可用 | a47 §6。`/workbench-review` 改名,顶部四条计数入口(候选图审核 / 图片集待批准 / 文案待批准 / 属性冲突),点进去仍是各自现有页面。**只做入口,不动模型**:没有统一审核表、统一 DTO、统一 mutation,三套审核语义一个都没改。计数全部来自后端已有接口(`/reviews?status=PENDING` 的 total、`summary.by_next_action` 两条),**读不到的整块不显示** —— 一个「0」和一个「没查到」在界面上长得一样,而运营会按前者理解 |
| 仪表盘 | ✅ 可用 | `/dashboard`,需求第十四章点名的全部指标。A9 之后它不再是首页,归到「系统管理」组 |
| 生成链路冒烟 | ✅ 可用 | `make smoke`,健康检查 → 素材 → **已授权模特模板** → 异步任务 → 评分 → 成品图 → 导出 → 仪表盘,走 Mock 不花钱。**不覆盖审核之后的链路(6.5–6.10:人工审核/图片集/文案/草稿/发布/轮询/下架)** —— 那段由真库 pytest(`test_publish_flow_db.py`、`test_poll_and_delist_db.py` 等)做集成验证,交互路径按 `LOCAL_MANUAL_TEST.md` 手工走。此行以前写"端到端",A45 独立审查 C-11 指出那是高估,已更正;同轮起冒烟改走已授权模板,不再借道 MODEL_REFERENCE 绕行缝 |
| 评分器校准 | ✅ 可用 | `make calibrate`,模型分档 vs 人工结论的混淆矩阵;样本不足 20 条拒绝给结论 |
| 失败提示分层 | ✅ 可用 | A12。判定只有 `describeError` 一份,展示只有 `<ErrorNotice>` 一个出口;运营看一句话 + 请求编号(可复制),管理员多一个**默认收起**的技术详情。`docker compose` / 迁移命令 / 表名收进管理员分支。**主流程 10 处已换,全站另有 17 处 `Alert + readError` 未迁移** —— 这 17 处**没有任何自动化防线**,只许减不许增靠代码评审 |

## 二、MVP 明确排除、也确实没有实现的

多租户、计费、账号体系、电商平台 API 对接。

> 曾经有两条测试(`test_out_of_scope_features_are_not_implemented_early`、
> `test_out_of_scope_tables_are_not_created_early`)守着这条边界。**那两条已在测试
> 套件清理时删除**,理由是阶段边界属于项目管理约束,留着会在日后正常增加认证、
> 多租户、订单时挡路。所以**这条边界现在没有自动化防线,靠代码评审**。
> 这份文档此前声称那两条守卫仍在,是错的。

## 三、已知限制

| 项 | 说明 |
| --- | --- |
| 费用只按主币种汇总,不折算 | 「本月已花费」与预算进度条只统计 `SPEND_CURRENCY` 那一种,**这是刻意的**(汇率是估的,这一页用来发现异常不是对账)。a32 起有别的币种时页面会明说「另有 X 没有计入」并列出各币种金额(后端 `by_currency`,金额由 `format_money` 生成);a34 起 `by_currency` 还带 `calls` / `unpriced_calls`,于是**只有未配价调用的币种不再从提示里消失** —— 它金额是 0,而那个 0 的意思是"未知"不是"没花"。仍**没有**的是每日曲线本身 —— `/spend` 页至今不画曲线,`daily[]` 的数据没有消费方 |
| ~~图片集变体绑定无入口~~ **已关闭,原文整条过期(2026-08-09 评审订正)** | 原文写着「前端没有设置变体的地方,`variant_id` 恒为 null,所以变体覆盖规则至今不触发,**BLOCK-02 保持「处理中」**」——**三句话全部与代码事实相反**,而且方向是最危险的那种:把已经做完的说成没做,下一个人会去补一个已经存在的门禁,或误判多色 SPU 今天可以带缺图批准。事实是:①入口在 `ImageSetTab.setVariant()`(A-26,行级颜色选择器 + 「再绑一个颜色」);②`image_set_rules.coverage()` 已删掉「有通用图就算覆盖」那条放行(§6.5:通用图只能进附图位、不得跨色回退);③`image_set_service.validate()` 与**批准路径**都传 `variant_ids=required_variant_ids(...)` + `required_angles`,有问题时抛 **409 阻断批准**。原文最后那句「今天所有图都是通用图,改成硬阻断等于让每个多色 SPU 立刻无法批准」是当时不做的理由,它随 A-26 的入口一起失效 |
| 退回人不回显 | 退回回执显示原因、补充说明与时间,不显示退回人 —— 系统没有账号体系,`rejected_by` 取的是 `X-Actor` 头(缺省 `system`),显示出来是一个假的问责对象。该值仍落库并进审计,查得到 |
| 属性值只能改标量与列表 | a31 按后端给的 `value_type` 渲染控件,列表字段不再被回传成字符串(BLOCK-11)。但值是**结构化对象**时仍然只读 —— 当前注册表里没有这类字段,真出现时界面会如实说「编辑器装不下」,而不是 `JSON.stringify` 之后当字符串存回去 |
| A12 只覆盖主流程 | 换掉的是工作台主链路的 10 处。快审页(4 处)、审核队列、商品详情、任务详情、设置页等 17 处仍是 `Alert + readError`:文案对,但技术层没有落点,管理员在那些页面还得开控制台。**这 17 处没有被冻结进任何契约测试** —— 本文此前两处都声称有,是错的。`describeError` / `readError` / `<ErrorNotice>` 的**行为**在 `frontend/tests/` 里是有测试的(`client.test.ts`、`error-and-cold-start.test.tsx`),但**没有任何一条去数调用点**,所以加第 18 处 `Alert + readError` 不会有东西报错。要变成棘轮,得加一条统计 `readError(` 的 `<Alert>` 用法数与 `useWriteError(` 调用点数、只许减不许增的测试。另有 5 处 `message.error(readError(e))` 的 toast 按设计不迁移(全站 toast 调用点共 16 个,本文原写「100 多处」是错的)—— toast 装不下折叠面板 |
| 无账号体系 | 审计操作者取 `X-Actor` 头,缺省 `system`;改配置另有 `ADMIN_TOKEN` 一道运维口令 |
| `POST /api/reviews/{id}/approve|reject|regenerate` 曾无条件 500 | `_basic_review_out` 签名被改成批量形态(`item, *, products, task_statuses`),列表页跟着改了,三个写接口没改,它们仍按老签名调用。A42 修:抽 `_single_review_out(session, item)` 转发,这样下一次签名变化时编译期报错而不是运行期 500。这是第二条真实生产缺陷(第一条是环境判定那一面) |
| 环境真实性横幅曾错报三档 | A37～A41 期间 `/api/environment` 把 Mock 出图与 Mock 评分报成 REAL、把正常工作的渠道 Simulator 报成 UNAVAILABLE。A42 已修。新接一个真后端时要在实现类上写 `is_simulator = False`,忘了写只会多喊一次警告,不会反过来说假的是真的 |
| ~~属性识别只有 Mock~~ **A45-batch14 接上了 vision,但一次都没连过真模型** | 任务 7 已落码:`extractors/vision.py`,双 API 形状,复用 `llm/` 的传输层与图片层,`EXTRACTOR_BACKEND=vision` 即启用。**但它从未对着真实端点发过一次请求** —— 守卫全是纯逻辑与 AST,验的是「请求体长什么样、响应怎么解析」,验不到「厂商真的会这么回答」。第一次接真端点时要盯三件事:结构化输出降级(strict json_schema 被拒 → 换 json_object → 换 prompt_only)、`finish_reason=length` 有没有如实报成「输出被截断」而不是「JSON 不合法」、以及**账单上的调用数与 `provider_usage_records` 里 `operation='attribute_extract'` 的行数对不对得上**。`describe_extractors()` 从本批起如实上报(`configured` 问实现自己),所以配错时状态条会说「没配好」而不是「已就绪」 |
| ~~**识别输入白名单还没收(§5.1 未落地)**~~ **已解决,原文已过期** |判定在 A45-batch14-7 就接上了(AI 图进不来),取数入口在 A45-batch14-19 收口(`media.evidence_assets_for()`)。原文说「今天仍走 `usable_assets()`、AI 图会进识别输入并产生真实付费调用」——**那句话从 14-7 起就不成立**,留着它会让人去查一个不存在的问题。真库验证 `tests/test_a45_batch14_19_evidence_query_db.py` 已写、未跑 |
| **AI 图伪装成样品:两条路堵了,第三条还开着** | A45-batch14-11 接了 §11 / AC-22 的溯源冲突拦截:同 SPU 同 sha256 命中带溯源的行时,新建那一路落隔离、去重命中那一路不再补角色。**但拦截的判据是「本系统生成过这张图」,不是「这张图是 AI 画的」** —— 从别处拿来的 AI 图(外部工具生成、供应商推来的合成图)没有任何溯源痕迹,照样成为 `PRODUCT_EVIDENCE`。堵那一条要靠图像侧的判别,不在本期范围。另外:冲突**只记 `logger.warning`,没有落审计**(`ingest()` 手里没有 actor,记谁头上是一个业务决定);隔离行经 `release()` 人工放行后会**重新成为证据**(它的 `source` 是 `MANUAL_UPLOAD`,而 `evidence_class` 由 source 派生)—— 那是设计(人工放行的语义就是「我确认这确实是实物样品照」),但**没有人在真界面上走过这一步** |
| ~~**"不指定模特"绕行缝(C-10)**~~ **已于 2026-08-11 关闭,但是以拒绝的方式关的** | `generation_service._assert_assets_are_usable()` 的 `MODEL_REFERENCE` 分支不再 return,改为 `ValidationError`;`tasks/generation_tasks._build_request` 里那条"拿不到模板图就退回自由上传模特图"的兜底也删了(它是同一条缝的第二道门,而且发生在四道检查都过了之后)。**关的方式是 fail-closed,不是接通闭环**:自由素材今天解析不到授权主体 —— `ProductAsset` 上没有指向 ModelTemplate 或授权记录的列,`MediaAsset.consent_id` 列在但**全仓没有写入点**,`MediaConsent` 又没有受众字段(连 §10.5 都判不了)。运营的出路是把那张模特图登记成 ModelTemplate 再选它,那是**唯一**能执行四道检查的路径。要重开自由上传这条路,得先补上上面三样中的任意一条可用链路 |
| 「有证据但不采信」没有界面 | A45-batch14-11 的判定已经产出 `ATTR_EVIDENCE_ONLY` 阻断与 `FILL_ATTRIBUTES` 动作码,前端补了动作码的三张镜像表 + 首页「其余待办」。**但属性页上没有为这条动线做任何事** —— 没有「这批字段为什么不采信」的分组展示,也没有一键人工填写入口。运营点「人工填写属性」会落在属性页,然后自己逐个找。真实抽取器接上、校准为空时这条动线会是主路径,那时要补 |
| ~~识别的付费调用与 HTTP 请求事务同生死~~ **已由 0054 关闭** | 两个 HTTP 入口现在只 `queue_extraction()`，先提交 QUEUED run，再投递 Celery；真实模型调用与 `record_usage` 在 worker 的独立会话中执行，后续 HTTP 回滚不再撤销已经发生的调用流水。迁移 `0054` 同时落输入快照、逐图成绩、取消与恢复列，relay/reaper 补漏投与卡死。Windows 真基础设施记录见本文顶部与 `AC-VERIFICATION.md` §11。**这只关闭“HTTP 事务同生死”这笔账**；真实供应商的计费口径仍未连端点验证，见上方 vision 限制，不合并宣称 |
| **运行日志控制台没有连过真 Redis** | a53 落地了环形缓冲(`ops:log_ring`)、载荷旁挂库(`ops:llm:{id}`)与三个读接口,但**写入、读取、TTL 到期三条都只有纯测试用假客户端覆盖过**。真 Redis 上还没验的是:LTRIM 之后 `held` 与 `cap` 对不对得上、TTL 到期后 `/api/ops/llm/{id}` 是不是如实回 404(而不是回一个半截记录)、以及 Celery worker 与 API 两个进程写进同一个键时 `seq` 去重在跟随模式下够不够用。**日志写失败是静默吞掉的**,所以这三样出问题时不会有任何报错 —— 唯一的信号是 `/api/ops/logs/meta` 里的 `dropped_since_boot` |
| 运行日志页浏览器未实测 | 前端门禁(typecheck / lint / Vitest 9 条 / build)全绿,但**没有人在浏览器里点开过这一页**。Playwright 用例属于任务 24,尚未开工 |
| 三个 ops 端点没有被 TestClient 打过 | `make test-nodb` 与 `pytest` 需要 fastapi / sqlalchemy,交付这一轮的机器上没装。判定层(域推导、折叠、404 分两种措辞)有纯测试,**接口层的取数与闸没有** |
| 配置变更无值历史 | 审计只记谁改了哪些键,不记改前改后的值(记了等于把明文密钥换个地方存) |
| worker 配置最终一致 | 改完配置 worker 最迟 `SETTINGS_CACHE_TTL_SECONDS` 秒跟上,期间两边可能不同 |
| FALLBACK 路由 | 显式抛错说明尚未实现,不假装支持 |
| ComfyUI / fal.ai | 仍是骨架,请求映射待官方文档确认 |
| FASHN 不支持取消 | 官方文档没有取消端点;轮询期间 Celery worker 阻塞,上量后需改 webhook 驱动 |
| Provider 切换只有机制 | C/D 档规则会要求换 Provider,但今天只有 Mock 已配置,`next_configured_provider()` 返回 None 并把原因写进 attempt |
| 成品图无批量重算入口 | 网站改尺寸时需要整批重算,目前只在「再次通过」时触发 |
| Celery 单队列 | 未做优先级与并发调优 |
| 租约回收对「跑到一半才崩」仍会重复付费 | 回执表(`batch_action_receipts`)挡的是「已经跑完的不重跑」,它在调用**之后**才写。worker 恰好死在付费调用与写回执之间时,重排那一次会真的再花一笔。唯一的缓解是租约足够长(`ITEM_LEASE_SECONDS` > `CLAIM_CHUNK` × 单件最长合法耗时,带 assert 守着)与 `MAX_ITEM_ATTEMPTS = 3` 的上限。**这一段没有真库测试** —— 见下一行 |
| 发布投递的状态互踩已经关掉(A45-batch17-2),重叠投递窗口仍在 | `publish_service._save()` 原来无条件写三张表:租约过期被重领之后,迟到返回的那次调用会把 DONE / SUCCEEDED / LISTED 覆写成 DEAD / UNKNOWN / SUBMIT_RESULT_UNKNOWN —— 商品在平台上好好的,界面说结果未知需人工。**本行原来根本不存在**:批次侧的同型缺口 A43 已修并记账,发布侧当时既没修也没记。现在落库走令牌(迁移 0047,`lease_token`),执行权不在自己手上时结果只进审计。仍**没有**关掉的是重叠投递本身:调用耗时越过 `LEASE_SECONDS = 180` 时同一份报文仍可能被发两遍,靠幂等键 + 唯一索引 + 平台 409 三道防线保证不多出商品(`DECISIONS.md` §3.19),而三道防线里第三道要依赖平台真的实现了幂等。今天不可达 —— 唯一的 transport 是进程内瞬时返回的 Simulator;第一个真实 HTTP transport 接入且客户端超时未钳制在 `LEASE_SECONDS` 以下时变为可达。真库双 session 用例 `tests/test_publish_lease_concurrency_db.py`(7 条)**已写、未跑** |
| 租约到期窗口仍在,但**状态互踩已经关掉** | `reap_expired_leases` 只看 `lease_until` 过期,**不问 owner 还在不在**(系统里没有活体名单,`lease_owner` 明写只用于排查)—— 这一半仍然成立,而且是刻意的。**另一半已经不成立了**:本行原来还写着「`_apply_outcome` 不校验本 worker 是否仍持有租约就覆写 status」,那是 A42 的事实;A43 / BLOCK-02 之后落库走 `apply_outcome(owner=, token=)`,四个条件进 WHERE,令牌被回收吊销之后旧 worker 的更新影响 0 行、结果只进审计。不带校验的 `_apply_outcome` 现在是**零调用点的废弃函数**(留着是为了让旧调用点立刻可见)。所以准确的说法是:**重叠执行仍可能发生(钱可能真的花两次),状态互踩不会**;前者靠租约时长与续租控制,后者靠令牌。双 session 场景在 `tests/test_batch_lease_concurrency_db.py`(10 条,含「租约过期但 worker 还活着」那一条),A42 跑绿过,batch12-6 之后**未重跑** |
| ~~租约与回收的行为**未在真库验证**~~ **A42 已验** | `tests/test_batch_lease_concurrency_db.py`,8 条双 session 用例,在真 PostgreSQL 16 上跑绿:两个 worker 互不相交(`SKIP LOCKED`)、活租约领不走、过期租约可接管、`lease_until IS NULL` 的存量残骸可领、回收放回队列、超上限落 `WORKER_LOST`、一次只领一件。变异验证过:删掉 `skip_locked` 会在 3 秒锁超时后失败并点名原因(**不是挂起** —— 用例给 B 装了 `lock_timeout`,否则 CI 只会报「作业超时」);领取漏掉 `IS NULL` 分支会让存量残骸那条变红 |
| 提交这件事集成测试验不了 | `tests/conftest.py` 的 `client` 夹具把 `db_session` 覆盖成一个**不提交**的 session(每个用例一个事务,结束回滚)。同一个 session 里读得到未提交的写,所以「真的提交了」与「只是 flush 了」在 API 测试里完全等价 —— 一个写端点漏掉 commit,现有测试一条都不会红。这不是夹具写错了(用例之间要隔离只能这么写),但代价是提交落在测试射程之外。**唯一防线是 `tests/pure/test_transaction_boundaries.py`「HTTP 边界」那五条**,它们做过变异验证。摘掉请求级自动 commit 这次改动**没有在真库上跑过** |
| 任务 19 两半都没有运行时证据 | N+1 那半(a38)守的是源码形状,`REVIEW.md` B.8 的「实测 SQL 计数」仍欠;事务边界这半(a42)守的是「谁写了 commit」,不是「事务真的在那一刻结束」。两条都拦得住最容易发生的退化,都**不能替代**真库验证 |
| 识别与提交的**次数**已进台账,但供应商侧口径未验 | A45-batch18 / P1-2 + P2-2:传输层重试与 FASHN 分批提交原来都按「一次业务调用 = 1 个计费单位」记,于是重试过的少记、preflight 失败的多记。现在识别按**网络往返次数**记(`llm/transport` 的 `on_attempt` → `extractors/call_accounting`),FASHN 失败按**已发出的 POST 数**记(`providers/call_accounting`),次数单独落 `provider_usage_records.provider_attempts`(迁移 `0048`)。**仍未验证的是供应商那一侧**:超时后重发对方到底计不计费、`x-fashn-credits-used` 是本次还是累计、失败时带不带那个头 —— 三个问题都要连真端点才答得出,今天全部按「宁可多记」处理并如实标 `units_source=inferred` |
| 供应商幂等键默认关着 | `EXTRACTOR_MODEL_SEND_IDEMPOTENCY_KEY=false`。打开后每张图带一把 `<run 幂等键>:<素材 id>` 的 `Idempotency-Key`,让供应商自己把传输层重发认成同一笔。默认关是因为**不认识这个头的严格网关会直接 400**,而那时的表现是「识别整条不通」,没有人会往幂等这个方向查。开之前先拿一次真实调用确认端点接受它 —— 这件事没做,所以重试仍可能让同一张图被受理两次(台账会如实记 2) |
| 清理清单现在含**结果未知且无 ID** 的行 | A45-batch18 / P1-3:原来查询强制 `external_spu_id IS NOT NULL`,于是一次 CREATE 到达平台、响应在返回 ID 前超时的行会从 inventory / plan / verify **同时**消失,报告说 `clean=true` 而平台上留着孤儿商品。现在这类行留在清单上,标 `needs_reconcile`、不可自动下架、带 `locator`(店铺 / SPU / 批次 / 时间窗),并让 `clean` 变成 False(CLI 退出码 1)。**没有验证过的是「按 locator 真能在平台后台找到它」** —— 那要真实渠道沙箱 |
| 破坏性清理禁止 channel-only | A45-batch18 / P1-4:`delist --apply` 现在必须带 `--tag` 或 `--shop`;只给 `--channel` 会被拒。预览不受限。原来两者共用一道 `any(...)` 闸,一次参数遗漏就是该渠道下全部店铺、全部批次的批量下架 |
| 批次租约的预算算错了三批,现已按识别配置推导 | A45-batch18 / P2-1:`LONGEST_LEGAL_ITEM_SECONDS` 原来是 `90×3×4`,三项全部取自 **VISION_MODEL_\*(评分器)**,而批次里跑的 EXTRACT 读的是 **EXTRACTOR_MODEL_\***。真实上限 `60×3×12 = 2160` 秒 > 当时的租约 1800 秒 —— 那条「租约必须长于单件最长合法耗时」的不变量**在默认配置下根本不成立**,而模块级 `if` 因为被除数取错一次都没红过。现在 `ITEM_LEASE_SECONDS` = 3600、`BATCH_PROGRESS_STALL_SECONDS` = 2700,并加了一条读**实际部署配置**的启动检查(`lease_budget_shortfall()`,设置页改大超时/图片上限时会报 error 日志) |
| 付费调用前的续租现在是**已提交**的事实 | A45-batch18 / P2-1:A43 加的 `renew_lease()` 顺序一直是对的(在 `_execute()` 之前),但那条 UPDATE 留在外层事务里,而外层事务要等结果保存完才提交 —— 整个付费调用期间回收器读到的仍是**领取时**那个 `lease_until`,续租等于没续。源码扫描看不见这个区别(两种写法顺序完全一样)。现在续租后立即提交,并补了双会话真库用例 `tests/test_a45_batch18_lease_visibility_db.py`(3 条,含一条反向用例证明不提交时确实读不到)。**该用例已写、未跑** |
| 门禁扫工作树,交付的是版本库 | A45-batch18 / P1-1:外部评审发现 `0046` / `0047` 两条迁移与对应测试**未被 Git 跟踪** —— 本机 `alembic heads` 说 0047、`verify_delivery` 16/16、纯测试全绿,而 clean checkout 的最后一条迁移是 `0045`。新增门禁 `check_every_migration_and_db_test_is_tracked_by_git()`(问 `git ls-files`,不是 Git 工作树时直接失败)。**它写完当场就红了**,红在本批自己新增的 `0048` 和那个新 DB 测试上 |
| 向导浏览器**部分**实测(2026-08-09 评审补) | A45-batch29 时这一行写的是「**没有人在浏览器里从头走过一遍**」。现在 AC-16 那一条走过了:`frontend/tests/e2e/wizard-refresh.spec.ts` 3 条真浏览器用例(刷新后停在 URL 上那一步、停在那个颜色、不带 `step` 时落后端算出的当前步),`page.reload()` 真的重建 JS 上下文 —— 组件卸载重挂载替代不了它。做过反证:把 `WizardPage` 的 `values.step ?? wizard.current_step` 改成恒用后端值,第一条当场变红。**仍然没有走过的**:七步从头到尾的业务动线(任务 24 的主体)、antd 布局在真实分辨率下读不读得下去、以及「断线重连不丢进行中任务」(要真 worker)。发布页(20-A)仍是一条浏览器用例都没有 |
| AC-05 的闸只在 HTTP 边界 | A45-batch29:`_ensure_action_allowed` 挂在 `generate_copy` / `build_draft` / `export_draft` 三个**接口**上,不在 service 层。**这是刻意的**:批次执行直接调 service 函数,它有自己的跳过判定与回执表,在 service 层加同一道闸会让批次在"这一件还没就绪"时抛异常而不是记一条跳过。代价是:任何绕过 HTTP 直接调 service 的新代码路径不受它管,而今天没有守卫能发现新增了这样一条路径 |
| 影响提示是**对象级**的,没有字段级 | A45-batch30:AC-17 的原话是"对象 / 字段 / 需要执行的动作"。`stale_matrix` 是 (变更源 × 对象) 的矩阵,给得出对象与动作,**给不出"哪几个字段会失效"** —— 那取决于运营具体改的是哪一个字段,而提示发生在他改之前、还没选定字段的时候。字段级今天只有**事后**那一份:草稿变 STALE 之后 `GET /draft/stale-reason`(BE-205)会点名字段。要做事前字段级,得让接口接受"我要改哪个字段"并按注册表推依赖,那是一条新口径,本批不猜 |
| 费用预估的用量是估的,而且方向恒定偏小 | A45-batch30:按"每个缺角度出 1 张候选图、每张评一次分"算,不含重试、评分退回与人工重做。文案生成今天不写付费流水(生成器未接付费后端),Mock 抽取器/评分器不计费 —— 两者都不计入,并在 `notes` 里说出来。未配价的动作金额记 `None` 且**不进总额**,界面显示「未配价」而不是 ¥0。所以那个总数**永远是下限**,`is_complete=false` 时界面会明说它不全 |
| 本地存储 | 后端 `/files` 直接托管,仅适合开发;生产改 `STORAGE_BACKEND=s3` |
| 上传走内存 | 20 MB 上限下可接受;大文件需改流式落盘 |
| 平台侧全手工 | 平台状态与驳回原因都靠人录入,不接平台 API |
| ~~API 驳回关不掉~~ **新数据已能自证关闭,旧数据仍需人工(2026-08-09 评审订正)** | 原文写「`resolve_gate()` 认的证据是驳回之后有一次新的**导出**,而 API 自动上架的商品根本不走导出,于是关不掉,**由任务 20-B 补齐**」——**20-B 已经落码**。`platform_service._publish_attempt_entries()` 把「驳回之后有一次**成功的**提交尝试」当作等价证据喂进 `resolve_gates()` 与解决路径;关联走 `ChannelListing.draft_id`,指纹那半边仍落在当前草稿指纹上,所以**没改草稿的重复提交过不了闸**;只认 `SUCCEEDED`(PENDING/IN_FLIGHT 没结果、UNKNOWN 不知道平台收没收到、FAILED/ABORTED 没提交成功)。**仍然欠两件**:①**真库 seam 没有** —— 从一行真实 `PublishAttempt` 穿过 `platform_service` 到驳回关闭,没有一条真库用例走完过;②`draft_id IS NULL` 的历史驳回关联不上尝试,仍然只能在工作台手工标记。发布接口对后者照旧如实报 `REJECTION_CANNOT_AUTO_CLOSE` |
| 发布页没跑过浏览器 | 界面已补齐(B-02 关闭,`PublishPage.tsx` + 侧栏「发布上架」+ 导出页跳转)。**「没有 tsc、没有 vitest」这半句已过期**(A45-batch24 订正):`npm run typecheck` 与 `node tools/syntax-check.mjs` 从阶段 4 起就覆盖全部 89 个前端文件,`PublishPage.tsx` 在内。仍然成立的是最后半句 —— **没有一条用例点开过它**:Vitest 的 78 条覆盖 saveBlob、路由拦截、草稿页图片预览等,不含发布页;Playwright 未开工。门禁验到"入口存在且不自建第二份判定",验不到"点下去这一步真的发生了" |

| ~~集成测试有 15 条真实失败~~ **A42 已全部修** | 第一次把 `requires_db` 那批真的跑起来:1652 条 0 跳过,**最初 15 条失败**。根因是三件事:(1)产品缺陷:`POST /api/reviews/{id}/approve|reject|regenerate` 三个接口无条件 500 —— `_basic_review_out` 签名被改而三处调用没改,**这是生产 bug,非测试杂项**;(2)既有夹具缺陷:`celery_eager` 把 `commit` 换成 `flush`,导致应用代码的 `rollback()` 回到用例开头,同一任务被派发两次,第二次抢不到、rollback 全清 —— 改用 `savepoint` 模式;(3)过期断言:人工通过后任务继续走完出图,不停在中间态。修后:1652 全绿,含批次并发 8 条、生成链路 21 条、审核链路 17 条,全部真库双 session/Celery eager 跑过 |
| ~~菜单按角色收敛不是权限~~ **菜单不再按角色收敛(a46-phase2 订正)** | 原文写「A8 的「系统管理」组只对管理员**显示**」。那次翻转之后 `NAV` 里已经没有 `adminOnly`,`nav-and-url-filters.test.tsx` 反过来钉着「不按账号隐藏」——而纯层还留着两条断言 `adminOnly` 必须存在的守卫,两边**互为反面**,红的那条一直没人看。本轮删掉纯层那两条(依据 `frontend/CLAUDE.md` 第三条:菜单可见性不是跨语言契约),判定只留 Vitest 一份。真正的边界仍然是后端 `require_admin` |
| ~~角色判定可被本地伪造~~ **a46-phase6 关闭** | 后端 `/auth/whoami` 答不上时降级看"本浏览器填了管理口令没"。往 localStorage 塞个假口令就能让菜单长出管理项,但那一栏的每个请求仍会被后端挡回 403。**浏览器登录模式下这条降级不再生效**:`is_admin` 由 Session 决定,a46-phase6 删掉了 localStorage 口令链 —— isAdmin 现在只认 probe.data?.is_admin === true,后端不答就是 false。行为已关闭;条目保留供历史追溯 |
| 浏览器登录与 Token UI 退役 | ✅ 可用(**前端用例未在浏览器里跑过**) | a46-phase2。`/login` 页 + 401 跳登录并带 `?next=` + 顶栏退出登录;`apiClient` 开 `withCredentials`,身份探测不再要求本地口令。部署认哪种凭据由 `/health` 的 `auth_mode` 说(`session` / `token`),前端三处分岔都读它。**新增的 Vitest、Playwright 与 `auth_mode` 那两条 pytest 一次都没执行过** —— 打包机器没有网络,装不上 `node_modules` 与 Playwright 浏览器。**条数不写在这里**(这一栏原先写「12 条 Vitest」,那是 phase2 自审时当场就改掉的第一版数,而订正只落在 HANDOVER,没有回流到这里 —— 正是第五节那条规矩要防的形状):逐文件清单与复跑命令见 `HANDOVER.md` 的 a46-phase2 与 a46-phase6 两节 |
| 刷新弹窗文案改不了 | `beforeunload` 只能触发浏览器自己的原生弹窗,文案由浏览器决定。站内导航那条路径是自绘 Modal,措辞可控 |
| 前端已改为数据路由 | `main.tsx` 用 `RouterProvider` + `createBrowserRouter`,**不是**为了用 loader/action,只因为 `useBlocker` 只在数据路由下可用。路由仍用 JSX 声明 |
| ~~首页筛选不进 URL~~ **A45-batch14-17 已修(GAP-033 关闭)** | 筛选状态改为住在 URL 里(`useUrlFilters`,URL 是唯一真相),工作台列表 / 生成任务 / 逐件快审 / 操作审计四页搬完,`useUrlSeed` 已删除。PRD §8.2 的四条要求现在全满足:URL 可复制、刷新保留、后退保留、点进来带条件。**代价与新风险各一条**:改筛选走 push,连点五个筛选要按五次后退(那是筛选类界面的正常行为,也是运营唯一能撤销一次误点的手段);后退/前进会改筛选却不经过任何 setter,所以清空勾选改挂在 `filters.signature` 上 —— 不这么做的话 BLOCK-09(越界批量)会从那扇门原样回来。**这一批的行为覆盖(Vitest 13 条)一次都没跑过**,被执行过的只有 12 条读源码的 Python 守卫 |
| 还有两页的筛选活不过一次刷新 | `ProductListPage` / `ReviewQueuePage` 不在 GAP-033 的射程里(没有首页卡片带条件进去),筛选与排序仍存在组件内 state。`useServerSort` 的 `store` 参数是可选的,搬它们时接上即可 —— 本批**没有动它们一个字**,行为与从前完全一致。不顺手搬是刻意的:那两页的筛选项没有对应的跨语言守卫,而本批能执行的验证只有守卫 |
| 「运行中任务」卡片不带筛选 | 它的计数是一个状态集合(`IN_FLIGHT_STATES`),而任务列表接口只收单个 status。点进去是全量列表,卡片上已写明 |
| 只读接口的 N+1 只在两处修过 | A41 把 `api/workbench.py` 的 `list_products` / `product_flow` 的兜底 `rollback` 挪到返回前(约束 14)。**同型风险没有全仓扫过** —— 任何「rollback / commit 之后再读 ORM 对象」的路径都会退化成逐行 SELECT,而 `test_workbench_query_budget.py` 结构上看不见它(它数的是源码里的 `session.*` 调用点,而这类查询是属性访问隐式触发的)。真实条数要带库开 `echo=True` 数一遍,这件事还没做 |
| 出参时间戳只统一了两组 | A41 的 `core/clock.iso_utc()` 收了工作台与批次两组出参(永远带 `+00:00`)。`platform_service`、发布接口、任务/审核那几组仍是裸 `.isoformat()`,于是「刚写完就回读」与「刷新重查」还会给出两种形状(`expire_on_commit=False` 的后果)。前端两种都认,所以它不会表现出来 —— 会看见的是导出文件和第三方消费者 |
| 本轮前端改动没有 Vitest 覆盖 | A41 修的两处前端缺陷(`saveBlob` 的 revoke 时序、列密度偏好被 matchMedia 推翻)都没补用例:改动是在离线环境做的,装不了 node_modules,写一条跑不了的用例会让「0 skip」那道门禁变成红的。有依赖的机器上应当补 —— 尤其列密度那条,它的失败路径是「手动设置 + 拖窗口」,不写用例下次还会漏 |
| 批量导出多出的那一列没有跟真实平台对过 | A41 给批量 SKU 表加了「所属 SPU」列（约束 17），列头复用 SPU 表里 `source: product.spu` 那个字段的列头。**目前只有 Simulator，没有真实渠道 transport**，所以「平台的导入器认不认这一列」没有验证过。接第一个真实渠道时要连同 `channels/generic/spec/swimwear.yaml` 一起复核：如果平台要求 SKU 行自带父 SPU，正解是把 `spu_code` 加进 `row_fields`（那会 bump `spec_version`、让全部草稿过期），而不是继续靠导出器补列 |
| 轮询守卫多一条 join 查询 | 约束 18 的 `_delivery_pending` 每保存一次轮询结果就查一次 `publish_outbox` join `publish_attempts`。走的是 `ix_publish_attempts_listing_started`，而同一轮里每行本来就要打一次真实外部调用，所以代价可以忽略——但这是**推理**，没有实测。上量后如果轮询扫描变慢，这里是第一个要看的地方 |

### 从过程文档收编的遗留项

原先散在 `FE-OPS-REVIEW-2.md` / `FE-OPS-CHANGELOG.md` 里,那两份已删除,
结论收在这里。

| 项 | 状态 |
| --- | --- |
| 平板适配 | 待业务决策。先问运营「审图在哪儿审」——「回工位」则不做,「边拍边审」则只适配快审页与首页 |
| 间距令牌未铺开 | `space` / `radius` 已定义,近 500 处 inline style 未收敛(本文原写 446,已过期;要当前数字跑 `grep -ro 'style={{' frontend/src | wc -l`)。随手改到哪个文件就收敛哪个,不单独排期 |
| 两条 lint 规则未验证语义 | `no-restricted-syntax` 的 AST selector **写错不会报错、只会静默失效**。CI 现在会跑 `npm run lint`,但那只证明规则能加载,不证明它选中了目标。需要人为写一段违规代码验一次它真的报 |
| `color-mix()` 浏览器下限 | `BrandTag` 用了它。Chrome 111+ / Safari 16.2+ / Firefox 113+。要支持更老的浏览器需退回预设浅色或手写 rgba |
| 暗色模式需人眼过一遍 | `BrandTag` 的 10% / 35% 混合比是对着亮色调的,深底上 `textMuted` 那一档可能太淡 |

## 四、需要你决策 / 提供的

- [x] ~~视觉大模型评分器用哪家、哪个模型~~ —— 已接入,四种后端按配置切换,不需改代码。
      **但上线前必须用人工审核样本重新校准 A/B/C/D 阈值**:换模型会整体平移分数分布,
      阈值不动会导致大批误判(见 `docs/VISION-EVALUATOR.md` 第八节)。
- [x] ~~**任务编号冲突,需要定一个**~~ —— **A40 已定:发布接口 = 任务 25**,
      原任务 18(Batch Outbox)保持原义且已由 A35 完成。补号行在 `REVIEW.md`
      12.1 表末,理由见 `docs/DECISIONS.md` §3.11。以下是当时的原始描述:
      `docs/REVIEW.md` 第 12.1 节的任务表里,
      **任务 18 是「Batch Outbox 与异常恢复」**(P3,依赖 17),与发布 API 无关;
      任务表里根本没有「发布 API」这一项(任务 20-A 直接依赖 15/16)。
      「任务 18(发布 API)」这个叫法出现在 REVIEW.md 开头的进度快照、
      `CLAUDE.md`、`HANDOVER.md` 三处,是后来某轮意识到 20 之前缺一层接口时
      顺手安的号,撞上了已有的 18。
      **A28 做的是发布 API**(按 HANDOVER 的描述),不是 Batch Outbox。
      建议给发布 API 一个新号并补进任务表。这件事必须定:`CLAUDE.md` 写着
      REVIEW.md 是验收口径,而 `verify_delivery.py` 有一半检查引用它的节号。
- [ ] **fal.ai**:具体使用哪个 model endpoint,以及它的输入 schema。
- [ ] **ComfyUI**:服务地址、真实工作流 JSON、各输入节点的真实 ID。
- [ ] **主密钥轮换**(人工动作)。早期交付包带出过 `.secrets/.settings.key`,
      应视为已泄露。步骤见 `docs/DECISIONS.md` §3.1。

## 五、本机真环境验收快照(2026-08-07)

PG `39.97.61.13:5432` + Redis `39.97.61.13:6379`,本机直连,**不起 Docker**。
迁移文件树 head = `0045`;真库 `imagegen` / `imagegen_test` 起始 head = `0038`,
本次把 `imagegen_test` 升到 `0045` 后跑 P0。`imagegen` 库未动。

逐条数字、失败响应原文、复现命令见 **`docs/AC-VERIFICATION.md`**。
本节给一张总结表,数字不写死(STATUS 第五节那条规矩);命令拿不准以
`AC-VERIFICATION.md` §7 为准。

| P0 项 | 状态 | 一句话根因 |
|---|---|---|
| P0-1 真库 pytest 全量 | ❌ | `POST /api/products` 接口契约改了(SPU 外键化),12-4/12-5 fixture 仍走老路径,11/13 在 `_product_with_asset` 上 422 |
| P0-2 Alembic 升降级 | ✅ | `imagegen_test` 上 0038→0045→base→0045 全链通过;`imagegen` 未动 |
| P0-3 前端四条 + docker | ⚠️ | ~~typecheck 6 条 TS2322(`nav-and-url-filters.test.tsx`);Vitest 因 `node_modules/.vite` root-owned EACCES~~ **前两格已于 2026-08-09 复验关闭**(typecheck 退出 0 零诊断、Vitest 97/97,见 `AC-VERIFICATION.md` §10);**docker 两条仍未验证** |
| P0-4 R-04/R-05 | ✅ | `run_pure_tests.py batch12_7` 16/16 + `verify_imports.py` 419 文件 |
| P0-5 BILLED_RESULT_UNKNOWN 演练 | ✅ | `12-7_billed_unknown` + `batch_receipt_lifecycle` 18/18 |
| P0-6 租约 fencing 双 session | ⚠️ | `batch_lease_concurrency` 1 条真库跑绿;`12-5` 5 条同 P0-1 fixture 问题 |

AC-01~AC-22:22 条**全部未验证**(原文多数沿用 v3.0,仓库内不可得;AC-21/22
有原文但需真实样照 + 真实抽取器调度)。**§14.3 的人工测试准入仍未满足**。

---

## 六、怎么验证

```bash
make test-pure     # 纯逻辑测试,只要有 python3 就能跑,不需要任何三方依赖
make test          # 容器内全量 pytest(含 requires_db 模块)
                   # 需要 PostgreSQL + Redis;缺 Redis 时生成链路会静默停在 CREATED
                   # (Celery 即使 eager 也要构造结果后端),表现是一批看不懂的失败。
                   # A42 合入时补了三道:CI 起 redis 服务、conftest 在 CI 里连不上
                   # 就点名炸掉、verify_delivery 盯住 ci.yml 别把它删了。
                   # **本机「有库无 Redis」仍然会得到那批失败** —— 治它要逐条贴
                   # requires_redis,而名单得真库跑一遍才准,见 conftest.py 那段注释
make smoke         # 对着跑起来的系统走一遍完整闭环
cd frontend && node tools/syntax-check.mjs   # 前端全量语法解析
```

本文不写测试通过数。写死的数字会在下一次增删用例时过期,而它过期了不会有任何东西
报错 —— 这个仓库的文档里曾经有 19 处这样的数字,各自冻在自己那一刻。要当前数字,
跑上面第一条。

---

## 七、文档地图

一共 20 份。**每份都写明「什么时候看」** —— 如果一份文档回答不了「谁会在什么情况下
打开它」,它就不该留下。这个数和下表的行数由
`tests/pure/test_a46_phase5_doc_truth.py` 钉在一起:漏收一份活文档、或者加了行
不改这句话,都会变红(a46-phase5 之前它写着 13,而表里已经是 15 行)。

地图**不含** `docs/MERGE-A45-*.md`、`docs/REVIEW-A4x-*.md` 与
`docs/REVIEW-STAGE*-CONCLUSION.md` 那批按批次/阶段留档的过程结论(眼下 9 份):
它们的检索入口是本文各版本块里的链接,读者是"要查某一批当时为什么这么改"的人。
要收编时按老规矩走 —— 结论沉 `DECISIONS.md`,再删原件;
别只删不沉(A44-BATCH8 那份是刻意不留的,见 `MERGE-A44-BATCH8-PATCH.md`)。

| 文档 | 什么时候看 |
| --- | --- |
| `README.md` | 第一次接触这个项目;想知道它是什么、怎么跑起来、有哪些页面和接口 |
| `docs/swimwear_sample_to_listing_prd_v3_1_1.md` | 要对"这系统该做成什么样"下结论 —— 阶段划分(§13)、人工测试准入(§14.3)、各条硬规矩的原文都在这里;全仓注释以 §N 指它 |
| `docs/STATUS.md` | **本文。** 想知道某个能力现在能不能用、有哪些已知限制、下一步卡在谁那里 |
| `docs/DECISIONS.md` | 要动数据库迁移、要改一条看起来「多余」的约束、准备升级一个已在运行的部署 |
| `docs/DEPLOYMENT.md` | 要把它部署起来,或者线上出了问题要按故障对照表排查。**在 Windows / macOS 上部署看第十节** —— 换行符、宿主机网络、缺少 `make` 三处会卡住,报错不指向真正原因 |
| `docs/OPS-REVIEW.md` | 要改工作台/批次/冷启动/平台驳回相关的代码 —— 代码里 20 多处注释以 `OPS-REVIEW P1/P3/P4/P5` 指回它。也是想了解运营真实动线时最值得读的一份 |
| `docs/VISION-EVALUATOR.md` | 要换视觉大模型、调阈值、或排查评分结果不对 |
| `docs/PROVIDER-FASHN.md` | 要第一次用真实 Key 验证 FASHN,或排查它的报错与费用 |
| `docs/SETTINGS.md` | 要加一个可在网页上改的配置项,或搞清楚为什么某项「改了没反应」 |
| `docs/LOG-CONSOLE.md` | 要加一条日志、要查「这次模型调用到底发了什么」、或者想知道运行日志页与操作审计页各回答什么问题。第十章记着落地时对设计的五处订正 |
| `comfyui/README.md` | 要接 ComfyUI —— 含节点 ID 的定位方法 |
| `sample-data/README.md` | 想知道示例商品与素材是怎么组织的 |
| `docs/REVIEW.md` | 要知道下一步该做什么 —— 施工方案(a20 v4.1),第 12 章任务表已标完成状态 |
| `docs/REVIEW-A28-TRACKING.md` | 要回答「a28 那份检视报告的 20 条阻断项现在还剩几条」。**下次复核只需核 4 条**,其余每条都绑了一条会红的测试 |
| `CLAUDE.md`(根 / backend / frontend) | 用 Claude Code 开工前。写的是约定与踩过的坑,不是目录说明 |
| `docs/AC-VERIFICATION.md` | 要回答「AC-01~AC-22 在本机真环境里到底哪几条跑过、哪几条没」 —— P0 6 项逐项结论、22 条 AC 状态表、复现命令合集都在这里 |
| `LOCAL_MANUAL_TEST.md` | 要在本机手工走一遍。Docker 启动、初始化、口令、**浏览器登录的六步验收(§4.5)**、6.1~6.10 逐步操作 |
| `docs/MANUAL-ACCEPTANCE.md` | 要做发布候选的完整 UAT 验收 —— 人员分工、两套库、配置清单、五个阶段的通过标准与证据要求 |
| `HANDOVER.md` | 想知道最近一轮改了什么、验了什么、**哪些没验**。顶部是最新一轮,往下是历史交接 |
| `AGENTS.md`(根 / backend / frontend) | 读 `AGENTS.md` 的 agent 工具用。内容与同级 `CLAUDE.md` **逐字一致**,由守卫钉着不许分叉 —— 要改约定改 `CLAUDE.md`,再同步过去 |

另有 `docs/vendor/fashn-skill/` 是 FASHN 官方文档的存档,不是本项目文档,
不计入上表也不要改动 —— `docs/PROVIDER-FASHN.md` 的实现依据全部指向它。

## A44 评审修复进度（batch1 ～ batch7）

逐批说明见 `docs/REVIEW-A44-BATCH3.md` ～ `REVIEW-A44-BATCH7.md`;
`a44-batch8-fixes.patch` 的合入取舍见 `docs/MERGE-A44-BATCH8-PATCH.md`
(那份补丁与本树是同一批意见的两条平行修复线,**不能直接 apply**)。

- A 类 44 条：已修 40 条；剩 A-11 / A-12（有意取舍）、A-31（新功能；原本挂着等 B-02，B-02 已关闭，可单独排期）。
- C 类 18 条：已修 8 条；剩 C-01/C-02（P0，需真库与真 Provider）、C-03/C-05'（取舍）、C-06/C-08 ～ C-10（运行时行为）。
- B 类：B-01 代码侧已合齐（业务侧待定），B-07 已给出可执行修复路径。
- 纯逻辑回归 1549 → 1646，失败数全程恒为 2（缺 sqlalchemy / pydantic 的 import 失败）。
- **D 类验证缺口一条都没关闭**：无网络、无依赖，真库 / tsc / vitest / docker 全部没跑过。

### B-02 / A45-#8 已关闭:发布链路补上了前端

原文是「`/publish/*` 六个端点在前端零引用,只能用 API 验收」。现在这条不成立了:

| 补的东西 | 位置 |
| --- | --- |
| 六个端点的客户端 | `frontend/src/api/publish.ts` |
| 「发布上架」页(清单 / 详情 / 提交 / 清理预案) | `frontend/src/pages/PublishPage.tsx` |
| 路由与侧栏(组名「导出」→「导出与上架」) | `frontend/src/App.tsx` |
| 从导出页进入的第一入口 | ExportTab 的「发布到平台」按钮,带 `?product_id=` |
| 结构性门禁两条 | `backend/tests/pure/test_publish_frontend_entry.py` |

**这条影响的是 A-07 / A-08 / A-13 的验收方式**:它们改的是这几个端点的后端质量,
在此之前没有页面可点,所以只能靠读代码验收;现在可以照着 6.4 节的
「发布前 / 发布中 / 发布后」在界面上走一遍。

那两条门禁比页面本身更重要。这次返工的形状不是「实现错了」,是**后端做完了、
但这条链路没有任何人能用**,而这种失效不会报错:接口测试全绿、门禁全过、
STATUS.md 写着已完成,只有真的坐下来点一遍才会发现动线在导出那一步就断了。
所以第一条门禁分三段断言(有客户端 / 有页面 / **路由挂上了**)——
「页面写完了但没挂路由」是这类返工最常见的形态。

第二条盯的是硬规则 4:发布状态散在四张表里,前端一旦自己拼就会出现
`publish_view.py` 顶部写的那种错(退避耗尽落进 DEAD,而界面一直显示「提交中」)。
所以按钮从 `allowed_actions` 渲染、文案用后端的 `display_status_label`,
页面里没有第二张状态表。

**剩下的**:A-31(渠道专用集前端入口)当初挂的理由是「应与 B-02 的发布页一起设计」,
那个前提现在没有了,它可以单独排期 —— 但它仍是新功能,不是修复。
另外这一页和其余前端一样落在 D 类缺口里,见上面「已知限制」表最后一条。


## 评审整改批:离线验证快照(2026-08-12)

这一批是**外部代码评审**驱动的整改(REVIEW II.8 / III.2 / III.6 / II.1)。之前 STATUS
的验证记录停在 08-09、HANDOVER 停在 a48、DECISIONS 停在 §3.74 —— 评审 II.5 点名的这处
交付一致性落差,本批补上**评审整改这一批**的记录(a50/a51 的仍缺,见文末)。

改了什么(四处代码 + 四份文档)与逐条理由:`docs/DECISIONS.md` §3.75。交接与「下一台
机器该跑什么」:`HANDOVER.md` 末节「评审整改批交接」。

**离线子集结果(本机 python3,无 node_modules / 无真库 / 无网络):**

| 门禁 | 结果 | 说明 |
|---|---|---|
| `make test-pure` | 2824/2824,10 跳过 | 本批 +11(FASHN 文案 7 + ErrorNotice 棘轮 4);跳过为缺 httpx/pydantic/sqlalchemy |
| `make verify-delivery` | 18/19 | 唯一 FAIL 是「交付源码被 Git 跟踪」——从 tarball 解出的目录跑不了它,这正是它该有的作用;pack.sh↔pack.ps1 数组不分叉已复验 |
| `make verify-imports` | 495 文件全绿 | |
| `make audit-anchors` | 565/565 | |
| `make audit-guards` | 660 守卫 | 反向断言都吃着封闭窗口 |
| `make audit-doc-refs` | 全绿 | 活文档 + 活代码路径引用指得到;台账里 27 个只提示 |

**仍未执行(与既有 D 类缺口同源):** 前端 tsc / Vitest / Playwright / build(装不了
node_modules)、全部真库用例(发布并发 7 条在内)、真实 FASHN / 视觉模型 / 真实渠道。
III.2 的棘轮只守「不再长新债」的结构;17 处迁移本身、以及任何前端行为改动,都要在
联网机器复跑 —— 迁移清单见 `frontend/tests/ratchet-error-notice.test-notes.md`。

**a50/a51 交接仍缺:** ~~包内 08-12 的那批改动(登录限流 `login_throttle.py`、
`client_ip.py`、路由级代码分割、nginx 安全头、`celery_app.py`、`db/session.py` 等)
没有交接文档,也没有对应的真库/前端回归记录。冻结交付前须补齐并在有网络 + 真库的机器上
重跑完整门禁 —— 本批没有替它把这件事做掉。~~

## 2026-08-12 评审整改 + a50/a51 收口交接

外部代码评审(REVIEW I / II / III)的原话归档在
`docs/REVIEW-EXTERNAL-2026-08-12.md`。a50/a51 的 08-12 那一批改动
(login_throttle / client_ip / 路由级代码分割 / nginx 安全头 / celery_app /
db/session)的交接在 `HANDOVER.md` 末节「a50/a51 交接」。本节是离线 + 真库
两套复验的快照,合上评审 II.5 / a49 留下的那笔账(凭据移出仓库树)。

**本次做了什么:**

1. 仓库根 `.env` 与 `.secrets/.settings.key` 移到
   `$env:USERPROFILE\swimwear-imagegen-secret-backup\`,**两个文件均未
   被 Git 跟踪**;`verify_delivery` 18/19 → 19/19;facet 默认 mock → SIMULATED
2. 顺手修一处 `test_provider_inline_size_message.py` 的 `Path` 死 import
3. **ProductListPage / ReviewQueuePage** 筛选 + 排序搬进 URL —— GAP-033 最后两页
4. 归档评审意见 + 决定 §3.74 / §3.75

**离线子集复验(本机 python3,无 node_modules / 无网络):**

```
纯逻辑              2853/2853       0 失败
lint_offline        0 错            445 文件
verify_delivery     19/19
verify_imports      497 文件
audit_anchors       565/565         (33 份脚本)
audit_source_guards 664 守卫
audit_doc_refs      全绿
audit_column_writers 553 列
verify_sample_data  10/10
前端 syntax-check    未在本轮跑       与既有 D 类缺口同源(无 node_modules)
```

**真库 pytest(用户授权 PG/Redis,2026-08-12):**

| 项 | 结果 | 说明 |
|---|---|---|
| `test_a45_batch20_draft_color_axis_db.py` | **9/9 通过** | 真库链路第一份执行证据 |
| `test_publish_lease_concurrency_db.py` | **7/7 通过** | 评审 P0-1 那 7 条发布并发真库用例 |
| `test_migrations.py` | **7/7 通过** | 升级 / 降级 + ORM 一致性 |
| `test_a45_batch14_*` (除 22 / 21) | **81/81 通过** | 14-19 证据查询 / 14-20 身份 / 14-20 stage4 |
| `test_a45_batch18_lease_visibility_db.py` / `21 / 22 / 23 / 24 / 28 / 31` | **81/81 通过** | 阶段 5 / 6 真库接缝 |
| `test_a47_plan_governs_db.py` | **0/10 通过** | **红 10 条,见下文** |
| `test_batch_lease_concurrency_db.py` / `_receipt_lifecycle` / `import_preview_version` / `poll_and_delist` / `publish_flow` / `publishing` / `a45_batch14_21_facts_stale_db` | **90/100 通过** | 10 红同源 |
| `test_api_generation.py` + `test_api_reviews.py` | **31 红** | 同源 |
| **真库 pytest 合计** | **51 failed, 397 passed, 0 skipped** | |

**红测试的同源根因(a49 §3.74 之前 vs 之后的接口语义变化):**

a49 §3.74 那一批整改做了一件语义改动:

- `generation_service` 的合规闸在 **模特模板未传** 时,旧版"告警后 return",
  改成 **拒绝创建任务**(§3.74 D-1)
- `_build_request` 里"拿不到模板图就退回自由上传模特图"的兜底被删
  (§3.74 D-1 后段)

这一改让 `tests/test_api_generation.py` / `test_api_reviews.py` /
`test_a47_plan_governs_db.py` / `test_a45_batch12_4_recovery_db.py` /
`test_a45_batch12_5_lease_and_billing_db.py` 等 **共 51 条真库用例的预期
行为过期了** —— 它们原本期望「自由模特图能跑出任务,只是产生假图」,
现在接口返回 `INPUT_INVALID` 422。

**这件事在仓库自陈里**没有**: §3.74 / §3.75 / STATUS / HANDOVER 均未点名
"老真库用例在 a49 之后是红的"。本轮把它记下来。

**修复方案**(本轮**未做**):
- 选项 a:把 `_create_task` 用例改成传 `model_template_id`(若 mock 那套能给出)
- 选项 b:把断言改成「422 + 错误码 INPUT_INVALID」 —— 接受新语义
- 选项 c:把这批用例标记为 xfail 并写明"a49 之前期望"

**关键判定:这 51 条红不算本轮整改引入的回归**,因为 a49 是评审整改批的产物,
本轮(2026-08-12)只是真跑了一遍 —— **从未跑过的门禁 = 不存在的门禁**。
修这些测试的债务属于 a49 之后的接力,本轮边界是「发现并如实标记」。

凭据通过环境变量注入(`POSTGRES_PASSWORD` / `REDIS_URL` / `TEST_DATABASE_URL`),
未写入仓库任何文件,跑完即丢。

**仍未消除、且不属于本轮范围(详见评审归档):**

| 项 | 来源 | 当前 |
|---|---|---|
| 主密钥轮换 | 评审 P0-4 | **未做**(本轮只移出仓库树);冻结交付前必须做 |
| FASHN 真端点首验 | 评审 P0-2 / §3.74 二-3 | 未做;`docs/PROVIDER-FASHN.md` §八清单 |
| 评分阈值校准 | 评审 P0-3 | 未做;≥20 条人工审核样本 |
| 第一个真实 HTTP 渠道 transport | 评审 P0-1 | 未做;业务先定平台 |
| 17 处 `<Alert+readError>` 迁移 | 评审 III.2 / §3.75 三 | 棘轮已上,迁移未做(本机无 Vitest) |
| 前端 tsc / Vitest / build | 仓库协作约束 | 本机无 node_modules;联网机器复跑 |
| Playwright / E2E | 任务 24 | 未开工 |
| Docker build × 2 | 本机无 Docker CLI | 真机 |
