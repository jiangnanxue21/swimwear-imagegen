# A45-batch14-16:溯源列落库,候选落盘写入(§4.8)

> **一句话结论:三个判定早就写好在读这两列,它们缺的一直是列本身。
> 本批落 `generation_task_id` / `generation_candidate_id` + 迁移 0038,
> 并在候选落盘那一处真的写进去 —— AC-22 的拦截从「只有过渡记号那一路
> 会命中」变成三路都命中。
> 纯逻辑 2284/2284、本批变异 **9/9 一次全红**、14-7 重跑 24/24、
> 锚点 265/265(13 份脚本)、交付 13/13、导入 381。
> **迁移仍然一次都没执行过。**

---

## 一、这一批和上一批是同一件事的第二半

14-15 落了归属外键,收掉两条欠账。本批落溯源列,收掉的是 §11 那批自己
写下的那句话:

> 「`legacy_kind=generation_candidate` 是**今天唯一存在的溯源痕迹** ——
> 少了它,这个模块今天一条也拦不住。」

三个判定读这两列的代码早就在树上(`getattr` 取,列不存在退回 None):

    evidence_rules.derive_evidence_class   §4.8 规则表的第一条
    provenance_conflict.verdict            AC-22「AI 图伪装拦截」
    provenance_conflict.may_fill_role      去重命中那一路的补角色闸

## 二、刻意不回填

存量的 AI 影子行有 `legacy_kind` / `legacy_id`,理论上回填得了。不做:

    回填要 join 候选表并**信任 `legacy_id` 指的就是候选行** ——
    而那是一个迁移期约定,不是外键;`legacy_id` 上没有任何约束。
    回填错的表现是**一张人工上传的样品被标成 AI 生成**,
    从此永久掉出识别输入,而没有任何地方会说为什么。

判定认「任一信号命中即算带溯源」,所以过渡记号那一路照旧生效,存量行
不回填也不会漏判。**两条记号并存不是重复**,变异 W3 钉着这一条:
真列落库之后过渡记号看起来冗余,而删掉它的表现是历史上所有 AI 影子行
一次性失去溯源 —— 新行照常拦得住,所以本机跑什么都正常。

## 三、SET NULL 的代价写在了模型注释里

清理生成任务不该连坐删掉素材文件。溯源断了之后那张图仍被
`source=AI_GENERATED` 拦着;真正靠这两列拦的是**被人重新上传成
MANUAL_UPLOAD 的那一份** —— 而它指着的是候选行,候选行不随任务删除消失。

## 四、变异 9/9 一次全红,但有两条是守卫自己先红的

本批没有出现「变异漏网」,反而是**写守卫的过程里自己红了两次**,
两次都值得记:

1. **上一批留下的一条守卫点名了 `heads == {"0037"}`。**本批加 0038 时它
   当场变红 —— 而红的原因是「有人正常地加了一条迁移」。已改成不变式
   (只有一个 head,不点名是哪一条)。CLAUDE.md 里「条数不写在这里」
   那条规矩说的是同一件事,而我上一批刚写下就违反了一次。
   好在这次是红不是静默过期。
2. **本批自己一条守卫的窗口切窄了**:迁移的列名由模块级 `_COLUMNS` 驱动
   (两列形状相同,循环比抄两遍安全),而我把名字查在 `upgrade()` 那一段里。
   查窄了是**正向**断言,所以它红了、有人看见了 —— 正是 14-14 那条门禁
   只管反向断言的理由。

## 五、跑过的

| 门禁 | 结果 |
|---|---|
| 纯逻辑 | **2284/2284**,0 失败,7 跳过 |
| 变异 `mutate_batch14_16.py` | **9/9 验红**,每条都由它自己那条守卫报出来 |
| 变异 `mutate_batch14_7.py` 重跑 | **24/24**(evidence_class 那批,本批让它的入参第一次有真列) |
| audit-anchors / audit-guards | 265/265(13 份脚本)/ 457 个守卫全绿 |
| verify-delivery | 13/13(迁移链单一 head = 0038) |

## 六、这一批**没有**做的

- **`evidence_class` 存储列 + CHECK 约束**:它要一次回填(给存量行算出
  派生值),而回填正是本批拒绝的那件事。判定与 CHECK 孪生都已备好,
  等一台有库的机器,在那里回填并当场验。
- **新去重键 `(spu_id, sha256)`**:要先给 `media_assets` 加 `spu_id` 外键
  (今天只有 `spu` 字符串)。那是又一次归属改造,单独一批。
- **`media.evidence_assets_for(spu_id, scope)`**:§5.1 的唯一取数入口。
  三列(归属 + 两条溯源)现在齐了,**它是下一批最该做的一件**。
- **迁移没执行过。**0037 / 0038 都是「已写、未执行」。
